# Executable Semantic Package Completion Map v1

Status: `working-draft`
Date: 2026-05-26
Scope owner: OxVBA compiler/VM/runtime/JIT/native-readiness
Primary package reference:
[`EXECUTABLE_SEMANTIC_PACKAGE_V1.md`](EXECUTABLE_SEMANTIC_PACKAGE_V1.md)
Completion workset:
[`../worksets/WORKSET_2026-05-27_TYPED_VM_METADATA_BUNDLE_COMPLETION.md`](../worksets/WORKSET_2026-05-27_TYPED_VM_METADATA_BUNDLE_COMPLETION.md)

## Purpose

Map each required executable semantic package fact to its current or target
home. This is the bridge from the full semantic model to implementation work in
bytecode, compiler metadata, `OxBundle`, VM setup, runtime helpers, COM/native
descriptors, and evidence.

This document deliberately absorbs the gap-matrix role so we do not create a
separate validation CSV until the rows are stable enough to need automation.

## Inputs

Semantic authorities:

- [`VBA_TYPE_SYSTEM_V1.md`](VBA_TYPE_SYSTEM_V1.md)
- [`VBA_EXPRESSION_CALL_SEMANTICS_V1.md`](VBA_EXPRESSION_CALL_SEMANTICS_V1.md)
- [`BYTECODE_VM_SEMANTIC_CONTRACT_V1.md`](BYTECODE_VM_SEMANTIC_CONTRACT_V1.md)
- [`VBA_SEMANTIC_TABLES_AND_BINDING_REFERENCE_V1.md`](VBA_SEMANTIC_TABLES_AND_BINDING_REFERENCE_V1.md)

Current implementation anchors:

- `crates/oxvba-compiler/src/bytecode.rs`
- `crates/oxvba-compiler/src/emit.rs`
- `crates/oxvba-compiler/src/project.rs`
- `crates/oxvba-compiler/src/resolve.rs`
- `crates/oxvba-runtime`
- `crates/oxvba-vm/src/interpreter.rs`
- `crates/oxvba-vm/src/semantics.rs`
- `crates/oxvba-com`
- `crates/oxvba-host/src/engine.rs`

## Gap Taxonomy

Use these exact labels when classifying rows:

- `implemented`: the fact exists in a durable metadata surface and is consumed
  where needed.
- `implemented-runtime-only`: runtime behavior exists, but the fact is not yet
  exposed as package metadata.
- `test-shortcoming`: behavior and metadata exist, but VM-runnable evidence is
  missing.
- `metadata-missing`: behavior may exist, but package metadata does not carry
  the fact.
- `VM-limitation`: the VM cannot execute the scoped behavior yet.
- `runtime-limitation`: the runtime carrier/helper cannot represent or execute
  the scoped behavior yet.
- `interop-limitation`: COM/native/export projection is missing or narrower
  than the semantic model.
- `oracle-required`: MS-VBAL is ambiguous or Office behavior must be observed
  before the row can close.
- `deferred-extension`: not VBA-compatible by default and only allowed behind
  an explicit extension gate.

## Completion Matrix Schema

Future machine-readable rows should use this shape:

```text
semantic_area
target_descriptor
current_location
current_support
gap_kind
VM_required
JIT_required
interop_required
test_anchor
spec_anchor
next_action
owner
```

## Initial Map

The `Current support` column names what is actually present today. The `Gap
kind` column uses the taxonomy above and should not be upgraded until the
package descriptor and VM evidence requirements are both satisfied.

| Semantic area | Target descriptor | Current location | Current support | Gap kind | Owner | Next action |
|---|---|---|---|---|---|---|
| Bytecode control stream | instruction stream, entry PCs, slot counts | `crates/oxvba-compiler/src/bytecode.rs`; `Bytecode`; `Instruction`; `slot_count`; `user_slot_count` | Durable bytecode stream and slot counts exist and VM executes them. | `implemented` | compiler-bytecode / VM | Document per-op semantic contract and keep opcode-family rows in sync. |
| Descriptor registry and identity | descriptor family ids, type registry ids, canonical digests | `crates/oxvba-compiler/src/descriptor_identity.rs`; `DescriptorFamily`; `VbaTypeId::descriptor_id`; `vba_type_registry`; `VmPackageIdentityEvidence::descriptor_identities` | Package-owned descriptor identity helpers now live in the compiler/package crate rather than the VM. The current `VbaTypeId` registry has stable type descriptor ids and default carrier rows for the implemented declared type enum, descriptor family ids are stable strings, and VM evidence emits canonical descriptor identity rows for bytecode, procedures, signatures, slots, call sites, array shapes, UDTs, object routes, interop, and lifecycle rows reached by current package evidence. Automated tests prove type id determinism, digest changes when descriptor fields change, and VM evidence emission. | `metadata-missing` | compiler-package / VM evidence | Extend the registry as later beads add enum, imported COM, native/export, error, helper, host-policy, and unsupported descriptor families; do not allocate semantic ids in VM/JIT consumers. |
| Project/import bundle context | project, module option, compile context, reference/import, source-map, native-library, and host-capability facts | `OxBundle` v11 `BundleProjectContext`; `BundleCompileContext`; `OxBundle::from_compiled_project_with_manifest`; `VmPackageIdentityEvidence::project_context` | Bundle v11 now serializes project name/kind/id/fingerprint, module facts, `Option Explicit`/`Option Compare`/`Option Base`/`Option Private Module`, compressed `Def*` default-type families, manifest and builtin conditional constants including `VBA7`/`Win64`, target pointer width and `LongPtr`/`LongLong` facts, manifest references with deterministic resolution status, referenced-project summaries, compiler source maps, native Declare library facts, host capability requirements derived from bytecode, package diagnostics for unresolved project/typelib imports, and gap classifications. VM evidence exposes these facts and includes project context in the package digest. Type-library projection completeness, native ABI completeness, host-policy consumption, and imported binding behavior remain later bead work. | `metadata-missing` | compiler-package / VM evidence | Expand type-library/native/import descriptors as `bd-tvmb.7`/`bd-tvmb.8`/`bd-tvmb.10` add behavior evidence; keep unsupported imports package diagnostics rather than runtime guesses. |
| Procedure runtime metadata | procedure ids, params, return slot, entry | `crates/oxvba-compiler/src/emit.rs`; `ProcedureRuntimeMetadata`; `ProcedureSignatureDescriptor`; `crates/oxvba-vm/src/interpreter.rs`; `crates/oxvba-host/src/engine.rs`; `VmExecutionPackage`; `VmPackageIdentityEvidence` | Procedure name, module, entry PC, line maps, param slots/types, return slot/type, slot role metadata, and first signature descriptor view exist. VM package identity evidence now records package origin, package digest, bytecode digest, procedure ids, procedure descriptor ids/digests, entry PCs, slot counts, signature descriptor digests, and canonical descriptor identity rows for package execution; host source/project/bundle/callable VM paths can expose the same recorded identity. Broader signature behavior and call binding still need descriptor-driven evidence. | `metadata-missing` | compiler-emit / VM package | Expand signature/call descriptor coverage and VM behavior evidence without moving semantic id allocation into VM/JIT consumers. |
| Slot declared type metadata | `SlotTypeDescriptor` | `ProcedureRuntimeSlotMetadata`; `ProcedureRuntimeSlotKind`; `ProcedureRuntimeMetadata.param_types`; compiler resolver declaration maps; `TempSlotAllocator`; `OxBundle` v10; `VmExecutionPackage::slot_type_descriptors`; `VmPackageIdentityEvidence`; `conformance/vm_package/identity_seed` | `SlotTypeDescriptor` rows are populated for parameters, locals, return slots, compiler-generated fixed-array element slots, expression temporaries, and nominal UDT base slots in current procedure metadata, serialized through new bundles, upgraded from v3/v4 bundles, exposed by VM package setup, and reported in VM evidence with per-procedure canonical descriptor digests and slot descriptor identity rows. VM-runnable fixtures assert primitive scalar, `String`/`BStr`, declared `Variant`, current UDT field-alias and UDT base-slot descriptor tokens, fixed/dynamic local array slot tokens, and generic `Object` slot tokens alongside value snapshots. Temporary declared types and some non-slot shape facts remain incomplete or explicitly `Unknown`. | `metadata-missing` | compiler-emit / VM evidence | Fill richer object/class/interface shape facts without changing VM slot storage; keep new descriptor families package-owned as they are added. |
| Primitive carriers | scalar carrier/layout descriptors | `DeclareParamType`; resolver `BoundType`; runtime arithmetic/coercion helpers; VM `RuntimeSlot`/`Variant` snapshots; `SlotTypeDescriptor` carrier hints; `ProcedureRuntimeMetadata::carrier_layouts`; `VmPackageIdentityEvidence::carrier_layout_evidence`; `vba_type_registry` | Primitive declarations, helper behavior, package slot carrier hints, current `VbaTypeId` type-registry/default-carrier rows, and `OxBundle` v12 carrier-layout descriptors are fixture-backed. VM evidence now emits carrier-layout descriptor digests for primitive scalar, String/BSTR, Variant, object, UDT, and Decimal96 Variant-subtype carriers. Selected v14 operator/coercion descriptor ids exist; behavior-driving carrier consumption remains later work. | `metadata-missing` for broader carrier-consuming execution | compiler-resolve / runtime / VM package | Bind remaining operator/coercion/helper rows to package carrier/layout descriptors before typed execution consumes them. |
| Declared Variant | COM-compatible Variant carrier descriptor | `crates/oxvba-runtime/src/variant.rs`; `Variant`; `VarType`; COM projection in `oxvba-com`; `SlotTypeDescriptor` carrier hints; `CarrierLayoutDescriptor` | Runtime Variant carrier exists, snapshots use it, declared Variant slots have package descriptor evidence as `VbaTypeId::Variant` with `RuntimeCarrierKind::Variant`, and carrier-layout evidence records `VT_VARIANT` compatibility. Full COM VARIANT projection descriptors are not yet package-owned. | `metadata-missing` | runtime / VM package / COM | Bind declared `Variant` slots to boundary-specific COM VARIANT projection descriptors and descriptor ids. |
| Decimal | Variant subtype/runtime payload | `crates/oxvba-runtime/src/decimal.rs`; `Decimal96`; `Variant::from_decimal96`; COM Decimal projection; `RuntimeCarrierKind::Decimal96VariantSubtype`; `ValueStateDescriptor` | Decimal payload support exists as a Variant subtype. Declared `Decimal` storage is represented as an extension-gated `Decimal96VariantSubtype` carrier with `VT_DECIMAL` carrier-layout evidence and a VM-visible `DecimalVariantSubtype` value-state diagnostic; it is not surfaced as an ordinary declared slot type. Full Decimal operator/projection descriptors remain later work. | `metadata-missing` | runtime / compiler-type-system | Keep Decimal under Variant-subtype carrier/value-state rows; add operator, coercion, COM/native projection evidence before typed execution consumes Decimal paths. |
| Strings | `BStr`, fixed string descriptors | `crates/oxvba-runtime/src/bstr.rs`; string bytecode/helpers; `DeclareParamType::String`; `SlotTypeDescriptor` carrier hints; `UdtTypeDescriptor` fixed string field rows; `VmPackageIdentityEvidence::lifecycle_evidence` | Runtime `BStr`, declared String params/locals, and first `RuntimeCarrierKind::BStr` package descriptor evidence exist. UDT field descriptors now record fixed-length `String * N` lengths and BSTR cleanup ownership flags for the VM-runnable UDT seed. VM lifecycle evidence now asserts selected non-UDT `String` slot cleanup rows (`LIFE-BSTR-SLOT`) plus selected UDT variable/fixed BSTR field cleanup rows. General fixed-length string assignment/truncation behavior, helper-temp failure cleanup, and lifetime counters are still missing. | `metadata-missing`; `VM-limitation` | runtime / compiler-emit / VM evidence | Add fixed/variable string behavior evidence, helper cleanup, and lifetime counters beyond selected slot/UDT lifecycle rows. |
| Arrays | `ArrayShapeDescriptor` / bounds / storage kind | `crates/oxvba-runtime/src/safe_array.rs`; `RuntimeArrayElementType`; resolver `BoundArrayDescriptor`; `ProcedureRuntimeMetadata::array_shapes`; `UdtTypeDescriptor` fixed array field bounds; `OxBundle` v10; VM array instructions; `VmPackageIdentityEvidence::array_shape_evidence`; `VmPackageIdentityEvidence::lifecycle_evidence`; `conformance/vm_package/identity_seed/vmr05_array_shape_bounds.bas`; `conformance/vm_package/identity_seed/vmr05_udt_descriptor_members.bas` | Package array-shape rows now record array name, base slot, rank, declared bounds where known, `Option Base`, element type/carrier, storage kind (`StaticFixed`, `Dynamic`, `ParamArrayPack`), bounds provenance, allocation/erase/preserve policy, and element lifecycle classification. VM evidence asserts fixed/static descriptor bounds, explicit `0 To 2` bounds, dynamic `ReDim 2 To 4` runtime SAFEARRAY bounds, selected array/SAFEARRAY lifecycle rows, unallocated fixed-array base slots, and ByRef scalar observations in a VM-runnable seed. Package execution now consumes rank-1 `StaticFixed` array descriptors for fixed/static `LBound`/`UBound`, while raw bytecode keeps the old runtime error 13 baseline for unallocated fixed-array base slots. UDT field descriptors now record fixed-array field bounds and flattened field-alias slots. Compiler/VM unit evidence covers multi-rank descriptor rows, but VM-runnable multi-rank fixtures, bounds-error evidence, and COM/native SAFEARRAY projection descriptors remain incomplete. | `metadata-missing`; `VM-limitation`; `test-shortcoming` | runtime / compiler-resolve / VM evidence | Add VM-runnable multi-rank and bounds-error fixtures, then expand descriptor-backed array behavior only with selected VMR-06 fixtures. |
| UDTs | nominal type, fields, copy/init/cleanup | resolver/project UDT metadata; `ProcedureRuntimeMetadata::udt_types`; `UdtTypeDescriptor`; VM UDT field/copy paths; type-system draft; `VmPackageIdentityEvidence::udt_descriptor_evidence`; `VmPackageIdentityEvidence::lifecycle_evidence`; `conformance/vm_package/identity_seed` | UDT behavior and compiler metadata are partial. VM-runnable seed fixtures now record the current flattened base-slot plus field-alias descriptor shape and first nominal `UdtTypeDescriptor` rows, including descriptor ids, instances, field order, descriptor layout index, recursive init policy, primitive/string/nested carriers, fixed string lengths, fixed array field bounds, field aliases, fieldwise-copy classification, and cleanup ownership flags. The selected VMR-06 lifecycle evidence lane now records UDT owning-field cleanup obligations for variable and fixed-length BSTR fields, including success, branch, error, helper, and deopt obligation tokens. Byte-offset layout, explicit cleanup-stack execution, and descriptor-driven VM field/copy execution are still missing. | `metadata-missing`; `test-shortcoming` | compiler-resolve / VM / runtime | Add byte-offset/ABI layout and broader lifecycle cleanup evidence, then route selected UDT field/copy behavior through descriptors only after VM fixtures prove parity. |
| Enums | nominal enum/member facts | resolver enum block parser; module constants lowering; `NameBindingDescriptor`; `VmPackageIdentityEvidence::name_binding_evidence` | Enum declarations are still lowered as `Long`-compatible module constants for execution, but package name-binding evidence now emits selected nominal enum type/member rows (`NAME-BINDING-ENUM-TYPE`, `NAME-BINDING-ENUM-MEMBER`) with visibility, member order, explicit-value, value, and `Long` carrier tokens. A first compiler/VM unit test proves enum facts travel with aggregate descriptors. A dedicated `EnumTypeDescriptor`, declared enum slot typing, and enum-specific coercion/diagnostic rows remain missing. | `metadata-missing`; selected rows package-described | compiler-resolve / package evidence | Promote selected name-binding rows into a dedicated enum descriptor once declared enum storage/coercion lanes are implemented. |
| Objects/classes/interfaces | object/class/interface descriptors | `ObjectRef`; `RuntimeClassDescriptor`; `RuntimeInterfaceDescriptor`; `ProcedureRuntimeMetadata::object_types`; `ObjectTypeDescriptor`; project dynamic object routes; COM `WithEvents` routes; COM typelib projection; `VmPackageIdentityEvidence::object_descriptor_evidence`; `conformance/vm_package/identity_seed/vmr05_object_descriptor_identity.bas`; `docs/validation/VBA_OBJECT_MEMBER_BINDING_SEED_TABLE_V1.csv` | Generic `Object` slot descriptors now record `Nothing` initial state, `ObjectRef` carrier, no activation/event binding, and per-slot instance evidence. VM runtime project evidence records VM-capable source-project class route identities, implemented interface aliases, and imported COM `WithEvents` route identities when compiled project route tables are supplied. The first object/member binding seed rows now name `Set`, `Nothing`, class/interface routes, default members, property accessors, COM dispatch, early-bound COM, events, and `WithEvents` obligations. Descriptor-driven `As New` activation, default instances, class/interface declared slot ids, imported COM class/interface descriptors, object cleanup/lifetime maps, and object/member binding consumption remain incomplete. | `metadata-missing`; `test-shortcoming` | runtime-object / compiler-project / COM | Fill descriptor coverage for `As New`, default instances, imported COM classes/interfaces, object lifetime, and object/member binding before behavior-driving consumption. |
| Procedure signatures | full `ProcedureSignatureDescriptor` | `ProcedureRuntimeMetadata::procedure_signature_descriptor`; `ParameterDescriptor`; `ProcedureSignature` and `VbaTypeDescriptor` in `project.rs`; `ProjectDynamicMemberRoute`; `VmExecutionPackage::procedure_signature_descriptors`; `VmPackageIdentityEvidence::signature_call_evidence` | Package signature descriptor view exists for procedure kind, parameter order/slot/type, parsed ByRef/ByVal mode, source mechanism when known, resolved runtime mechanism, Optional/default and Variant-missing policy, ParamArray element/bounds shape, return type/slot, property group, property value ByVal semantics, and class hidden-receiver/`Me` metadata. VM package evidence now records signature descriptor digests and seed call-behavior observations; the selected VMR-06 call-entry coercion consumes signature facts and has a v14 `CoercionDescriptor`, but broader descriptor-driven call execution remains incomplete. | `metadata-missing` for broader call binding | compiler-project / compiler-emit / VM evidence | Route VM call binding through package facts only after call-site/coercion descriptors are complete enough for each selected behavior lane. |
| Expression classification | `ExpressionSemanticsDescriptor` | resolver `BoundExpr`; compiler emit lowering; `ProcedureRuntimeMetadata::expression_semantics`; VM evidence | Bundle v14 now emits package-owned expression classification descriptors for current lowered expression shapes, including literals, variables, array elements, function/procedure results, intrinsic results, operator/comparison results, temporaries, and pointer buffers. The descriptors carry declared type, value-state tags, carrier hints, source context, and default-member policy tokens into VM identity evidence. Property/default-member expression coverage beyond current lowered forms remains incomplete. | `metadata-missing` for broader member/default shapes | compiler-resolve / package | Expand expression descriptors to all source forms, member chains, `With` expressions, and default-member expressions before any JIT-local expression lookup is allowed. |
| Let/Set coercion | `CoercionDescriptor` | runtime coercion helpers; assignment intent bytecode; resolver/VM behavior; `ProcedureRuntimeMetadata::coercions`; `docs/validation/VBA_COERCION_SEED_TABLE_V1.csv` | Bundle v14 now binds selected seed/helper rows to package-owned `CoercionDescriptor` ids for Let/Set assignment, explicit conversion intrinsics, truthiness, property value ByVal assignment, and the selected `Long` to declared `Double ByVal` call-entry shape. VM evidence emits coercion ids, source/target declared types, value-state tags, helper ids, and gap classifications. Broader descriptor-driven coercion execution, full truth-table extraction, locale-sensitive conversion, and COM/native boundary projection remain incomplete. | `metadata-missing`; selected rows package-described | runtime-coerce / compiler-emit / VM | Route only selected behavior-changing coercions through descriptors after fixture evidence; keep compatibility-risk rows oracle-gated. |
| Operators | `OperatorSemanticsDescriptor` | bytecode arithmetic/string/compare instructions; runtime arithmetic helpers; VM semantics; `ProcedureRuntimeMetadata::operator_semantics`; `docs/validation/VBA_OPERATOR_SEED_TABLE_V1.csv` | Bundle v14 now emits package-owned operator descriptors for current lowered arithmetic, `&`, comparisons, truthiness, Boolean `Not`/`And`/`Or`, branch predicates, selected i32 fast paths, and an explicit deferred `IIf` evaluation-order row. VM evidence reports seed row ids, helper ids, compare mode, eager/non-short-circuit evaluation-order tokens, and gap classifications. Full VBA operator parity, locale/collation, suspicious current helper behaviors, and descriptor-driven operator execution remain incomplete. | `metadata-missing`; `oracle-required` for risky rows | runtime-arithmetic / VM / compiler-bytecode | Expand table rows and VM/oracle evidence before specializing operators in JIT/native lanes. |
| Assignment/property | property accessor and value-param descriptors | `ProjectMemberCallDescriptor`; `ProjectDynamicMemberRoute`; assignment intent bytecode; `ProcedureRuntimeMetadata::name_bindings`; `ProcedureRuntimeMetadata::object_member_bindings`; VM behavior; `docs/validation/VBA_OBJECT_MEMBER_BINDING_SEED_TABLE_V1.csv` | Bundle v14 now emits name-binding policy descriptors and object/member binding descriptors for selected lanes: local procedure-scope precedence, compiler-owned ambiguity diagnostics, `Property Get`/`Let`/`Set` accessor rows, property value ByVal policy, Set-assignment seed rows, and default-member fallback policy rows reached by call sites. Full accessor pairing, default-member registry, `With` context, imported member precedence, object identity/lifetime, and VM member-binding consumption remain incomplete. | `metadata-missing`; `test-shortcoming` | compiler-project / VM object binding | Broaden member binding descriptors and diagnostics, then consume them only in selected VM-backed object/property lanes. |
| Call sites | `CallSiteDescriptor` / `ArgumentBindingDescriptor` | compiler call lowering; `ProcedureRuntimeMetadata::call_sites`; `VmExecutionPackage::call_site_descriptors`; `VmPackageIdentityEvidence::call_site_evidence`; `ProjectDynamicParamRoute`; VM call binding | Seed package call-site rows and VM evidence exist for top-level project procedure/default-member fallback calls, including target kind, named/positional/omitted/ParamArray source shape, ByRef alias/writeback, ByRef expression temp/no-writeback, ByVal copy, Optional default, Optional `Variant` missing-policy metadata, empty/non-empty ParamArray packs, fixed-array materialization, property value ByVal classification, default-member fallback policy, invocation syntax (`Call`, no-`Call`, expression call, synthetic property assignment), argument evaluation order, current diagnostic-policy ownership for compiler-owned 448/449/450 invalid-call cases, return copyout, and selected call-entry coercion/helper row ids. Package execution now consumes the selected VMR-06 descriptor shape for direct `Long` variable to declared `Double ByVal` call entry while raw bytecode execution remains the pre-VMR06 baseline. Optional `Variant` missing behavior is still not VM-compatible. Expression call-site rows beyond current direct lowering, external Declare/COM coverage, canonical descriptor ids, broader descriptor-driven VM call binding, and Optional-missing runtime behavior remain incomplete. | `metadata-missing`; `VM-limitation` | compiler-emit / VM call binding | Expand coverage to remaining expression calls, external/COM calls, and default-member policy details; fix or explicitly defer Optional-missing runtime behavior; route later call-binding behavior through package facts only with a selected VMR-06 fixture and gap classification. |
| Error routing | error state-machine descriptors and resume targets | VM error fields; bytecode error instructions/patches; `ErrorFrame` in `interpreter.rs`; `Err` runtime surface; COM/native/host boundary result paths | Runtime error state and resume behavior exist, but package-level error/resume maps and descriptor evidence are missing. The package must model errors as control flow plus mutable frame/`Err` state: enabled vs active handlers, fault sites, legal resume targets, `Err` reset/clear points, caller unwinding, call-out `Resume Next` behavior, fallible helper edges, and COM/native/host projection. | `implemented-runtime-only`; `metadata-missing`; `test-shortcoming` | VM / compiler-emit / oracle | Add package error/resume maps, VM evidence schema, and oracle-backed rows for VBA quirks before any JIT lowering consumes error descriptors. |
| Cleanup/lifetime | cleanup obligation map | runtime carriers; VM branch/return/error paths; COM/native marshaling helpers; `VmPackageIdentityEvidence::lifecycle_evidence`; `docs/validation/VBA_LIFECYCLE_CLEANUP_SEED_TABLE_V1.csv` | Cleanup behavior exists in scattered runtime and boundary paths. The first lifecycle seed rows now classify primitive, Variant/Decimal payload, BStr, SafeArray, ObjectRef, UDT field, ByRef temp/writeback, COM dispatch temp, native writeback buffer, and deopt cleanup obligations. VM lifecycle evidence now records the selected descriptor-backed UDT owning-field cleanup path for BSTR fields, including success, branch, error, helper, deopt, and runtime alias-carrier observations. Canonical package-owned lifecycle descriptor ids, explicit cleanup-stack execution, and boundary cleanup observations remain missing. | `metadata-missing`; `test-shortcoming` | runtime / VM / COM-native | Add canonical slot lifecycle, cleanup, error-exit, boundary, and deopt descriptors and wire VM evidence to observe more selected cleanup paths. |
| COM projection | COM descriptor set | `crates/oxvba-com`; `ProjectDynamicObjectRoute`; type-library projection in `project.rs`; VM COM bridge paths; `VmPackageIdentityEvidence::interop_descriptor_evidence`; `crates/oxvba-host/tests/jit_v2_tracer_vm_seed.rs`; `docs/validation/VBA_OBJECT_MEMBER_BINDING_SEED_TABLE_V1.csv` | Windows COM bridge and typelib/project routes exist. VM evidence now records hosted TB06/TB07 COM activation/dispatch instruction expectations, including early-bound flag, selector source, arity/named-arg counts, and runtime-owned HRESULT/EXCEPINFO/ArgErr classification. The object/member seed rows now split late-bound dispatch, named/default selector shape, early-bound dispatch, vtable strategy, and `WithEvents` routing. Package descriptors are still not the unified semantic source for all COM late/early/event paths, and boundary result/cleanup evidence is incomplete. | `interop-limitation`; `metadata-missing`; `test-shortcoming` | COM / compiler-project / VM host | Project from semantic descriptors, not raw wire types; promote COM selector/member/result/cleanup facts from evidence into package descriptors before JIT entry. |
| Native Declare | native ABI descriptor | `ExternalCallDescriptor`; `ExternalCallWriteback`; HAL `DynLinkDescriptorView`; host-backed native runner; `VmPackageIdentityEvidence::interop_descriptor_evidence`; `crates/oxvba-host/tests/jit_v2_tracer_vm_seed.rs` | Native descriptors and scalar/writeback lanes exist. VM evidence now records current TB08 descriptor and invoke facts for library/alias/name, return/parameter tokens, ByRef flags, argument slots, and writeback slots. General Automation `Variant`/`SAFEARRAY` declared-parameter ABI support and cleanup/buffer ownership evidence remain incomplete. | `interop-limitation`; `metadata-missing`; `test-shortcoming` | compiler-bytecode / HAL / VM native | Audit scalar, BSTR, Variant, SAFEARRAY, and ByRef coverage and split residual ABI gaps into package descriptor rows and VM evidence. |
| Exported callable | inbound/outbound ABI descriptor | wrapper/export metadata; native export/XLL docs; tracer seed | Export metadata exists for wrapper/add-in lanes; VM/package inbound/outbound callable projection descriptor is not first-class. | `interop-limitation` | wrapper/export / host / package | Define package-level inbound projection, cleanup, writeback, and error return policy. |
| Host capability policy | host requirement descriptors | HAL `HostPolicy`; `CapabilityId`; host services; deterministic unsupported diagnostics; `BundleProjectContext::host_capabilities`; `VmPackageIdentityEvidence::project_context` | Host policy exists at runtime. Bundle v11 now records digestable host capability requirements for host-sensitive bytecode and native Declare calls, and VM evidence exposes those requirements through project-context evidence. Policy evaluation, unsupported host diagnostics, and host capability descriptor ids are not yet unified package-owned behavior. | `metadata-missing` | HAL / host / package | Promote host capability requirements into canonical host-policy descriptors and unsupported diagnostics before behavior-driving consumption. |
| Evidence schema | VM/JIT/package evidence | `scripts/run-jit-v2-tracer-fixtures.ps1`; `crates/oxvba-host/tests/jit_v2_tracer_vm_seed.rs`; `conformance/vm_package/identity_seed`; VM snapshot helpers; `crates/oxvba-host/src/engine.rs`; `VmPackageIdentityEvidence`; `docs/validation/VBA_LIFECYCLE_CLEANUP_SEED_TABLE_V1.csv`; `docs/validation/VBA_OBJECT_MEMBER_BINDING_SEED_TABLE_V1.csv` | VM seed fixtures, retained snapshots, package/procedure/bytecode identity evidence, host package-identity snapshot/session evidence, first slot descriptor evidence with per-procedure digests, descriptor-token assertions for primitive/String/Variant/current-UDT-field/array/object slots, signature/call observations, call-site descriptor evidence including v13 invocation syntax and diagnostic-policy tokens, array-shape descriptor evidence, UDT descriptor evidence, object descriptor evidence, lifecycle evidence for selected UDT owning-field cleanup, first COM/native interop descriptor evidence, bundle v11 project-context and compile-context evidence, bundle v12 carrier-layout and value-state evidence, bundle v14 expression/operator/coercion/name/member descriptor evidence, first cleanup-obligation seed rows, and first object/member binding seed rows exist. Full boundary result/cleanup observations, host-policy diagnostics, explicit cleanup-stack execution evidence, and broader expression-call/object-member descriptor coverage remain incomplete. | `test-shortcoming` | VM evidence / conformance | Record boundary result/cleanup observations, host-policy diagnostics, explicit cleanup-stack checks, value-state propagation rows, and remaining expression-call/object-member descriptor coverage in VM evidence. |

## VMR-04 Call Fixture Gap Classification

These rows classify the call-fixture mismatches and uncovered call-shape gaps
before any VMR-06 behavior-affecting call-binding change may consume the
descriptors. A row marked as a gap is not a compatibility claim; it is a
decision about the next owner and evidence required before changing behavior.

| Evidence anchor | Current descriptor/evidence | Current VM behavior | Classification | Required before behavior change | Owner / follow-up |
|---|---|---|---|---|---|
| `VMR01_TYPED_FUNCTION`; `VMR04_CALL_ARGUMENT_BINDING`; `VMR04_BYREF_EXPRESSION_FORMS` ByRef literal/expression arguments | `CallSiteDescriptor` records invocation syntax, source argument order, `binding=byrefalias`/`writeback-required` for direct variables, and `binding=byrefexpressiontemp`/`no-writeback-temp` for statement-level parenthesized force-ByVal, arithmetic expression, literal, and function-result source forms; signature evidence records `gap:byref-copyback-not-observed` for temporary forms. | The compiler lowers direct variable arguments as caller aliases with writeback. It lowers the currently VM-runnable non-variable forms into the callee parameter slot and emits no caller writeback. Exact typed ByRef variable mismatches are rejected by compiler diagnostics. | `oracle-required` for Office/MS-VBAL confirmation of each source form; `test-shortcoming` for property/default-member result forms not yet VM-runnable in the seed. This is not treated as a VM bug unless the spec/oracle says a covered source form should reject or route differently. | Use `VMR04_BYREF_EXPRESSION_FORMS` as the positive VM baseline for variable alias, parenthesized force-ByVal, expression, literal, and function-result behavior. Add Office-backed oracle evidence for property/default-member results and any source forms whose parse shape cannot be represented by current VM fixtures before broad descriptor-driven call binding consumes them. | `bd-iave.6.4` for the current VM-runnable baseline; object/default-member residuals remain with COM/native descriptor work in `bd-iave.7.4` and later object-member binding rows; VMR-06 only after the oracle decision. |
| `VMR04_CALL_ARGUMENT_BINDING` `TakeDouble(ByVal value As Double)` called with a `Long` variable | Signature and call-site descriptors know the parameter declared type is `Double` and the binding kind is `ByValCopy`; call-site evidence now records `COERCE-CALL-BYVAL-DECLARED-TARGET`, `COERCE-LET-NUMERIC-WIDEN`, and `oxvba_runtime::coerce_to` for the selected `value` argument; bundle v14 emits a matching `CoercionDescriptor` for the selected call-entry row. | Package execution now applies the selected descriptor-driven call-entry coercion and the callee observes `VarType(value)=5`; raw bytecode execution still observes the old `VarType(value)=2` baseline because it has no package metadata. `main:byvalcoerced` remains `f64:4.5`, caller `seed` remains unchanged, and unrelated call-binding observations remain unchanged. | Selected shape implemented; residual `metadata-missing` for broader call-entry coercions. | Keep the narrow match as the VMR-06 proof point. Add additional fixtures before expanding to other source/target types, expression forms, properties, COM/native, Optional missing, or error-routing coercions. | `bd-iave.9.2` implemented the selected package path; `bd-iave.9.4` added table/helper-id evidence; `bd-tvmb.5` added the package `CoercionDescriptor`; later VMR-06 beads own broader shapes. |
| `VMR03_SIGNATURE_CALLS`; `VMR04_CALL_ARGUMENT_BINDING` explicit Optional `Long = 7` | Signature and call-site descriptors record `optional-default=i32-7`; VM evidence records `optional-default-i32-observed`. | The callee receives the explicit default value and return copyout is observed. | No mismatch for the scoped fixture. Broader optional-default types remain `metadata-missing` until coercion/default-value descriptors are richer. | Keep this as the positive control while broadening default-value rows beyond `i32`. | Semantic table rows (`bd-iave.8`) before broad default-value behavior changes. |
| `VMR04_CALL_ARGUMENT_BINDING` `Optional ByVal value As Variant` omitted | Signature descriptor records `VariantMissingError448` with `PreserveMissingArgumentState`; call-site evidence records `optional-default=variant-missing-error-448`; package v12 value-state descriptors can now emit a `MissingArgument` row for optional Variant parameters. | Current lowering emits the ordinary optional default path and the callee observes `VarType(value)=2`, not a preserved missing-argument state. | `VM-limitation`; `runtime-limitation` until `IsMissing`/introspection evidence and call-binding consumption use the package missing-argument state. | Confirm `IsMissing`/`VarType`/error behavior against MS-VBAL or Office oracle, then route omitted Optional `Variant` call binding through the package missing-argument state instead of `LoadConstI32 0`. | VMR-06 call binding (`bd-iave.9.1`/`bd-iave.9.2`) plus value-state/coercion rows (`bd-iave.8.1`). |
| `VMR03_SIGNATURE_CALLS`; `VMR04_CALL_ARGUMENT_BINDING`; `VMR04_CALL_DIAGNOSTIC_DESCRIPTOR_BASELINE` empty and non-empty `ParamArray` | Signature and call-site descriptors record `ParamArray` shape and element counts; VM evidence records pack observations, empty/non-empty upper-bound snapshots, and the supported named fixed-argument plus positional `ParamArray` pack shape. | The scoped fixtures run and observe empty upper bound `-1`, two-element upper bound `1`, and named fixed argument binding with a positional pack. Named direct binding to a `ParamArray` parameter is rejected by the current compiler diagnostic fixture. | No mismatch for the scoped fixture. Element coercion, omitted elements, lifetime, and non-Variant element projection remain `metadata-missing` / `test-shortcoming`. | Keep the named `ParamArray` rejection diagnostic as the current baseline; add rows/fixtures for element coercion, omitted elements, cleanup/lifetime, and non-Variant boundary projection before broad call binding claims. | VMR-05 descriptors, semantic table rows (`bd-iave.8`), or JIT-readiness gates (`bd-iave.10`) depending on array, lifecycle, or interop surface. |
| `VMR04_CALL_DIAGNOSTIC_DESCRIPTOR_BASELINE`; `diagnostic_manifest.csv` named arguments, duplicate mappings, omitted required parameters, wrong argument counts, and invalid named/`ParamArray` forms | The positive VM fixture proves descriptor evidence for named source arguments, argument evaluation order, ByRef alias writeback, ByVal copy, `ParamArrayPack` binding, and package diagnostic-policy rows naming the current compiler owner for missing required (`449`), too many/positional-after-named (`450`), and unknown named/duplicate mapping/named `ParamArray` (`448`) classifications. Diagnostic manifest rows retain the failing compile fixtures. | Current early-bound project procedure calls reject those invalid source forms during compilation, so no VM package executes for the diagnostic rows. The VM-runnable baseline proves the successful named/pack shape that is currently accepted. | `current-compiler-diagnostic` for invalid early-bound source forms; `metadata-missing` for future package-owned diagnostic descriptors if VMR-06 moves the decision into VM call binding. This is not a VM runtime limitation while the compiler is the active owner of these diagnostics. | Preserve the diagnostic manifest as a guardrail. Before descriptor-driven VM call binding changes diagnostic ownership, add package diagnostic descriptors or VM runtime diagnostic fixtures that preserve the same 448/449/450 classification. | `bd-iave.6.5` complete for the current baseline; VMR-06 owns any later diagnostic ownership move. |
| Property/default-member, COM, native Declare, and exported-callable call-site shapes | Property value ByVal has seed evidence; generic object and route identity evidence now exists; COM/native instruction and descriptor expectation evidence exists for the hosted TB06 through TB08 seeds; object/member seed rows now identify the shared binding families. COM/native/export descriptors are still not unified call-site package facts for the VM package fixtures. | Current external and object paths run through specialized project/COM/native routes rather than a complete shared call-site descriptor contract. | `metadata-missing`; `test-shortcoming`; `interop-limitation` for COM/native/export boundaries. | Promote selector/default-member/named-arg/projection/writeback evidence into shared package descriptors before JIT or native planning consumes them. | JIT-readiness gates (`bd-iave.10.3`) and VMR-06 only after behavior-changing owners are chosen. |

## Tracer Bullet Package Fact Readiness

`VM seed status` records what can run under the current VM. `Package fact gap
kinds` records why the tracer is not yet executable-JIT-ready. The tracer
matrix at
[`../validation/JIT_V2_TRACER_BULLET_MATRIX_V1.csv`](../validation/JIT_V2_TRACER_BULLET_MATRIX_V1.csv)
must carry the same gap labels.

| Tracer | VM seed status | Required package facts | Package fact gap kinds | Readiness classification |
|---|---|---|---|---|
| TB01 Primitive typed scalar loop | `vm-ready` | package/procedure identity; slot descriptors for `Long`, `Double`, and `Boolean`; primitive carrier/layout descriptors; operator/coercion rows; loop PC/source maps; descriptor evidence | `metadata-missing`; `test-shortcoming` | VM source and retained snapshots run, and package/procedure/bytecode identity plus primitive/String/Variant slot descriptor-token evidence now exists. The JIT gate now requires this VM package slot/carrier evidence before `ProcLoweringIr` consumes TB01. Selected v14 coercion/operator descriptors now exist, but full carrier-consuming execution, full truth tables, and verifier evidence are not complete yet. |
| TB02 UDT struct field/copy | `vm-ready` | UDT descriptor id; nominal type identity; field order, offsets, and carriers; whole-copy semantics; field cleanup/deopt maps; descriptor evidence | `metadata-missing`; `test-shortcoming` | VM source and projected field snapshots run. Package seed fixtures now record nominal UDT descriptor ids, instances, field order/carriers, descriptor layout-index tokens, recursive init policy, nested UDT references, fixed string lengths, fixed array field bounds, field-alias slots, fieldwise-copy classification, and cleanup ownership flags. The lifecycle seed table names primitive, owning, and fixed-array UDT field cleanup obligations, and VM lifecycle evidence now asserts selected BSTR-owning UDT field cleanup obligations for variable and fixed-length string fields. The JIT gate now requires this UDT descriptor/lifecycle evidence before `ProcLoweringIr` consumes TB02. Byte offsets, explicit cleanup-stack execution, and descriptor-driven whole-copy/field execution remain open. |
| TB03 Error routing Resume Next | `vm-ready` | package error state-machine descriptor; enabled/active handler state; fault PC/source; resume target PC; failing helper/error descriptor; `Err` state/reset evidence; caller/callee call-out behavior; cleanup/deopt snapshot fields | `implemented-runtime-only`; `metadata-missing`; `test-shortcoming` | VM error behavior exists and the tracer VM seed runs, but package-level error maps, helper failure descriptors, resume target evidence, and `Err` snapshot fields are missing. The JIT gate now requires package-owned error-frame evidence, including the quirky VBA control-flow/state-machine details, before `ProcLoweringIr` consumes TB03. |
| TB04 BSTR lifetime | `vm-ready` | declared string slot descriptors; BSTR carrier/layout facts; concat/Len helper descriptors; branch/failure/deopt cleanup maps; lifetime counters | `metadata-missing`; `test-shortcoming` | VM string behavior runs, and the lifecycle seed table now names variable/fixed string and helper-temp cleanup obligations. VM lifecycle evidence now records selected non-UDT `String` slot cleanup rows plus variable and fixed-length BSTR cleanup obligations for UDT fields, including success/branch/error/helper/deopt obligation tokens. The JIT gate now requires concat/Len helper descriptors, helper failure cleanup, and lifetime counters before `ProcLoweringIr` consumes TB04. |
| TB05 SAFEARRAY For Each and bounds | `vm-ready-bounds-followup` | array shape/bounds descriptors; Option Base provenance; element carrier/lifetime maps; bounds-error evidence; SAFEARRAY ownership evidence | `metadata-missing`; `VM-limitation`; `test-shortcoming` | VM positive subset runs for store/index/For Each and now records package array descriptors for fixed/static and dynamic local arrays plus runtime SAFEARRAY bounds for the dynamic `ReDim` case. Package execution also proves the selected rank-1 fixed/static `LBound`/`UBound` descriptor path, with raw bytecode retaining the old fixed-array base-slot runtime error baseline. VM evidence now reports selected fixed/static element, dynamic SAFEARRAY, and local SAFEARRAY slot lifecycle ownership tokens. The JIT gate now requires this array descriptor/lifecycle evidence before `ProcLoweringIr` consumes TB05. Runtime bounds-error evidence, VM-runnable multi-rank coverage, and COM/native SAFEARRAY projection remain required before closure. |
| TB06 Late-bound COM Resume Next | `vm-ready-hosted` | COM object descriptor; selector/default-member/named-arg descriptors; HRESULT/EXCEPINFO/ArgErr projection; `ObjectRef` identity; host capability requirement | `interop-limitation`; `metadata-missing`; `test-shortcoming` | Hosted VM seed runs against controlled COM. VM package evidence now records `CreateObject`/dispatch instruction expectations, runtime selector source, early-bound false, and runtime-owned HRESULT/EXCEPINFO/ArgErr classification; the lifecycle seed table names COM dispatch temporary cleanup obligations; and the object/member seed table names late-bound dispatch and named/default selector obligations. The JIT gate now requires package-owned late-bound boundary descriptors, full HRESULT/EXCEPINFO/ArgErr observations, default-member facts, object identity observations, and boundary cleanup evidence before `ProcLoweringIr` consumes TB06. |
| TB07 Early-bound COM typelib | `vm-ready-hosted` | typelib/reference descriptor; imported class/interface/member identity; dispatch-vtable strategy; argument/return projection; `ObjectRef` identity evidence | `interop-limitation`; `metadata-missing`; `test-shortcoming` | Hosted project seed runs with the OxVba typelib reference. VM package evidence now records early-bound dispatch instruction expectations, and the object/member seed table names early-bound dispatch plus vtable strategy obligations. The JIT gate now requires imported COM class/interface/member descriptors, typelib descriptor digests, dispatch-vtable strategy evidence, object identity observations, and argument/return projection evidence before `ProcLoweringIr` consumes TB07. |
| TB08 Native Declare shared ABI | `vm-ready-native-hosted` | native ABI descriptor digest; scalar/BSTR/Variant/SAFEARRAY parameter projection; ByRef writeback policy; cleanup buffers; host dynamic-link policy | `interop-limitation`; `metadata-missing`; `test-shortcoming` | Current VM/native seed covers the implemented host-backed subset. VM package evidence now records current `ExternalCallDescriptor` and native invoke facts for library/alias/name, return/parameter tokens, ByRef flags, argument slots, and writeback slots, and the lifecycle seed table names native writeback buffer cleanup/commit obligations. The JIT gate now requires native ABI descriptor digests, writeback evidence, cleanup/buffer ownership evidence, and native error policy before `ProcLoweringIr` consumes TB08; general Automation `Variant` and `SAFEARRAY` declared-parameter ABI support remains a real interop limitation. |
| TB09 Exported callable projection | `vm-ready-export-followup` | inbound ABI projection descriptor; return projection; ByRef writeback policy; cleanup/error return policy; unsupported-shape diagnostics | `interop-limitation`; `metadata-missing`; `test-shortcoming` | Internal callable seed runs, but external inbound/outbound export projection is not a first-class VM/package descriptor yet. The JIT gate now requires inbound ABI projection descriptors, return projection, ByRef writeback policy, cleanup/error return policy, and unsupported-shape diagnostics before `ProcLoweringIr` consumes TB09. |

## VM Rework Readiness Slices

The first VM rework should move from evidence and metadata toward behavior
only after the descriptor surfaces are visible and fixture-backed. These slices
are intentionally ordered so a broad storage rewrite is not the first step.

| Slice | Goal | Descriptor families | VM change type | Stop condition |
|---|---|---|---|---|
| VMR-01 | Package identity and procedure metadata | package digest, descriptor family ids, type registry ids, procedure id, bytecode digest, entry PC, slot counts, project context digest input | metadata/evidence only | VM run evidence records package, bytecode, procedure, signature, slot, project context, and reached descriptor identity/digest rows without changing behavior. |
| VMR-02 | Slot descriptor surface | `SlotTypeDescriptor`, declared type ids, roles, initial states, carrier hints | metadata loading plus snapshots | VM can load and expose descriptors for parameters, locals, return slots, compiler-generated fixed-array element slots, temporaries, UDT base slots, and generic `Object` slots while still executing existing slots, and VM evidence reports descriptor digests and rows. Package fixtures assert primitive scalar, `String`/`BStr`, declared `Variant`, current UDT field-alias/base-slot descriptor tokens, generic object slot descriptor tokens, and value snapshots. Richer class/interface/COM object shape descriptors remain next steps. |
| VMR-03 | Signature descriptor surface | `ProcedureSignatureDescriptor`, `ParameterDescriptor`, return descriptor | metadata/evidence first | Compiler package and VM package setup expose full call-relevant signature facts plus VM evidence comparing current seed `CallProc` lowering against signature metadata for ByVal, ByRef, Optional default, ParamArray, property value, and return copyout behavior. Descriptor-driven call execution remains deferred to VMR-04/VMR-06. |
| VMR-04 | Expression and call descriptor seeds | `CallSiteDescriptor`, `ArgumentBindingDescriptor`, expression category descriptors | metadata/evidence first | Call-site descriptor rows and VM evidence now cover top-level project call lowering for ByRef alias/writeback, ByRef expression temp, ByVal copy including the selected package-backed declared-`Double` call-entry coercion, Optional default, Optional `Variant` missing-policy metadata, empty/non-empty ParamArray, named fixed arguments with positional ParamArray packs, property value ByVal classification, return copyout, and compile-diagnostic evidence for the current named/arity/ParamArray rejection set. Optional-missing, property/default-member, COM/native, export, and broader call-entry coercion gaps are classified in the VMR-04 ledger and routed to later VMR-05/VMR-06/semantic-table/JIT-readiness work before any behavior-changing consumption. |
| VMR-05 | Array, UDT, object descriptor seeds | `ArrayShapeDescriptor`, `UdtTypeDescriptor`, `ObjectTypeDescriptor`, object/class/interface route descriptors, COM/native interop descriptor evidence | metadata/evidence first | Array shape evidence now captures fixed/static and dynamic local array descriptors, allocation/erase/preserve policy tokens, element lifecycle tokens, dynamic runtime SAFEARRAY bounds, selected array/SAFEARRAY lifecycle ownership rows, and the package-backed rank-1 fixed/static `LBound`/`UBound` selected behavior while retaining the raw-bytecode error baseline. UDT evidence now captures nominal type descriptors, nested/fixed-member facts, field aliases, recursive init/layout-index tokens, and first cleanup ownership flags. Object evidence now captures generic `Object` slot identity plus source-project class/interface route identities and imported COM `WithEvents` route identities for current VM-capable route tables. COM/native interop evidence now captures hosted TB06 through TB08 instruction/descriptor expectations without changing boundary behavior. Remaining slices must add broader bounds-error evidence, byte-offset/ABI UDT layout, `As New`/default-instance/imported-COM object descriptors, package-owned COM/native result/cleanup descriptors, and explicit gap classifications before behavior-driving consumption. |
| VMR-06 | Behavior-affecting metadata consumption | selected call, array, coercion, and cleanup descriptors | targeted VM behavior/evidence changes | The selected `VMR06-CALL-BYVAL-COERCE-001` slice now runs in package execution: descriptor-driven call-entry Let coercion for a direct `Long` argument into a declared `Double ByVal` parameter, with call-site evidence recording the selected coercion/helper row ids. The selected `VMR06-ARRAY-STATIC-BOUNDS-001` slice now runs in package execution: descriptor-driven rank-1 fixed/static `LBound`/`UBound` from `ArrayShapeDescriptor`. The selected `VMR06-UDT-OWNING-FIELD-CLEANUP-001` slice now consumes UDT cleanup descriptors into VM lifecycle evidence for owning BSTR fields without rewriting runtime carrier drops. All fixtures keep raw bytecode as the old baseline and package execution as the semantic-package path. Each later behavior change needs its own fixture and completion-map gap classification. |

## Outstanding Ambiguities Before VM Rework

The first VM batch may proceed because the P0 ownership questions below are
either decided or explicitly tracked. These decisions permit metadata/evidence
work only; behavior-affecting VM consumption still waits for the later VMR-06
gate and fixture evidence.

| Ambiguity | Disposition | Rule for the next VM batch | Residual owner / follow-up |
|---|---|---|---|
| Descriptor id ownership | Decided / seed implemented | The executable semantic package owns canonical descriptor ids and descriptor digests. Compiler/resolver/emit code may contribute source facts, and `OxBundle` may serialize them, but VM and JIT consumers do not allocate semantic ids. The current seed is `crates/oxvba-compiler/src/descriptor_identity.rs`, with VM evidence consuming those helpers. | Later beads extend the package registry to new descriptor families; VM/JIT consumers must continue to reject unknown package facts rather than inventing ids. |
| Carrier hint authority | Decided | First-pass carrier hints are observational evidence derived from declared type metadata. They must not drive slot storage, helper selection, typed lowering, or compatibility claims until a behavior-affecting VMR-06 bead promotes a specific descriptor use with fixture evidence. | VMR-02 captures declared type and carrier hint evidence; VMR-06 owns any future execution use. |
| Temporary slot modeling | Decided | Compiler-generated slots must receive `SlotTypeDescriptor` rows with role `Temporary` or `CompilerGenerated`, stable slot index, synthetic name when no source name exists, declared type if known, initial state, carrier hint, and cleanup obligation. User-visible slots remain bounded by current `user_slot_count` snapshot rules. | VMR-02 descriptor population beads define the first synthetic naming and cleanup fields. |
| In-memory package identity | Decided / seed implemented | A VM run that does not load a persisted `OxBundle` still records an in-memory package identity: package origin, bytecode digest, procedure identity, entry PC, slot counts, and canonical descriptor identity/digest rows. The digest is evidence/cache correlation, not a public persistence format. | Later persisted-package work may serialize more descriptor registries, but direct VM and host paths already expose the VMR-01 identity evidence. |
| Partial resolver output | Tracked | Partial or unknown semantic facts must be represented as explicit unknown/unsupported descriptor states with deterministic diagnostics. The VM may continue current behavior for metadata-only evidence, but JIT/native entry gates must treat unknown descriptor facts as unsupported rather than rediscovering them. | VMR-04 and VMR-05 split call-site, object/member, array, UDT, and COM descriptor unknowns as they become executable fixtures. |
| Compatibility versus current behavior | Decided | Current VM behavior is executable truth for regression and differential evidence, but it is not automatically VBA compatibility truth. Any behavior-affecting metadata consumption must cite a spec/oracle/project decision when changing behavior; otherwise the package evidence records current behavior with the appropriate gap label. | Semantic table beads and future behavior beads must carry the authority row or oracle gate before closing parity language. |

No P0 ambiguity remains for the metadata/evidence-only first VM batch. The
tracked P1 details above are blockers only for the specific descriptor family
or behavior-affecting bead that consumes them.

## First Rework Boundary

The first VM rework batch is metadata/evidence only. It may add package views,
descriptor structs, descriptor digests, VM evidence fields, and fixtures that
show current behavior beside the target descriptors. It must not change runtime
slot storage, helper semantics, COM/native behavior, error routing, or
`oxvba-jit` status.

## Closure Rule

A row is complete only when:

- the descriptor exists in package metadata or a versioned descriptor referenced
  by the package;
- the VM consumes it or evidence proves it is not execution-affecting for the
  scoped row;
- VM-runnable fixtures cover the behavior or the row is explicitly oracle-only;
- JIT/native planning can reference the descriptor without rediscovering the
  semantic fact from source, bytecode pattern matching, or ambient runtime
  state.
