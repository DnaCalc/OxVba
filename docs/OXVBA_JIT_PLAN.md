# OxVba — JIT Plan

**Status:** Plan (design-verified 2026-07-02; two design passes + a fresh-eyes convention sweep folded in). M4-0 baseline implementation is complete; evidence is recorded in `docs/evidence/perf/JIT_M4_BASELINE_20260703.md`. M4-1 IR-prep implementation is complete; evidence is recorded in `docs/evidence/jit/JIT_M4_IR_PREP_20260703.md`. M4-2 runtime ABI implementation is complete; evidence is recorded in `docs/evidence/jit/JIT_M4_RT_ABI_20260703.md`.
**Scope:** M4 — the Cranelift JIT backend: architecture, design, and implementation program, from IR-prep through full corpus parity, typed fast paths, JIT-generated COM vtables, and AOT PE export.
**Companion documents:** `OXIR_VM3_ERROR_MODEL.md` (the normative error semantics this plan compiles), `spec/JIT_V2_RUN_PROTOCOL_V1.md` (the shared vm3/JIT activation and entry sequencing contract), `AOT_CRANELIFT_PE_EXPORT_DESIGN_2026-06-20.md` (the AOT packaging substrate §11 builds on), `VM3_COMPLETION_AND_VM2_RETIREMENT_PLAN.md` (the predecessor plan whose workset idiom this document follows).

---

## Readiness assessment

**Verdict: READY.** The prerequisites that are missing are well-bounded IR/infrastructure work that forms this plan's first worksets (M4-0..M4-2), not blockers.

What is in place:

- **vm3 is a complete, oracle-validated executable specification.** M3 is complete; the `bd-4ktq` spec-gap epic is closed (all Critical/High SilentWrong items fixed; the ~70 remaining Tier-3/4/5 inventory items are open but non-blocking and covered by the lockstep rule in §9). Since the vm2-retirement worksets executed, vm3 is the **sole product runtime** — comhost cdylib, CLI, and host sessions all run `OxImage` on vm3; `oxvba-vm2` is deleted.
- **OxIR was designed for native lowering.** It is a fully typed basic-block CFG: 65 `OxInst` variants and 12 terminators (`crates/oxvba-oxir/src/inst.rs`), an 18-variant type lattice with documented machine-representation invariants (`ty.rs`, `box_unbox_is_identity`), explicit per-statement fault edges (`OxBlock.fault_target` + `FaultDispatch { resume, resume_next }`), `is_fallible()` contracts on every op, typed COM descriptors interned inline in the program, and an existing verifier (`verify.rs`). The instruction documentation itself says fallible ops "lower cleanly to a branch on a returned status" — the IR anticipated exactly the compilation model in §3.
- **The runtime is already a library.** ~73K lines of runtime services are cleanly separable and JIT-reusable: `oxvba-eval` (arith/compare/coerce as free functions), `oxvba-lib::invoke` (every builtin), `oxvba-runtime` (Variant/BSTR/SafeArray/VbaRecord/ObjectRef + the termination queue), `oxvba-com` and `oxvba-hal` behind `&dyn HostServices`. Only ~6K lines of `oxvba-vm3` are interpreter-specific.
- **The regression net exists.** ~300-program conformance corpus; `crates/oxvba-differential/vm3_golden.snap` (insta; re-bless via `OXVBA_BLESS_GOLDEN=1`); live-Excel oracle captures under `docs/evidence/conformance/`; live com_matrix suites in `oxvba-host`; six differential axes (result values, Err state, side-effect journal, Init/Terminate timing, COM transport counts, COM typed-arg fidelity).
- **The error model is normative and largely IR-encoded.** R1–R14 in `OXIR_VM3_ERROR_MODEL.md`; fault edges, landing pads, `ClearErr`-on-Exit, and GoSub terminators are already in the IR, so most of the model is shared between engines by construction.

What was missing before M4-0..M4-2 implementation:

1. **Escape analysis was absent.** M4-1 now records escape facts for the canonical OxIR image; broader register promotion still belongs to later fast-path work.
2. **Box/Unbox insertion was absent.** M4-1 added canonical Assign normalization with explicit `Box`/`Unbox`/`Coerce`; the M4-4 in-progress slice currently lowers scalar/Variant `Box` shapes plus direct-OxIR checked exact-tag `Unbox` coverage for current scalar/`Variant`/`ProcRef` slot targets.
3. **No performance baseline existed.** M4-0 added the baseline evidence and measurement gates.
4. **No Cranelift integration existed.** M4-3 established the skeleton and M4-4 is expanding the compiled subset without VM fallback.

---

## §0 Fixed design principles

1. **vm3 is the executable spec.** The JIT must match vm3 on all six differential axes over the full corpus. Every design decision below is scored first on "can this diverge from vm3?", second on performance.
2. **Shared runtime, not shared execution model.** Runtime services are reused as libraries behind `extern "C"` shims. vm3's frame/slot machine is *not* reused — that is the documented cause of JIT-v1's deletion.
3. **The typed IR is the point.** Wherever the IR carries static types — operands, calling signatures, array element types, COM member descriptors — the generated code and its runtime boundaries are typed. Variant-shaped machinery exists only where the language is genuinely dynamic. (Three early-draft decisions failed this test and were reversed; the decision histories are recorded in §5.1, §5.5, and §6 so the mistake is not re-made.)
4. **No native exceptions.** Cranelift has no usable Win64 unwind/EH story (no SEH registration for JIT frames, no landing pads). All VBA faults are status-code returns. This matches OxIR's explicit fault edges by design — it is the model, not a stopgap.
5. **Phased inside one architecture.** Phase A (helper-heavy, correct) commits to the same typed value model and typed calling convention that Phase B (inline fast paths) needs, so B is additive per-op work, never a representation migration.
6. **No silent fallback, ever.** An unimplemented construct is a whole-program clean `Unsupported` decline, tracked by a monotone scope ratchet. Mixed vm3/JIT execution exists only as a loudly-logged `jit-debug` bisection facility, unreachable from product configuration.

---

## §1 Crate architecture

### 1.1 The dependency problem

JIT-compiled code cannot contain VBA semantics inline (Phase A is helper-heavy by design); it calls runtime services through a C-ABI shim surface. Those shims need `oxvba-eval`, `oxvba-lib`, `oxvba-runtime`, `oxvba-com`, and `oxvba-hal`. Meanwhile `oxvba-vm3` must never grow a cranelift dependency, and nothing the shims depend on may depend back on them.

| Option | Shape | Verdict |
|---|---|---|
| A. Extend `oxvba-runtime` with the shim surface | no new crate | **Cycle.** `oxvba-eval`/`oxvba-lib` depend on `oxvba-runtime`; the shims must call *into* eval/lib/com. Runtime is a leaf below them; hoisting shims into it inverts the DAG. Ruled out structurally. |
| B. Shims inside `oxvba-jit` | one JIT crate | No cycle, but the shim surface is pure runtime code with zero cranelift content: burying it under cranelift deps taxes every shim unit test with cranelift compile time, and kills the AOT seam — the stub-loader design needs the helper surface as an ordinary linkable rlib/staticlib. Also the shared-state extraction (§4) must live somewhere vm3 can depend on, and vm3 must not depend on `oxvba-jit`. |
| **C. New crate `oxvba-rt-abi`** | runtime ABI + shared execution state | **Recommended.** Clean DAG, shims testable without cranelift, AOT-ready (the same crate compiles as the AOT support library). Forced anyway the moment vm3 and the JIT share any runtime state object: that type must live below both, and `oxvba-runtime` cannot host it (it needs `LibContext` from `oxvba-lib` and `&dyn HostServices` from `oxvba-hal`). |

### 1.2 Crate diagram (target state)

```
                 oxvba-syntax → oxvba-symbol → oxvba-bind → oxvba-oxir
                                                              │  (OxProgram / OxImage,
                                                              │   + M4 prep passes & verifier exts)
        oxvba-hal   oxvba-runtime   oxvba-eval   oxvba-lib   oxvba-com
           ▲             ▲              ▲            ▲           ▲
           └─────────────┴──────┬───────┴────────────┴───────────┘
                                │
                        ┌───────┴────────┐
                        │  oxvba-rt-abi  │  NEW: ExecState, ErrEngine (R1–R14 cells + routing),
                        │  (no cranelift)│  ProcInvoker seam, extern "C" shim surface,
                        └───┬────────┬───┘  test-build live-handle counters
                            │        │
              ┌─────────────┘        └─────────────┐
        ┌─────┴─────┐                        ┌─────┴─────┐
        │ oxvba-vm3 │                        │ oxvba-jit │ ← cranelift-{codegen,frontend,
        │ (interp)  │                        │           │    module,jit,native}
        └─────┬─────┘                        └─────┬─────┘
              └───────────────┬────────────────────┘
                        ┌─────┴──────┐
                        │ oxvba-host │  ExecBackend selection → CLI / comhost / sessions
                        └─────┬──────┘
                    ┌─────────┴──────────┐
                    │ oxvba-differential │  Executor::{Vm3, Jit}, golden, balance axis,
                    └────────────────────┘  fuzz, criterion benches
```

DAG invariants (enforced by `cargo tree` checks in M4-2's verify gate): `oxvba-vm3` has no path to cranelift; `oxvba-jit` has no path to `oxvba-vm3`; cranelift appears in exactly one crate.

`oxvba-rt-abi` contains:

- **`ExecState`** — the VM-agnostic observable runtime state (§4).
- **`ErrEngine`** — the R1–R14 state cells + the single routing decision, extracted from vm3 (§3.1).
- **`ProcInvoker`** — `invoke(&mut ExecState, prog, func, args) -> Result<Variant, Fault>`. vm3 installs an adapter over `run_proc_with_values`; the JIT installs a lookup into its compiled-function table. This is the re-entrancy spine (Terminate drain, event delivery, RaiseEvent fan-out run user code through it) and, deliberately, the seam a future M6 tiering engine plugs into.
- The **`extern "C"` shim surface** (§5.5–§5.6), unit-testable from plain Rust.

### 1.3 Engine selection

No backward-compat constraints on internal APIs, so do it cleanly:

- `pub enum ExecBackend { Vm3, Jit }` in `oxvba-host`; `HostConfig { backend: ExecBackend }` **replaces** `enable_jit` (delete it). CLI: `--backend vm3|jit` canonical, `--jit` kept as alias.
- comhost cdylib: `OXVBA_BACKEND=jit` env override read at `Engine::new` (no API change for COM clients).
- `Executor::Jit` in `oxvba-differential`; every existing runner works unchanged once `HostConfig.backend` exists.
- Honest decline: a JIT compile hitting an unimplemented construct surfaces as the standard `Unsupported("jit: <op> not lowered")` outcome — the exact vm3-M2 bring-up pattern the harness already skip-tracks.

---

## §2 Compilation unit & module strategy

**Decision: full AOT compile of the whole OxImage at session load.**

| Alternative | Pros | Cons |
|---|---|---|
| **(a) Compile every `OxFunc` of every `OxProgram` at image load** | Deterministic (no warm-up divergence visible to the corpus); direct calls everywhere (all callees known at relocation time); one `finalize_definitions()`; matches vm3's "whole image present" model | Pays compile time for dead functions — irrelevant at VBA scale (Cranelift compiles on the order of MB/s of CLIF; see §10 compile budget) |
| (b) Lazy per-function tiering | faster start for huge projects | call patching/trampolines, non-deterministic compile timing interleaved with user side effects — a class of JIT-only bugs the oracle cannot see. **Pre-designed as a contained fallback** behind the function-table indirection if the compile budget breaks (§10), not built speculatively. |
| (c) One JITModule per function | isolation | pointless relocation complexity, no direct calls |

Flow (`JitEngine::compile(image: &OxImage, …) -> CompiledImage`):

1. `JITBuilder::with_isa(native_isa)`; every runtime shim registered by address via `JITBuilder::symbol("rt_…", fn as *const u8)` — explicit registration; Windows has no dlsym-style global namespace for Rust statics.
2. **Declare pass**: for each `(prog p, func f)` declare `Linkage::Local` function `ox$p{p}$f{f}$<sanitized name>` — uniqueness from the indices; the name suffix exists for `OXVBA_JIT_TRACE` diagnostics only.
3. **Define pass**: lower each `OxFunc` with `cranelift_frontend::FunctionBuilder` (per-function context cleared between functions).
4. One `module.finalize_definitions()` (flips W^X page permissions); harvest finalized code pointers into per-program **function tables** `Vec<*const u8>` on the `CompiledImage`. The tables back all indirect dispatch: `CallProcRef`, `CallByName`, class/property member dispatch, event routes, entry thunks — and they are the patchable seam M6 tier-up needs.
5. **Cross-program `CallExtern`**: the whole image compiles together, so `ImportId → (prog, func)` resolves at define time to a **direct call**. No PLT/GOT machinery; `is_pic = false`.
6. **Globals**: one Rust-owned `Box<[Variant]>` per program (16-byte slots), base pointers on the run context. Rust-side ownership means image teardown releases globals exactly like vm3 dropping its slot vectors. Compiled code addresses a global as `global_bases[p] + 16·g`.
7. **`global_initializer` / `entry`** are ordinary `OxFunc`s. A Rust **driver** replicates vm3's exact run protocol — per-program initializer order, entry invocation, Halt semantics, end-of-run drain — extracted as a documented shared contract in M4-2 so both engines follow one sequencing spec.
8. **`Backend` trait** over `cranelift-module` so that `JITModule` vs `ObjectModule` is one file's concern. This is the AOT seam (§11); code outside that file must not call `JITModule`-only APIs.

---

## §3 Error / fault model compilation

This section compiles `OXIR_VM3_ERROR_MODEL.md` (R1–R14). Key observation: most of the model already lives in the IR — explicit fault edges, per-statement landing pads ending in `FaultDispatch`, `ClearErr` on Exit paths (R10), `ErrorHandler::ClearActiveError` for `On Error GoTo -1` (R13), `GoSub`/`GoSubReturn` terminators (R12), `Raise { inherit }` (R11). Both engines consume the same blocks, so those rules are shared by construction. What remains engine-side is a small set of state cells and one routing decision.

### 3.1 ErrEngine: shared implementation, not replication

| Alternative | Analysis |
|---|---|
| Full extraction of vm3's dispatch loop into an exec-core crate | The 6,100-line vm3 entangles fault routing with frame management, `Loc` resolution, and instruction dispatch. Carving out the whole propagation loop is a multi-week refactor **of the oracle itself**, which would then need re-validation. Rejected. |
| Pure replication in the JIT, differential corpus as the guarantee | The routing decision has genuinely subtle cases (handler re-raise, `Resume` without active error → 20, Err inheritance fields, Erl copy timing) where silent drift between two hand-written copies is exactly the bug class that survives until a corpus program happens to hit it. Rejected. |
| **Extract the cells + decision function only — `ErrEngine` in `oxvba-rt-abi`** | The engine-side surface is small: cells (`error_mode`, `active_error`, `err: ErrState`, `erl_line`, `last_dll_error`) with per-activation save/restore, plus one function `route_fault(fault) -> FaultAction { Handle(pad) | ResumeNext | Propagate | Fatal }` encoding single-shot Goto demotion (R9) and the Resume-legality check. vm3 is re-pointed in the same workset; **the gate is a byte-identical vm3 golden snapshot** — the proof that extraction is behavior-preserving. **Recommended.** |

GoSub stacks stay engine-private: they are pure control state (per-frame LIFO of block references) with no semantic content to drift.

### 3.2 The dynamic-block-target problem

Three mechanisms jump to a **runtime-held** block: the handler in `error_mode = Goto(h)`; the `resume`/`resume_next` seeds captured in `active_error`; `GoSubReturn`'s popped return block. Native code cannot jump to a dynamic address without a table. All such targets are intra-function (the cells are per-activation, per the model doc) and statically enumerable:

- `H` = all `SetErrorHandler(GotoLabel(b))` targets in the function,
- `S` = all `{resume, resume_next}` seeds on the function's `FaultDispatch` pads,
- `G` = all `GoSub { ret }` blocks.

| Alternative | Analysis |
|---|---|
| `br_table` over **all** blocks of the function | trivially correct, but O(blocks) table per function and — worse — it *hides* invariant violations: a corrupt index jumps somewhere plausible. |
| **Compact table over `T = sort(H ∪ S ∪ G)`; runtime cells store the small index** | Table is tiny (typically < 10 entries). A bad index hits the `br_table` default → `trap` (compiler bug caught loudly). Storing function-relative indices makes the runtime state meaningless across frames — exactly the per-activation semantics R1 demands. **Recommended.** |

Each compiled function gets one **redispatch block**: a single `i32` block parameter → `br_table` over `T` → jump. `SetErrorHandler(GotoLabel(b))` stores `index_of(b)`; `FaultDispatch` seeds are passed as immediates.

### 3.3 Status-code return convention

Every shim and every compiled call returns `i32`:

```
ST_OK    = 0   // success
ST_FAULT = 1   // VBA fault pending; payload written to ExecState
ST_HALT  = 2   // End statement propagating (bypasses handlers, R14)
```

Every call site lowers to:

```
s = call <callee>(…)
brif s == 0        → ok
brif s == ST_HALT  → jump cleanup_epilogue(ST_HALT)
jump <this block's fault pad>          ; ST_FAULT — caller's pad = the call statement's pad,
                                        ; which is R2 (default propagation) for free
```

Fault details never travel in the return value: on `ST_FAULT` the shim writes the rich `Fault { number, description, source, help… }` into ExecState (preserving the M3 COM HRESULT→Err fidelity).

### 3.4 FaultDispatch lowering

```
; native pad block for FaultDispatch { resume: r, resume_next: n }
d = call rt_route_fault(state, idx(r), idx(n))
  ; ErrEngine: seats Err from the pending fault, then on error_mode:
  ;   None       → DISP_UNWIND
  ;   ResumeNext → active_error = Some{r,n,caught}; DISP_RESUME_NEXT
  ;   Goto(h)    → demote mode to None (R9 single-shot);
  ;                active_error = Some{r,n,caught=Goto(h)};
  ;                dispatch_target = idx(h); DISP_HANDLER
switch d:
  DISP_UNWIND      → jump cleanup_epilogue(ST_FAULT)   ; fault stays pending for the caller
  DISP_RESUME_NEXT → jump native(n)                    ; STATIC target — no table needed
  DISP_HANDLER     → t = load dispatch_target ; jump redispatch(t)
```

Note only the handler leg needs the table; `resume_next` is a static jump at each site.

### 3.5 Resume family and GoSub

`Resume` / `Resume Next` terminators (fallible — their block has a pad):

```
s = call rt_resume(state, KIND, &out_idx)
  ; no active_error → pending fault = err 20 "Resume without error"; ST_FAULT
  ; else: reset Err, clear the latch, RE-ARM the caught handler policy, out_idx = seed
brif s != 0 → this block's pad     ; err 20 routes like any fault
jump redispatch(out_idx)
```

`Resume <label>`: same helper for the check/reset/re-arm, then a **static** jump to the label block. `GoSub { target, ret }`: push `idx(ret)` onto the activation's gosub stack (unbounded LIFO, R12), static jump to `target`. `GoSubReturn`: pop (empty → err 3 via the pad) → `redispatch(out_idx)`.

### 3.6 Cross-frame unwinding

The callee runs its cleanup epilogue and activation-leave **before** returning `ST_FAULT`, so the caller's error cells are already restored when its pad dispatches — the native status return plays vm3's `propagate_fault` exactly, with the resume seeds static at each call statement's pad (so `Resume` in the caller re-runs the right statement). Driver-level `ST_FAULT` is the host terminate boundary (R1); `ST_HALT` truncates with no drain (R14).

---

## §4 Runtime state: ExecState and the JIT run context

**Principle:** every piece of vm3 state observable across a re-entrancy boundary (event delivery, Terminate drain, host session invoke) moves into a VM-agnostic `ExecState` in `oxvba-rt-abi`, and both engines operate on the same instance. Interpreter-implementation details stay engine-private. This is what makes re-entrancy work without a translation layer: a COM event arriving mid-run lands in `ExecState`, and whichever engine is active delivers it through the same subscription maps.

### 4.1 State-ownership table

| vm3 state today | Semantics | M4 home |
|---|---|---|
| `err: ErrState`, `erl_line`, `last_dll_error`; `error_mode`, `active_error` (+ per-frame saves) | Err object + handler policy, R1–R14 | `ExecState.err_engine` (`ErrEngine`, §3.1); save/restore via `enter_activation()/leave_activation()` in the frame shims |
| `pending_fault` | fault in flight to a pad | ExecState scratch cell (the flag is the return status) |
| pending-termination queue + `draining` guard | Class_Terminate fixpoint | queue stays global in `oxvba-runtime` (it already is); the `draining` guard moves to ExecState |
| `withevents`, `com_subscriptions` (+by-key), `pumping`, `withevents_iters`, `project_event_sink`, ordering counters | event fabric | ExecState (moved verbatim) |
| `for_each`, `param_array_aliases`, `as_new_slots` (Loc-keyed maps) | loop/alias/lazy-init bookkeeping | **engine-private.** These maps are interpreter bookkeeping for information the JIT has statically: For-Each state lives in a hidden per-lexical-loop-site stack slot (keyed at compile time by the iter place; per-native-frame ⇒ recursion-correct, matching vm3's frame-indexed Locs); AsNew guards are static per-place — the Nothing-ness of the slot *is* the state; ParamArray alias copy-out is caller-side static from `ArrayLiteral{aliases}`. No Loc-equivalent-key machinery is built; the differential corpus proves equivalence. vm3 keeps its maps unchanged. |
| `lib: LibContext` (Rnd seed) | builtin state | ExecState |
| `host: &dyn HostServices` | HAL | ExecState (all effectful shims reach it through the state pointer) |
| per-program globals, `class_descriptors`, predeclared singletons, `event_routes`, `next_instance_id` | loaded-image tables | ExecState.loaded[] |
| frames / temps / aliases / ip (interpreter); gosub stacks | activation records | engine-private |

`ExecState` is `!Send`; one per session/thread, exactly vm3's thread-local model.

### 4.2 The JIT run context

Every compiled function takes a hidden first parameter:

```rust
#[repr(C)] pub struct JitRun {
    pub exec: *mut ExecState,           // shared observable state (§4.1)
    pub fn_tables: …,                    // per-program FuncId → code ptr (typed + dynamic entries)
    pub image_meta: *const OxImageMeta,  // classes / COM descriptor arena / dispatch metadata
    pub activations: Vec<Activation>,    // shadow activation stack (parallel to the native stack)
    pub stack_limit: usize,              // native-stack guard watermark (§6)
    pub trace: TraceConfig,
}
#[repr(C)] pub struct Activation {
    pub prog: u32, pub func: u32,
    pub saved_err: ErrEngineSaved,       // R1/R2 save/restore
    pub erl_line: i32,
    pub gosub: SmallVec<[u32; 4]>,       // dispatch-table indices (§3.2)
}
```

`rt_frame_enter(ctx, prog, func) -> *mut Activation` pushes (saving the caller's error cells, zeroing the callee's per R1) and returns the record pointer, which the prologue caches so `SetLineNumber`/`ErlGet` are single inline stores/loads. `rt_frame_leave(ctx)` pops and restores — called from the unified cleanup epilogue on **all** exits, which is vm3's `propagate_fault` restore (R2) on the fault path.

---

## §5 Value representation & lowering

### 5.1 The typed model (central decision)

**Rejected: model (a), "every place is a 16-byte Variant slot".** Not merely slow — architecturally self-defeating:

- It reproduces vm2's type erasure, the exact thing OxIR exists to undo. Every op re-inspects tags at runtime; the JIT becomes a call-threaded interpreter with the dispatch loop unrolled (measured ceiling ~1.5–2× over vm3).
- It leaves no room for Phase B: inlining a Long add under (a) still needs tag checks and reboxing on both sides, so "add fast paths later" means changing the storage model — the rewrite, deferred.
- It makes `Box`/`Unbox` meaningless and wastes the IR's bit-identical payload invariant.
- Historically, JIT v1 died of it (recorded in `oxvba-jit/src/lib.rs`).

**Recommended: model (b), typed representation.**

**OxTy → CLIF type map** (Win64; matches the `ty.rs` machine-representation table exactly):

| OxTy | CLIF register type | Slot | Notes |
|---|---|---|---|
| Bool | `i8` (0 / −1) | 1 B | the ONE non-identity box: box = `sextend` to i16 (−1/0) in Variant, unbox = `ireduce`. **Never identity-fuse** (verifier rule). |
| Byte | `i8` | 1 B | unsigned ops |
| Integer | `i16` | 2 B | |
| Long | `i32` | 4 B | |
| LongLong / LongPtr | `i64` | 8 B | 64-bit target fixed for M4 |
| Single | `f32` | 4 B | |
| Double / Date | `f64` | 8 B | Date = OLE serial |
| Currency | `i64` | 8 B | scaled ×10⁴; mul/div helper-only (i128 kernel, §8) |
| Decimal | — | 16 B memory-only | by-pointer to helpers; no i128 in CLIF |
| Str / FixedStr(n) | `i64` (BSTR) | 8 B handle | owning |
| Object(_) | `i64` (IUnknown ptr) | 8 B handle | owning |
| Record(_) | `i64` (payload ptr) | 8 B handle | owning, value semantics |
| Array(_, _) | `i64` (SAFEARRAY ptr) | 8 B handle | owning |
| ProcRef | `i64` packed (prog:u32, func:u32) | 8 B | non-owning |
| Variant | — | 16 B slot, 8-aligned | always memory, always addressable |

**Two-tier place assignment:**

| Tier | Which places | Representation |
|---|---|---|
| **R (register)** | non-escaped, non-owning scalar locals + scalar temps | `cranelift_frontend::Variable` — full SSA promotion, phis for free |
| **M (memory)** | escaped locals; owning types; Variant; Decimal; ByRef params (incoming pointer); globals | explicit `StackSlot` / global cell |

**Bare-handle rule** *(decision history: an earlier draft proposed a "tagged-slot rule" — owning locals stored as full 16-byte Variants with statically-known tags — for zero-copy Variant-shim boundaries and one uniform cleanup helper. Rejected on the same grounds as the Variant calling convention in §6: it is the interpreter's slot shape leaking in, and that draft already admitted bare handles were the end state via a planned Phase-B "promotion" — i.e. a guaranteed migration. Do it once, correctly.)*

Owning-typed places (Str/Object/Record/Array) are stored as **bare 8-byte handles** in their machine representation:

- Cleanup is a **static type-specific release list** per function: `rt_bstr_release` / `rt_object_release` / `rt_array_release` / `rt_record_release`, all null-safe; the prologue zeroes handles. Variant-typed places keep 16-byte slots and the uniform variant release.
- **ByRef alignment bonus:** a ByRef String slot is exactly a `BSTR*`, ByRef Object exactly `IUnknown**` — the same shapes Declare and COM native conventions use. Typed ByRef, NativeThunk vtable slots (§6), and Declare writebacks all point at the slot directly, with no payload-offset arithmetic.
- Boxing into a Variant happens only at genuinely dynamic boundaries (late-bound COM args, `CallByName`, Variant-typed places) — a static, mechanical two-store site.

### 5.2 Ownership discipline (O1–O8) — the hard correctness area

vm3 gets lifetime correctness from Rust `Variant: Drop`. Native code must make every drop explicit. Invariants:

- **O1 — Slot ownership.** Every M-tier slot owns its contents at every instruction boundary. The prologue zeroes (null handles; `VT_EMPTY` for Variant slots), so the type-specific null-safe releases are always safe and idempotent-after-zero.
- **O2 — Helper out-params are moves.** Every helper writes its `dst` as a fresh owned value and never reads or frees prior contents. Therefore `dst` must be *dead* at call time — lowering always targets a per-op scratch, never a live place.
- **O3 — Store protocol.** "Place P ← result": (1) helper writes owned result into scratch S; (2) release P's old value (statically elided for non-owning types); (3) copy S→P; (4) S is dead (ownership moved). Compute-then-release makes `s = s & "x"` and `Set o = o.Child` safe; S≠P makes aliasing a non-issue. The release in (2) may **enqueue** a Terminate — it never runs user code inline (O6).
- **O4 — Operand reads are borrows.** Helper operands pass by pointer with no AddRef/clone; helpers that retain, clone internally (they already do — vm3 passes `&Variant` identically). Explicit `OxInst::AddRef` is lowered to a helper call, never elided.
- **O5 — Temps.** Single-assignment ⇒ each owning temp slot is written at most once between clears. `StmtBoundary { clear_temps_from }` lowers to a static release+zero sequence over owning temp slots ≥ the floor. Releasing already-empty slots is a free no-op. **This must replicate vm3's temp-clear timing exactly** — temp releases are Terminate-observable.
- **O6 — Terminate timing.** Releases only **enqueue** pending terminations (the shared `oxvba-runtime` queue). User `Class_Terminate` code runs only at `DrainTerminations` and at frame-pop points, via `rt_maybe_drain` — vm3's `maybe_drain` core extracted into rt-abi in M4-2, one implementation, re-entrancy guard in ExecState.
- **O7 — One cleanup epilogue per function.** Release all owning M-tier locals (excluding ByRef indirections — caller-owned) and owning temp slots, `rt_frame_leave`, `return status`. All exits — normal Return, fault unwind, Halt — jump here with `status` as a block parameter. This is what replaces Rust `Drop` on the unwind path.
- **O8 — ByVal owning args.** Caller materializes an owned copy in a scratch; callee clones into its own local in the prologue; the caller's scratch dies at its next temp clear. Two clones is vm3-equivalent; a move flag is a later optimization if profiling justifies it.

### 5.3 Escape analysis (IR-prep, precise rules)

A local L of function F is **escaped** iff any of:

1. `OxArg::ByRef(Local(L))` in any `CallProc` / `CallProcRef` / `CallExtern` / `RaiseEvent`;
2. `OxCallArg::ByRef(Local(L))` (writeback target) in any `CallNative` / `CallByName` / `ComCallEarly` / `ComCallLate`;
3. `Ptr { VarPtr | StrPtr | ObjPtr }` with L as source (address-taken);
4. Declare pointer-writeback target;
5. member of `ArrayLiteral { aliases }` (ParamArray element aliasing);
6. the `iter` of `ForEachInit`/`ForEachNext`;
7. ever the place of `AsNew`.

Globals are always M-tier and need no flag. Escaped **temps** are a computed per-function analysis set (temps have no IR metadata — see the temp type table in M4-1). The flag is only a tiering input for owning types (M-tier anyway); it is load-bearing for **scalars**, which otherwise become SSA Variables and lose their address. The pass lives in `oxvba-oxir` (vm3 and the verifier see the same flags) and is behavior-neutral for vm3 — gate: golden unchanged.

### 5.4 Box/Unbox insertion (scoped hybrid)

| | (i) IR-level insertion in elaboration | (ii) JIT-side materialize-on-demand |
|---|---|---|
| Oracle coverage | vm3 executes the converted IR first → the pass is validated against the golden **before the JIT exists** | conversions live only in the JIT; a bug there is only caught by the differential, conflated with every other JIT bug |
| Verifier | can enforce "Assign is representation-preserving" — a strong well-typedness invariant | Assign stays polymorphic, untypeable |
| IR churn | inflates instruction count; touches elaboration + snapshot review | zero |
| Phase B | Box/Unbox pairs are what fusion cancels (the identity invariant exists for this) | fast paths must re-derive boundary knowledge |

**Recommendation: scoped hybrid.** Do (i) for **Assign normalization only**: after the pass, every `Assign` is representation-preserving, with `Box`/`Unbox`/`Coerce` explicit — validated by an unchanged vm3 golden before any JIT consumes it; the verifier gains the "same-repr Assign" rule. Helper-argument boxing stays JIT-side (ii): with typed helper families (§5.5) it occurs only at genuinely dynamic boundaries, emitted mechanically by one `emit_operand_as_variant()`. Full insertion is revisited as a Phase-B fusion prerequisite. Unbox of helper results is `checked` unless the helper contract guarantees the tag.

### 5.5 Instruction lowering classification (Phase A)

**Typed-helper principle** *(decision history: an earlier draft routed ALL ops through Variant-pointer shims — "box operands into scratch Variants, call `oxjit_arith(*const Variant, …)`", letting the shim re-dispatch on tags. That is the same type-erasure-at-every-boundary disease as the rejected calling convention. Reversed.)*

Shims are **typed where the IR is typed**; Variant-shaped shims exist only where the IR is genuinely dynamic. The IR already separates the regimes: `NumericMode::Checked(ty)` ops have statically-typed operands and results — `oxvba-eval` computes them through exact typed kernels (`checked_binop` in i64, `currency_*` in i128) behind its Variant facade — while `NumericMode::Widening` is the Variant regime.

- **Checked lanes → typed shims**: `rt_add_i32(l: i32, r: i32, dst: *mut i32) -> i32` etc. — no boxing, no tag dispatch. Likewise Compare on statically same-typed scalars, Truthy on Bool, typed array element access.
- **Widening lanes / Variant operands → Variant shims.**
- **One semantic kernel, two facades** (M4-2): refactor `oxvba-eval` to expose the typed kernels as public typed entry points; the existing Variant-facade functions AND the new typed shims both delegate to them. No semantic is implemented twice — divergence between lanes is structurally impossible.
- Phase B then merely **inlines** typed-shim bodies (`iadd` + overflow branch, bounds-checked element address) — per-op local changes gated by the differential, never a representation change.

Classification:

**Inline CLIF (no call):** `Assign` (post-normalization moves; M↔M copies + O3 release), `Box`/`Unbox{unchecked}` (tag+payload stores/loads; Bool sext/ireduce), `LoadProcRef` (iconst), `SetLineNumber` (store to the cached activation), `StmtBoundary` (static release sequence + optional trace hook), terminators `Jump`/`Branch`/`Unreachable` (→ trap: compiler bug).

**Helper families (everything else):**

| Family | OxInsts | Backing |
|---|---|---|
| arith | Arith/Div/Pow/Neg/Concat/Compare/Logical/Not/Truthy/Coerce/Unbox{checked}/CompareObjectIs/TypeOfIs/VariantChanged/ValidateAssignment | `oxvba-eval` kernels (typed + Variant facades) |
| calls | CallNative → `oxvba_lib::invoke`; CallByName → member resolver | lib / image tables |
| COM | ComCallEarly / ComCallLate | HAL; per-site immutable descriptors in a compile-time arena, referenced by absolute-address iconst |
| objects | NewObject/NewExtern/Predeclared*/FieldGet/FieldSet/FieldArray*/AddRef/Release/DrainTerminations | `oxvba-runtime` object model |
| records | NewRecord/RecordGet/Set/LSet/ArrayGet/ArraySet | VbaRecord |
| arrays | ArrayLiteral/Append/Redim/Get/Set/Erase/Bound/ForEachInit/Next | SafeArray |
| events | WithEvents×5 / RaiseEvent | event fabric in ExecState |
| error | SetErrorHandler (helper — R5's Err auto-reset), ClearErr, ErrFieldGet/Set | ErrEngine |
| ptr | Ptr{VarPtr/StrPtr/ObjPtr} | address computation over M-tier slots |

AsNew-marked places: every operand read lowers through `rt_read_asnew` (lazy `Class_Initialize` ⇒ fallible — which is why `Assign` is fallible in the IR). **Nothing is hybrid in Phase A** — one mechanism per op keeps the first differential run debuggable.

### 5.6 Shim ABI and representative lowerings

All shims: `extern "C"`, registered by address, bodies wrapped in `catch_unwind` — a Rust panic must never unwind through Cranelift frames (UB on Win64: no unwind info is registered for JIT code). Panic ⇒ InternalError fault, or abort under `OXVBA_JIT_STRICT`. Fault payloads go to ExecState, never the return value. Representative signatures:

```rust
// typed lane (Checked): no boxing, no tags
extern "C" fn rt_add_i32(state: *mut ExecState, l: i32, r: i32, dst: *mut i32) -> i32;
extern "C" fn rt_currency_mul(state: *mut ExecState, l: i64, r: i64, dst: *mut i64) -> i32; // i128 kernel

// Variant lane (Widening / dynamic)
extern "C" fn rt_arith_v(state: *mut ExecState, op: u32, mode: u32,
                         l: *const Variant, r: *const Variant, dst: *mut Variant) -> i32;
extern "C" fn rt_compare_v(state: *mut ExecState, op: u32, mode: u32,
                           l: *const Variant, r: *const Variant, dst: *mut Variant) -> i32;
extern "C" fn rt_logical_v(state: *mut ExecState, op: u32,
                           l: *const Variant, r: *const Variant, dst: *mut Variant) -> i32;
extern "C" fn rt_not_v(state: *mut ExecState, src: *const Variant, dst: *mut Variant) -> i32;
extern "C" fn rt_truthy_v(state: *mut ExecState, src: *const Variant, dst: *mut i32) -> i32;

extern "C" fn rt_array_get_i32(state, arr: i64 /*SAFEARRAY*/, idx: *const i32, n: usize,
                               dst: *mut i32) -> i32;                    // typed element lane
extern "C" fn rt_call_native(state, site: *const NativeSiteDesc,
                             args: *const Variant, n: usize,
                             byref_out: *const *mut Variant, dst: *mut Variant) -> i32;
extern "C" fn rt_com_call_late(state, site: *const LateSiteDesc, recv: *const Variant,
                               args: *mut Variant, n: usize, dst: *mut Variant) -> i32;
extern "C" fn rt_variant_release(state, v: *mut Variant);                // enqueue-only, infallible
extern "C" fn rt_bstr_release(state, h: i64);                            // null-safe, ditto per handle type
extern "C" fn rt_maybe_drain(state) -> i32;                              // runs Class_Terminate fixpoint
```

**`Arith { dst, Add, l, r, Checked(Long) }`** (block has fault pad F):

```
s = call rt_add_i32(state, v_l, v_r, &scratch_i32)     ; v_l, v_r are SSA i32 values — no boxing
brif s == 0 → ok ; brif s == ST_HALT → epilogue(HALT) ; jump F_native
ok: dst_var = load scratch_i32                          ; or direct Variable def
```

**`ComCallLate`**: box receiver + each `OxCallArg` into a contiguous scratch Variant array (Named/Omitted/Const shapes encoded in the static `LateSiteDesc`), call shim; on OK move `dst` per O3, then run the descriptor's ByRef copy-out list (the shim mutated the scratch array in place; copy-out uses the same `VariantChanged`-guarded shared function vm3 uses).

**`ForEachInit`/`ForEachNext`**: enumerator state lives in this loop site's hidden stack slot (§4.1); `rt_foreach_next(state, slot, item_out, has_out)` writes the owned item (O3 into the item place) and a Bool. SafeArray-vs-IEnumVARIANT dispatch is inside the shared helper — identical to vm3.

**`StmtBoundary { stmt, clear_temps_from }`**: static release+zero over owning temp slots ≥ floor (O5); under `OXVBA_JIT_TRACE=stmt`, additionally `call rt_trace_stmt(state, stmt)`. This is a real, labeled site — never compiled away (§14.5).

**`DrainTerminations`**: `call rt_maybe_drain(state)` — re-enters compiled code (Terminate bodies) through ProcInvoker; plain native recursion under the shared `draining` guard.

---

## §6 Calling convention — typed primary entry + dynamic-entry thunk

**Decision history.** An earlier draft selected a uniform Variant-pointer convention (`fn(ctx, ret: *mut Variant, argv: *const *mut Variant, argc) -> i32`) as the *only* convention, citing Optional/Missing and ParamArray marshaling complexity and the cost of per-signature thunks at indirect call sites. A design challenge prompted re-investigation, which **refuted all three premises**:

1. **The binder already normalizes every call to one-arg-per-param with caller-side defaults** (`oxvba-bind/src/call.rs`, `omitted_optional_arg`): an omitted optional binds its folded default expression coerced to the declared type, else the declared-type zero, else `Nothing` for objects. The `Missing` sentinel (`MISSING_ARG` = 0x80020004) is passed **only** for `Optional x As Variant` with no default — it can only ever land in a Variant-typed slot. Typed parameters never see it; no suppliedness channel is needed.
2. **ParamArray is boxed by the binder into one array argument** (call.rs: "keeps the call vector one-arg-per-param, so free procs and methods need no downstream variadic handling").
3. **The per-signature thunk machinery is required by the product regardless**: JIT-generated COM interface vtable implementations (typed native slots per `TypeLibMemberMetadata` — today comhost hand-implements only a bounded Automation-safe dual tier), AddressOf→Declare callbacks, native `.dll` exports with arbitrary signatures, and (Phase B) direct typed calls *into* native COM vtables and Declare targets replacing libffi. An untyped internal convention avoids none of that machinery — it only adds boxing at every VBA-internal call and pushes typed codegen to the edges where it must exist anyway.

Conclusion: the Variant-argv shape was vm3's frame representation leaking downstream. The typed OxIR exists precisely to enable typed compilation, and the call vector is already shaped for it.

### 6.1 Primary convention — typed native entry per OxFunc

Signature derived from `locals[0..param_count]` (each carries full `OxTy` + `OxParamInfo { by_ref, variadic }`):

```
fn(ctx: *mut JitRun, [me: instance ptr — class methods], ret: *mut <ret machine type / Variant>, params…) -> i32 status
```

- **ByVal scalar** → by-value machine register (i32/i64/f32/f64; Bool i8; Currency i64).
- **ByVal Variant/owning** → `*const Variant` / handle by pointer; callee clones into its own local (O8).
- **ByRef** → `*mut` typed slot: scalar params point at the caller's escaped machine-typed slot; String/Object/Array/Record at the caller's bare-handle slot (exactly `BSTR*` / `IUnknown**` shapes, §5.1); Variant at the 16-byte slot. Type-mismatched ByRef never reaches the boundary — elaboration already inserts compound copy-in/copy-out with the `VariantChanged` guard.
- **Return** via typed out-pointer (dead-on-entry, O2) rather than multi-return: keeps every compiled entry directly callable from Rust shims (ProcInvoker) over plain C ABI. The status `i32` is the sole register return.
- **Omitted** reaches only Variant params (finding 1): caller passes a scratch holding `MISSING_ARG`; callee writes to it are discarded (vm3 parity).
- **ParamArray**: one array argument (finding 2). Element-alias copy-out uses the shared helper vm3 uses. *Implementation-time confirmation:* verify the IR carries enough for caller-side copy-out from `ArrayLiteral{aliases}`; if not, a small IR-prep addition in M4-1.
- *Implementation-time confirmation:* mirror the `Me`-receiver convention exactly from vm3's class-method call path.

### 6.2 Dynamic-entry thunk

The uniform shape survives — demoted from "the convention" to a **JIT-generated per-function adapter**:

```
extern "C" fn(ctx, ret: *mut Variant, argv: *const *mut Variant, argc: usize) -> i32
```

Generated mechanically from the known signature: unbox/coerce each argv slot to the param's machine type (the same coercion semantics as vm3's late-bound path, via the shared coerce helpers — err 13 on mismatch), call the typed entry, box the result. Consumers: `CallByName`, late-bound class member dispatch (Untyped receivers), Property Get/Let/Set resolution, ProcInvoker (event delivery, Terminate drain, host sessions, the differential harness), and `CallProcRef` where the signature cannot be statically proven. Both entries coexist permanently by design — the dynamic entry is required for late binding regardless. Generate dynamic thunks lazily / only for dynamically-reachable functions if code size warrants.

- **Static dispatch uses typed entries**: `CallProc`/`CallExtern` are direct typed calls; `CallProcRef` with statically-known signature is a typed indirect call, else the dynamic entry via the fn tables.
- **Bring-up without a convention migration**: early worksets may route static calls through dynamic entries (they exist first, M4-3), but the typed entry is the convention of record — static call sites switch to typed calls in M4-4 as an optimization inside one architecture, not a migration.

### 6.3 NativeThunk generator

One component with a shared signature-derivation core; its consumers span the milestone:

1. **COM interface vtable implementations** (a stated aim of the JIT): per-member native slots `extern "system" fn(this, typed COM wire args…, retval*) -> HRESULT` — wire↔runtime conversions via the existing `oxvba-com` helpers (`disp_params_to_runtime_call_frame` / `variant_to_com_value`), call the typed entry, map `ST_FAULT` → HRESULT + IErrorInfo. This replaces and generalizes comhost's bounded hand-written dual tier to **arbitrary** TypeLib shapes (M4-13).
2. **AddressOf → native callbacks** (to vm3's current parity level only — presently declined; the JIT declines identically).
3. **DLL export functions** (M4-14; arbitrary signatures per the generic-native-export goal).
4. (Reverse direction, Phase B) **direct typed calls into native COM vtables and Declare targets**, replacing libffi on statically-descriptored sites — the same signature model, caller side (M4-12).

### 6.4 Recursion and stack overflow (error 28)

vm3 uses heap frames (effectively unbounded); the JIT uses the native stack. Two measures, both adopted:

1. Run compiled code on a **dedicated big-stack thread** (e.g. 64 MB) owned by the engine.
2. **Prologue guard** against `ctx.stack_limit`: on trip, seat err 28 "Out of stack space" and return `ST_FAULT` *before* `rt_frame_enter` (nothing to clean up) — a catchable VBA fault flowing through normal R2 machinery, like real VBA. Additionally match vm3's frame-depth ceiling constant so both engines raise 28 at the same depth. **Never** a Cranelift trap (not resumable, not catchable).

---

## §7 Coverage boundary and re-entrancy

**Compile 100% of the OxIR vocabulary; no vm3 fallback in the product path** (JIT-v1's epitaph). What routes through *shared runtime facilities* rather than JIT-specific code — in Phase A: Declare FFI marshaling, COM transports, the event fabric, member resolution, Terminate drain, builtins. Phase B moves statically-descriptored native calls to direct typed emission (§6.3 reverse direction); everything else stays helper-mediated.

Re-entrancy: `CompiledImage::invoke` (Rust → dynamic entry → status) is the entry thunk ProcInvoker uses for event sinks, Terminate drain, and RaiseEvent fan-out — plain native recursion under the shared `draining`/`pumping` guards in ExecState. Since ExecState is `!Send` and the activation stack is strictly LIFO, re-entrancy needs no special machinery beyond those guards.

---

## §8 Cranelift specifics

- **Crates**: `cranelift-codegen`, `cranelift-frontend`, `cranelift-module`, `cranelift-jit`, `cranelift-native` (+ transitive `target-lexicon`), pinned as one version family (wasmtime-3x era, ≥ 0.116; record the exact pin at M4-3). Win64 `windows_fastcall` fully supported for JIT code and extern "C" boundary calls.
- **Settings**: `opt_level=speed` (`none` under a debug env var for compile-debug loops), `is_pic=false` (JITModule), `enable_verifier=true` in debug/trace builds, `unwind_info=false` (nothing consumes it; helpers never unwind — `catch_unwind` in every shim), **NaN canonicalization ON** (the harness canonicalizes NaN for comparison, but journal-visible formatting must match too).
- **Traps are compiler bugs only**: `br_table` default, `Unreachable`, debug tag asserts. Trap handler formats function/offset and aborts with "JIT internal error" — never surfaced as a VBA error.
- **No i128 in CLIF**: Decimal is a 16-byte memory value handled by helpers; Currency mul/div overflow needs 128-bit intermediates → permanent helpers on `oxvba-eval`'s i128 kernels (bit-exact by construction).
- **Floats**: Phase A all-helper. Phase B may inline f64 add/sub/mul (IEEE on both sides). `/`, `^`, `Mod`, float→int narrowing (banker's rounding!), and comparisons stay helpers permanently unless proven — CLIF `fcvt*` trapping/saturating semantics do not match VBA's error-6/rounding rules.
- **Memory ops**: Variant copies as two i64 load/store pairs (slots 8-aligned); `MemFlags::trusted` on frame slots.

---

## §9 Verification strategy

vm3 is the oracle; the JIT earns trust the way vm3 did against vm2 — full-corpus differential on all six axes — plus new axes the JIT specifically endangers. The live-Excel oracle remains ground truth transitively (vm3 is oracle-validated; JIT ≡ vm3 ⇒ JIT ≡ Excel on the captured set).

- **`Executor::Jit`** in `oxvba-differential`; every runner works once `HostConfig.backend` exists. `RunOutcome` gains `handle_balance: Option<HandleBalance>` and `compile: Option<CompileStats>` (per-program compile time + declined-op list).
- **`vm3_jit_differential`**: every corpus program, both engines, axis-by-axis assertion. JIT `unsupported` ⇒ skip-with-record; vm3-unsupported-but-JIT-supported ⇒ **hard fail** (scope inversion is a bug).
- **`jit_golden`**: JIT outcomes rendered with the *same* `render_outcome`, diffed line-by-line against the existing `vm3_golden.snap`. **The JIT never gets its own truth file** — it matches vm3's or fails.
- **`jit_scope.snap` ratchet**: a blessed `{program → compiled | declined(reason)}` list. Transitions are reviewed diffs; compiled-count is monotone non-decreasing across M4-3..M4-11; exit criterion at M4-11 is declines == 0.
- **Live com_matrix × 2 backends** (`OXVBA_TEST_BACKEND`): Excel/Scripting/DAO, in- and out-of-proc, events V7/V8 — the only tests where compiled code meets a real COM apartment; mandatory from M4-9.
- **Handle-balance axis** (the #1 new risk class — leaks/UAF the value axes can't see): `oxvba-runtime` gains test-feature atomic counters for live BSTRs, object boxes, SAFEARRAYs, and record buffers at their single alloc/free choke points. Balance == 0 asserted per corpus program for **both** engines. Established on vm3 in M4-0 — flushing any pre-existing interpreter imbalance before the JIT can hide behind it (intentional `Box::leak` class descriptors exempted at the leak site). Plus: nightly ASAN (`-Zsanitizer=address`, MSVC nightly) over the differential subset, and poison-on-release (0xDD tag) canary Variants in debug so a UAF becomes a deterministic type-mismatch divergence rather than silence.
- **Fuzz differential (scoped narrow)**: a proptest generator emitting single-module programs — scalar/Variant expression trees to depth ~6 across all numeric carriers, `Dim` of every scalar type, If/Select/For/Do to depth 3, ByVal/ByRef call mixes, `On Error` wrappers. Full `RunOutcome` compared vm3-vs-JIT; shrunk finds land as **permanent corpus programs** (every fuzz find compounds the golden net). Nightly + on-demand. Grammar-complete VBA fuzzing (objects/COM/events) is an explicit non-goal — re-entrancy bugs are found by the corpus and live legs.
- **Numeric micro-corpus** (~15–20 programs, written in M4-0 so they harden the vm3 golden before any JIT exists): integer overflow at ±bounds (err 6 exactness), Currency scaled-i64 overflow, banker's rounding at .5 boundaries (incl. `CLng(2147483647.5)` → 6), `Int`/`Fix` vs conversion rounding, NaN/±Inf production and comparison, `-0.0`, Single↔Double round-trips, `\`/`Mod` at INT_MIN.
- **Bisection** (mixed vm3↔native frame interop as a product feature: **rejected** — Loc-into-native-slot lifetime hazards, split state, and its own bug surface would poison the signal):
  1. corpus-program minimization (programs are small);
  2. **statement-trace diff**: both engines emit identical `{prog, func, stmt, err.number, journal_len, depth}` records under trace; the first divergent record localizes to one statement — finer than function-granular toggling, with zero interop machinery;
  3. `OXVBA_JIT_DENY_FUNCS` deny-list mixed execution via the ProcInvoker seam — `jit-debug` feature only, loudly logged, never product-reachable; a binary-search driver isolates the diverging function, then diff its CLIF/disassembly.
- **Test-gate table**:

| Gate | What | When | Budget |
|---|---|---|---|
| `cargo test --workspace` | existing nets incl. vm3 golden | per-commit | unchanged |
| `vm3_jit_differential` | full corpus, both engines, all axes + balance | per-commit from M4-3 | target < 5 min for the pair (measure in M4-0; parallelize by program if over) |
| `jit_golden` | JIT ≡ `vm3_golden.snap` | per-commit from M4-3 | ~0 (one execution feeds both assertions) |
| `jit_scope` ratchet | compiled-set monotone, declines reviewed | per-commit | ~0 |
| fuzz differential | 10k cases | nightly / on-demand | ≤ 30 min |
| ASAN differential | sanitized subset | nightly | ≤ 60 min |
| live com_matrix × 2 | real COM + events | milestone close (M4-9+), release | operator-run |
| criterion benches | §10 suite | per-milestone + perf PRs | ≤ 10 min |

- **Lockstep process rule**: every vm3 semantic change (the remaining Tier-3/4/5 spec-gap work continues in parallel) lands **with a corpus program in the same commit** — the differential then covers the JIT automatically. A vm3-only fix without corpus cover is treated as an unverified change.

---

## §10 Performance program

**Baseline before the JIT exists** (M4-0): a criterion suite in `crates/oxvba-differential/benches/` (it owns the canonical run entry points and parameterizes over `Executor` for free), vm3 numbers recorded to `docs/evidence/perf/`. Bench fixtures (~50–200 ms vm3 runtime each; execution measured net of compile, compile time its own group):

1. `scalar_loop` — Long/Double arithmetic loop (the Phase-B headline)
2. `string_concat` — `s = s & …` and a `Mid$`-builder variant (BSTR churn)
3. `array_loop` — element read/modify/write over `Long()` and `Variant()`
4. `udt_fields` — record field get/set loop
5. `call_overhead` — tight ByVal + ByRef proc-call loops
6. `error_loop` — `On Error Resume Next` around a faulting op per iteration (fault-path tax, quantifying risk 3)
7. `collection_ops` — Add/Item/Remove churn
8. `com_late_vs_early` — fixed dispatch counts, both transports (proves the JIT doesn't regress COM)
9. `image_load` — `.oxi` JSON parse + prepare (feeds risk 10 and §14's conditional binary format)

**Checked-vs-Widening lane census** (M4-0): instrument counts over the corpus and real projects — what fraction of Arith/Compare ops carry static types? This grounds the expectations below; and if the binder is conservatively choosing Widening where types are provable, improving its Checked coverage lifts **both** engines and raises the JIT ceiling — file follow-on beads if the census shows headroom.

**Honest expectations**:

| Tier | Expected vs vm3 | Notes |
|---|---|---|
| Phase A, typed-dense code | **3–10×** | dispatch elimination + registers + typed shims (no boxing/tag-dispatch on Checked lanes) |
| Phase A, Widening/Variant-dominant | 2–5× | the census tells us the real mix |
| Phase A, string/builtin-heavy | 1.5–2× | helper-bound |
| Phase A, COM-bound | ~1× | transport-dominated |
| Phase B | 10–50× `scalar_loop`, 5–15× `array_loop`, 2–4× `udt_fields` | strings/COM ≈ Phase A |

Untyped user code (all-Variant) stays in Widening helpers until the M6 speculation tier — said plainly, not hidden.

**M4 perf exit gate** (with the full verification suite green): ≥ 10× vm3 on `scalar_loop`, ≥ 5× on `array_loop`, ≥ 2× geomean over benches 1–7, ≥ 0.95× (no regression) on benches 8–9.

**Compile budget**: < 100 ms typical project, < 1 s largest corpus image; per-function compile time tracked in the compile criterion group. If a real project breaks the budget: the lazy-compile fallback is pre-designed behind the function-table indirection (entries start as compile-me trampolines behind ProcInvoker) — a contained change, not built speculatively.

**M6 hooks preserved, not built**: patchable fn-table entries (tier-up point), StmtBoundary lowering centralized in one function (profiling/DAP hooks later without touching every op), the `Backend` trait (JITModule/ObjectModule).

---

## §11 AOT PE export

In M4 scope (user decision). Builds on `AOT_CRANELIFT_PE_EXPORT_DESIGN_2026-06-20.md`.

- **Artifact**: the `Backend` trait instantiated with `ObjectModule`/a serialized relocatable blob (the design note's "skip linking" camp, wasmtime-`.cwasm`-style). Per-build output = relocatable code blob + descriptor arena + metadata. **No system linker at user build time.**
- **Stub loader**: an author-side pre-linked stub PE (built once with MSVC/lld) containing the loader (map blob RW → copy → `VirtualProtect` RX, apply cranelift relocations, W^X hygiene) and the **host import table** — fixed slots of shim/runtime function pointers. The rt-abi surface compiles as a staticlib into the stub; this is precisely why the shims live in `oxvba-rt-abi`, not `oxvba-jit` (§1.1).
- **Export wiring — resolving the design note's open Option A/B question**: OxVba has *both* export shapes, so both options ship, phased:
  - (i) **WrappedComServer `.dll`** — fixed ABI (`DllGetClassObject`/`DllCanUnloadNow`/`DllRegisterServer`) → **Option B** (fixed thunk exports in the stub, slots wired at load) fits perfectly and keeps the append-a-blob model. Ships first (M4-14).
  - (ii) **Generic native `.dll` export** — per-program export names (the build-time-reflection generic mechanism) → **Option A** (bake the export table by PE surgery via the `object` crate). Ships second, sharing the blob+loader substrate; export bodies are NativeThunk-generated (§6.3).
- **Verification**: the corpus differential re-runs against AOT-loaded code — same ExecState/shims, so identical behavior is expected; the gate proves relocation/loader correctness. AV/SmartScreen risk (the packer shape) recorded: sign the stub, strict W^X, prefer reserved-section patching if flagged in practice.

---

## §12 Worksets (DAG; critical path M4-0 → … → M4-14)

```
M4-0 ──┬── M4-1 ──┐
       └── M4-2 ──┴── M4-3 → M4-4 → M4-5 → M4-6 → M4-7 → M4-8 → M4-9 → M4-10 → M4-11 → M4-12 → M4-13 → M4-14
                              (M4-6.5 fuzz ∥ from M4-3)
```

Sizes: S ≈ ≤ 2 days, M ≈ 3–5 days, L ≈ 1–2 weeks. The corpus lights up incrementally via the `jit_scope` ratchet; unimplemented constructs decline with clean `Unsupported` throughout.

### M4-0 — Baselines, gates, and plumbing (M)
Criterion suite + vm3 numbers recorded to `docs/evidence/perf/`; corpus wall-clock measured (CI budget baseline); live-handle counters in `oxvba-runtime` + `handle_balance` in `RunOutcome` + vm3 corpus balance green (imbalances found → beads filed/fixed first); numeric micro-corpus programs added and vm3 golden re-blessed; **Checked-vs-Widening lane census** recorded (follow-on binder beads if headroom); `ExecBackend` replaces `enable_jit` end-to-end (CLI/host/comhost/differential); `Executor::Jit` added (standard decline).
**Verify:** workspace green; vm3 handle balance == 0 on all corpus programs; benches < 5% run-to-run noise; census in evidence; `oxvba run --backend jit` exits with the standard not-implemented diagnostic. **Depends:** —

### M4-1 — IR-prep passes (L)
Temp type table on `OxFunc` (`temps: Vec<OxTy>`; elaboration records at `new_temp`; `.oxi` version bump); escape analysis per §5.3 (+ escaped-temps analysis set); Assign-normalization Box/Unbox/Coerce insertion (§5.4); fixed-array shape refinement where declaration bounds are static; verifier extensions (escape soundness, Assign representation-preservation, Bool never identity-fused, fault-closure/dispatch-set domains, temp typing). **Passes always-on for both engines — one canonical image** (a JIT-only IR pipeline would mean the differential compares different programs; disqualifying).
**Verify:** vm3 golden **byte-identical** with passes on; verifier green over every corpus image; escape corner unit tests (ParamArray alias, AsNew, ByRef-of-element). **Depends:** M4-0. ∥ M4-2.
**Implementation note (2026-07-03, updated 2026-07-06):** Complete in `bd-h4oh.2`; evidence is recorded in `docs/evidence/jit/JIT_M4_IR_PREP_20260703.md`. The remaining record-layout identity cleanup was landed under M4-7 bead `bd-h4oh.9.1`; evidence is recorded in `docs/evidence/jit/JIT_M4_RECORD_LAYOUT_STRICT_ASSIGN_20260706.md`. The only non-green lane is the non-blocking formal runner, which could not start in the current Linux environment because PowerShell is not installed.

### M4-2 — `oxvba-rt-abi`: ExecState, ErrEngine, kernels, shims v1 (L)
New crate per §1; extract `ErrEngine` (cells + `route_fault`) and `ExecState` (§4.1 table) out of vm3, re-point vm3; ProcInvoker seam with the vm3 adapter; run-protocol contract documented (the §2 driver sequencing); extract `maybe_drain` core, ByRef/ParamArray copy-out, and native-call marshaling out of `Vm3` methods into shared functions; **`oxvba-eval` typed-kernel facade refactor** (checked-i64 / currency-i128 / f64 kernels as public typed entry points; Variant facades delegate — one semantic kernel, two facades); shim surface v1: typed families for Checked lanes + Variant shims for Widening/dynamic, clone/release (incl. type-specific handle releases), `rt_lib_invoke`, `rt_maybe_drain`, err shims.
**Verify:** vm3 golden byte-identical after extraction (the behavior-preservation proof); balance still 0; shim unit tests incl. property-test equality of typed and Variant facades over the same kernels; `cargo tree`: no cycle, vm3 cranelift-free. **Depends:** M4-0. ∥ M4-1.
**Implementation note (2026-07-03):** Complete in `bd-h4oh.3`; evidence is recorded in `docs/evidence/jit/JIT_M4_RT_ABI_20260703.md`. The only non-green lane is the non-blocking formal runner, which could not start in the current Linux environment because PowerShell is not installed.

### M4-3 — Cranelift skeleton: straight-line code runs (M)
Pinned cranelift deps in `oxvba-jit` only; `Backend` trait over `JITModule`; JitRun/frame layout + dynamic-entry ABI; lower StmtBoundary/Assign/consts/arith-via-shims/Return; host compiles the image when `backend == Jit`; `jit_scope.snap` ratchet live.
**Verify:** first corpus slice green in the differential (values/err/balance axes); everything else declines cleanly; ratchet active. **Depends:** M4-1, M4-2.
**Implementation note (2026-07-03):** Complete in `bd-h4oh.4`; evidence is recorded in `docs/evidence/jit/JIT_M4_CRANELIFT_SKELETON_20260703.md`. The first source-level slice runs through `Executor::Jit` with Cranelift `0.133.1`, covering straight-line `Long` globals/locals/temps, `Numeric(Long)` coercions inserted by the binder, checked `Long` add/sub/mul through `oxvba-rt-abi` shims, host `backend == Jit` manifest execution, and a live `jit_scope.snap` ratchet. Unsupported shapes remain whole-program declines with no VM fallback. The only non-green lane is the non-blocking formal runner, which could not start in the current Linux environment because PowerShell is not installed.

### M4-4 — Control flow, calls, ByRef (M–L)
Branch/loops; **typed primary entries become the convention of record** (signature derivation from OxFunc params; typed direct calls for CallProc/CallExtern); dynamic-entry thunk generator (unbox/coerce/box adapters); activation save/restore; ByRef aliasing via typed slot pointers; ParamArray (incl. the alias copy-out confirmation flagged in §6.1); err-28 stack guard with vm3-matching ceiling.
**Verify:** control/call corpus slices green **through typed entries**; late-bound programs green through dynamic entries; deep-recursion program raises 28 identically on both engines (promote vm3's test shape to corpus). **Depends:** M4-3.
**Implementation note (2026-07-03, in progress):** `bd-h4oh.5` has an implemented-subset recorded in `docs/evidence/jit/JIT_M4_CONTROL_CALLS_IN_PROGRESS_20260703.md`.

Current evidence addendum: the implemented subset also includes the selected Optional Variant missing-sentinel row `calls/optional_variant_intermediate_omitted_ismissing`, proving `Call Capture(3, , 5)` routes the intermediate omitted Optional Variant as the runtime `MISSING_ARG` sentinel observed by `IsMissing(middle)=True`, while the later supplied Optional Variant is not missing and still contributes to `first + last = 8`. The synthetic VBA information-function `CallExtern` gate now admits `IsMissing` through the same `rt_lib_invoke` path as the existing `VarType`/`TypeName`/`Is*` rows. Broader explicit omitted sentinel shapes beyond this selected intermediate Optional Variant row, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the synthetic VBA information-function `CallExtern` gate also admits `IsObject` for the existing scalar/Variant information operand subset. The existing `inline/builtin_information_exprs` row now includes `IsObject(17)=False`, proving the scalar false path through `rt_lib_invoke`; object-bearing true cases, default-member/object identity behavior, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level information predicate row `inline/stdlib_variant_predicates` now runs through the JIT differential scope, proving `IsDate(DateSerial(2026, 2, 28))=True`, `IsNumeric(17)=True`, and `IsObject(17)=False` under `Executor::Jit`. This is a no-new-lowerer source ratchet over the already promoted `DateSerial`/`IsDate`/`IsNumeric`/`IsObject` paths; object-bearing true cases, broader string source-place/default-member/object/record information operands, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level sentinel/error predicate row `inline/coercion_null_empty_error_predicates` now runs through the JIT differential scope, proving `IsEmpty(Empty)=True`, `IsNull(Null)=True`, `IsError(CVErr(7))=True`, `IsError(Null)=False`, and `IsNumeric(CVErr(7))=False` under `Executor::Jit`. This is another no-new-lowerer source ratchet over the already promoted `IsEmpty`/`IsNull`/`IsError`/`IsNumeric` and `CVErr` paths; broader string source-place/default-member/object/record information operands, `VarType`/`IsNumeric` tag matrices beyond the promoted rows, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `VarType`/`IsNumeric` tag matrix row `inline/introspection_vartype_isnumeric_tags` now runs through the JIT differential scope, proving `VarType(vbNullString)=8`, `VarType(Null)=1`, `VarType(CVErr(9))=10`, `VarType(7)=2`, `IsNumeric(vbNullString)=False`, `IsNumeric(Null)=False`, `IsNumeric(CVErr(9))=False`, and `IsNumeric(7)=True` under `Executor::Jit`. The information-function gate now admits direct string-literal operands via the existing variant-descriptor path (`StringLiteral`), covering `vbNullString`/`Const(Str(""))` without broadening to string source places or object/default-member/record operands; broader string source-place/default-member/object/record information operands, non-constant `TypeOfIs`, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `vbNullString` predicate row `inline/string_vbnullstring_predicates` now runs through the JIT differential scope, proving a Variant local assigned `vbNullString` retains an empty String payload and reports `IsEmpty(v)=False`, `IsNull(v)=False`, and `IsError(v)=False` under `Executor::Jit`. This is a no-new-lowerer source ratchet over the already promoted Variant information operand path; broader string source-place/default-member/object/record information operands, non-constant `TypeOfIs`, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level date string policy row `inline/stdlib_date_string_policy` now runs through the JIT differential scope, proving `Year(DateValue("1 Jan 2000"))=2000`, `Month(CDate("Jan. 1, 2000"))=1`, `Day(DateValue("January 1, 2000"))=1`, `IsDate("1 Jan 2000")=True`, and `IsDate("February 30, 2000")=False` under `Executor::Jit`. The lowerer gate now admits direct string literals for `DateValue`/`TimeValue` and Date/Variant operands for one-argument date-part built-ins, enough for nested `DateValue(...)` Variant Date temps; broader `DateValue`/`TimeValue` string source-place/Variant operands, slash-date/locale-sensitive parsing, non-Date Variant date-part carriers beyond promoted `DateValue`/`CDate` results, non-constant `TypeOfIs`, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the JIT now admits the descriptor-free `TypeOf Nothing Is <Type>` object subset and stores `False`, matching the VM3 guard that handles `Nothing` before descriptor lookup. Evidence: `jit_typeof_nothing_is_false_without_object_descriptors` and the source-level differential row `jit_project_typeof_nothing_matches_vm3_without_descriptors`; non-constant `TypeOfIs` operands still decline with a descriptor-required unsupported diagnostic, so project class/interface matching remains M4-8 object work.

Current evidence addendum: the source-level serial/value round-trip rows `inline/stdlib_date_serial_value` and `inline/stdlib_time_serial_value` now run through the JIT differential scope, proving `DateValue(DateSerial(2026, 2, 28))` preserves Date serial `46081` and `TimeValue(TimeSerial(1, 2, 3))` preserves time serial `3723/86400` under `Executor::Jit`. This is a no-new-lowerer source ratchet over the already promoted `DateSerial`/`TimeSerial` and `DateValue`/`TimeValue` Variant Date operand paths; broader serial range/error rows, successful `DateAdd`/`DateDiff` interval/value semantics, slash-date/locale-sensitive parsing, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level date add/diff row `inline/stdlib_date_add_diff` now runs through the JIT differential scope as a raised row, proving VM3/JIT both seat runtime error 5 for numeric-interval `DateAdd(1, 3, DateSerial(2026, 2, 28))` before the subsequent `DateDiff` under `Executor::Jit`. The lowerer now admits the exact three-argument descriptor-backed `DateAdd(interval, number, date)` and three-argument `DateDiff(interval, date1, date2)` shapes needed for this source to compile; successful interval-string DateAdd/DateDiff values, optional DateDiff firstday/firstweek arguments, Excel/VBA interval validation, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level date-part expansion row `inline/stdlib_datetime_expansion` now runs through the JIT differential scope, proving `Year(DateSerial(2024, 2, 3))=2024`, `Month(DateSerial(2024, 2, 3))=2`, `Day(DateSerial(2024, 2, 3))=3`, and `Weekday(DateSerial(2024, 2, 3))=7` under `Executor::Jit`. This is a no-new-lowerer source ratchet over the already promoted `DateSerial` and one-argument date-part Variant Date operand paths; optional Weekday firstday/calendar shapes beyond the promoted exact row, broader date-name/time-current host rows, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level numeric/conversion rows `inline/stdlib_numeric_expansion` and `inline/conversion_extended_scalar_subset` now run through the JIT differential scope, proving `Hex(31)="1F"`, `Oct(17)="21"`, `Atn(9)=1.460139105621001`, `Tan(11)=-225.95084645419513`, `CSng(7)=7`, `CByte(8)=8`, `CCur(9)=9`, and `CDec(10)=10` under `Executor::Jit`. This is a no-new-lowerer source ratchet over the already promoted `Hex`/`Oct`, unary math, and scalar conversion gates; broader string/format expansion rows beyond the later-promoted `Chr`/`Asc`/`StrConv`/one-argument `Format` source rows, broader random/financial rows that require unsupported argument shapes, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level numeric identity and conversion-chain rows `inline/stdlib_math_transcendental_identity`, `inline/conversion_val_str_subset`, `inline/conversion_clng_cint_chain`, and `inline/conversion_nested_clng_cint` now run through the JIT differential scope, proving `Sin(0)=0`, `Cos(0)=1`, `Log(1)=0`, `Exp(0)=1`, `Val(Str(9))=9`, `CInt(7)=7`, `CLng(CInt(7)+3)=10`, `CInt(...-2)=8`, and `CLng(CInt(7))=7` under `Executor::Jit`. This is a no-new-lowerer source ratchet over the already promoted unary math, `Str`/`Val`, and scalar conversion gates; string constant rows still require wider constant/source operand gates, random/financial rows still require unsupported call shapes, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/conversion_cint_basic` and `inline/stdlib_error_cverr_identity` rows now run through the JIT differential scope without lowerer changes, proving `CInt(5)=5` with an Integer carrier and `CVErr(17)` preserves an Error carrier under `Executor::Jit`. Broader conversion matrices, object conversion errors, error-handler/Resume Next flows, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/stdlib_error_err_raise_fail` row now runs through the JIT differential scope as a raised row, proving bare constant `Err.Raise 9` seats runtime error 9 under `Executor::Jit`. This promotes only the no-source/no-description/no-help metadata `inherit=true` terminator form with an Integer/Long constant error number; explicit `Err.Raise` metadata arguments, non-constant numbers, handled `On Error`/Resume Next flows, Windows/Excel source/description/help metadata confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/stdlib_error_err_raise_resume` row now runs through the JIT differential scope, proving `On Error Resume Next` catches a bare constant `Err.Raise 11`, resumes at the following statement, and leaves `Err.Number=11` under `Executor::Jit`. The JIT now routes seated `ST_FAULT` statuses from fallible helper calls through the current block's fault pad, lowers the Resume Next/Goto 0/Goto -1 error-handler state changes through the shared `ErrEngine`, dispatches Resume Next pads through `rt_route_fault`, and admits `Err.Number` reads to Long/Variant destinations. `On Error GoTo <label>`, `Resume`/`Resume Next` terminators, explicit `Err.Raise` metadata arguments, non-constant raise numbers, Err fields beyond `Number`, `Erl` line capture, Windows/Excel source/description/help metadata confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/on_error_resume_next` row now runs through the JIT differential scope, proving legacy `Error 5` composes with the same Resume Next fault-pad route and leaves `Err.Number=5` under `Executor::Jit`. This widens only the bare constant no-explicit-metadata `Raise` terminator shape to include `inherit=false` (`Error <n>`) defaults while preserving bare `Err.Raise` omitted-field inheritance when current `Err` fields are inheritable; explicit metadata arguments, non-constant raise numbers, `On Error GoTo <label>`, `Resume`/`Resume Next` terminators, Err fields beyond `Number`, `Erl` line capture, Windows/Excel metadata confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/on_error_resume_continue`, `inline/on_error_default_fail`, and `inline/on_error_goto_zero_fail` rows now run through the JIT differential scope without further lowerer changes. They prove Resume Next continues after legacy `Error 2` and reaches `x=2`, an unhandled legacy `Error 9` propagates as runtime error 9, and `On Error GoTo 0` disables the handler before `Error 3` propagates as runtime error 3 under `Executor::Jit`. This remains within the bare constant legacy `Error`/Resume Next/GoTo 0 subset; label handlers, `Resume`/`Resume Next` terminators, explicit `Err.Raise` metadata arguments, non-constant raise numbers, Err fields beyond `Number`, `Erl` line capture, Windows/Excel metadata confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/err_clear_basic`, `inline/error_raise_custom_clear_cycle`, and `inline/err_proc_call_boundary_clears` rows now run through the JIT differential scope after lowering `OxInst::ClearErr` through `rt_err_clear`. They prove `Err.Clear` resets `Err.Number` to 0 after legacy `Error 7`, repeated Resume Next `Err.Raise`/`Err.Clear` cycles report `100`, `0`, and `200`, and a plain procedure call does not clear the active Resume Next `Err.Number=7` under `Executor::Jit`. This remains within bare constant `Error`/`Err.Raise`, Resume Next, `Err.Clear`, and `Err.Number`; label handlers, `Resume`/`Resume Next` terminators, explicit `Err.Raise` metadata arguments, non-constant raise numbers, Err fields beyond `Number`, `Erl` line capture, Windows/Excel metadata confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/err_surface_fields_subset` and `inline/err_clear_full_surface_reset` rows now run through the JIT differential scope after widening `ErrFieldGet` lowering to numeric `Number`/`HelpContext`/`LastDllError` and string `Description`/`Source`/`HelpFile` fields. They prove the deterministic runtime default surface for legacy `Error 9` (`Subscript out of range`, help context `1000009`, the current VBA7.1 help-file path, and the harness-specific default Source value) and prove `Err.Clear` resets those fields to zero/empty under `Executor::Jit`. This remains within deterministic Linux runtime Err fields; `Err` property writes, explicit `Err.Raise` metadata arguments, non-constant raise numbers, `Erl` line capture, Declare-driven `LastDllError`, Windows/Excel metadata/help-file confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/resume_next_statement_ok` and `inline/err_resume_next_clears` rows now run through the JIT differential scope after lowering `OxTerminator::ResumeNext` through `rt_resume`. They prove a `Resume Next` statement continues at the runtime-held resume-next target when legal and, when no active error remains after an earlier Resume Next handler dispatch, seats runtime error 20 while the active handler policy still resumes at the following statement so `Err.Number=20` is observable under `Executor::Jit`. This remains within bare `Resume Next` statement handling under active `On Error Resume Next`; `Resume`, `Resume <label>`, label handlers, explicit `Err.Raise` metadata arguments, non-constant raise numbers, `Erl` line capture, Windows/Excel metadata confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/resume_statement_basic`, `inline/resume_label_basic`, `inline/on_error_goto_label_resume`, and `inline/error_goto_label_resume_next` rows now run through the JIT differential scope after widening `SetErrorHandler` to `On Error GoTo <label>`, dispatching label-handler pads through a runtime-block table while retaining the cheap static Resume Next/unwind path for functions without label handlers, and lowering `Resume` plus `Resume <label>` through `rt_resume`. They prove a label handler can catch legacy `Error`/`Err.Raise`, `Resume` can retry the faulting statement after the handler mutates state, `Resume <label>` can transfer to a named source label, and handler-side `Resume Next` resumes after the original faulting statement under `Executor::Jit`. This remains within bare label handlers and constant error numbers; nested handler chains, `Erl` line capture, explicit `Err.Raise` metadata arguments, non-constant raise numbers, GoSub/Return redispatch, Windows/Excel metadata confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level math-primitives row `inline/stdlib_math_primitives` now runs through the JIT differential scope as a raised row, proving VM3/JIT both seat runtime error 5 for `Round(19, -1)` under `Executor::Jit`. Because the fixture terminates at the negative-digits `Round` call, it does not broaden successful source-level `Abs`/`Sgn`/`Sqr` value evidence beyond the already promoted synthetic rows; successful mixed primitive source fixtures, wider string constant/source operand gates, random/financial rows, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level scalar-constant string rows `inline/stdlib_len_basic`, `inline/stdlib_slice_ops`, and `inline/stdlib_instr_case_ops` now run through the JIT differential scope, proving `Len(1234)=4`, `Left(12345, 2)="12"`, `Right(12345, 2)="45"`, `Mid(12345, 2, 3)="234"`, `InStr(12345, 34)=3`, `LCase(789)="789"`, and `UCase(654)="654"` under `Executor::Jit`. The lowerer gate now admits scalar constants as descriptor-backed Variant operands for the existing string-oriented `CallExtern` path; broader ANSI `Chr`/`Asc` codepage edges, broader `StrConv` modes/byte-array/CJK forms, broader `Format`/`Format$` masks and locale forms, broader typed numeric source places, broader optional/string rows beyond the promoted descriptor subset, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level string expansion core row `inline/stdlib_string_expansion_core` now runs through the JIT differential scope, proving `Space(4)="    "`, `String$(3, 65)="AAA"`, `Chr$(66)="B"`, `Asc(c)=66`, and `StrConv(777, 1)="777"` under `Executor::Jit`. The lowerer gate now admits ANSI `Chr`/`Asc` alongside the existing wide `ChrW`/`AscW` path and the fixed two-argument `StrConv(value, conversion)` shape through the descriptor-backed Variant/fixed-integer `CallExtern` path; broader ANSI codepage/non-ASCII edges, `StrConv` byte-array/CJK/locale modes, broader `Format`/`Format$` masks and locale forms, broader typed source places, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level format core row `inline/stdlib_format_core` now runs through the JIT differential scope, proving `Len(Format(12345))=5` and `Len(Format$(678))=3` under `Executor::Jit`. The lowerer gate now admits one-argument `Format`/`Format$` through the descriptor-backed Variant/scalar-constant `CallExtern` path so the runtime helper takes its CStr-passthrough branch; explicit format masks, named/custom number/date/time formats, locale-sensitive formatting, non-promoted optional arguments, broader string-typed alias/destination shapes, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level advanced string rows `inline/stdlib_advanced_instrrev_like`, `inline/stdlib_advanced_replace_trim`, and `inline/stdlib_advanced_strcomp` now run through the JIT differential scope without lowerer changes, proving `InStrRev(123231, 23)=4`, `12 Like 12` takes the true branch, `Replace(12345, 23, 67)="16745"`, `Trim(456)="456"`, `RTrim(321)="321"`, `StrComp(12, 123)=-1`, and `StrComp(123, 123)=0` under `Executor::Jit`. Broader `Split`/`Join`, optional compare/search/replacement forms beyond promoted rows, locale-sensitive text comparison, byte/Unicode edges, string aliases/destinations, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/stdlib_advanced_split_join` row now runs through the JIT differential scope as a raised row, proving VM3/JIT both seat runtime error 13 for `Join(789, 0)` after `Split(123231, 23)` has compiled and executed under `Executor::Jit`. The lowerer admits exact two-argument descriptor-backed `Split` and `Join` shapes through the existing Variant/scalar-constant `CallExtern` path; successful array-producing `Split`, successful array-consuming `Join`, optional delimiter/limit/compare forms beyond this exact row, array element carrier matrices, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/stdlib_rnd_isolated` row now runs through the JIT differential scope, proving `Rnd -1`, `Randomize 1`, and the subsequent deterministic `Int(Rnd() * 100)` sequence produce `33` and `6` under `Executor::Jit`. The lowerer now admits `Rnd([number])` and `Randomize([number])` with no destination or Variant destinations through the descriptor-backed `CallExtern` path, and `Int`/`Fix` admits Variant temps so the random arithmetic result can feed `Int`. Broader `Rnd(0)`, additional seed/argument matrices, host-timer `Randomize` without an explicit seed, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level financial zero-rate row `inline/stdlib_financial_zero_rate` now runs through the JIT differential scope, proving four-argument `FV(0, 3, 2, 5)=-11`, `PV(0, 3, 2, 5)=-11`, and `PMT(0, 3, 6, 3)=-3` under `Executor::Jit`. The lowerer now admits exactly four ByVal descriptor-backed Variant/scalar-constant operands for `FV`/`PV`/`PMT`; optional fifth `type` arguments, non-zero-rate financial algorithms beyond existing VM3 evidence, `NPV`/`IRR`/`MIRR` rows, random/financial mixed rows, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the source-level `inline/financial_algorithm_rate_nper_subset` row now runs through the JIT differential scope, proving `Rate(12, -100, 1000)=0.02922854076913337` and `NPer(0, -100, 1000)=10` under `Executor::Jit`. The lowerer now admits exact three-argument `Rate`/`NPer` calls through the descriptor-backed `CallExtern` path, using the existing Variant triple validator for `Replace` while narrowing `Rate`/`NPer` operands to non-null numeric constants/source places so unary-minus temps from the fixture can be boxed without widening every Variant argument shape. Optional `fv`/`type`/`guess` financial arguments, broader `NPV`/`IRR`/`MIRR`, non-convergence/tolerance rows, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: ordinary static `CallProc` lowering now includes a `JIT_CALL_ARG_BYREF_COPY` descriptor for source-level `ByVal` actuals supplied to default/explicit `ByRef` parameters. The runtime materializes those descriptors into callee-local parameter slots and deliberately does not install a caller alias, so expression, parenthesized, and explicit `ByVal` actuals do not copy back. The source-level differential scope now includes `calls/consolidate_nested_call_chain`, `control/consolidate_for_select_call`, `control/consolidate_while_byref_mix`, `error/error_resume_function_propagation`, and `error/error_nested_mode_transitions`, proving the new copy-in descriptor composes with nested ordinary calls, `For`/`Select`/`While` control flow, label-handler `Resume` propagation, and nested error-mode transitions under `Executor::Jit`. Broader typed primary entries, direct typed calls, dynamic-entry thunks, late-bound dispatch, broader ByRef copy-in/coercion matrices beyond the promoted scalar/String/Variant slot families, `GoSub`/`Return` redispatch through error handlers, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: `OxTerminator::GoSub` and `OxTerminator::GoSubReturn` now lower through a per-activation JIT GoSub stack and the same static runtime-block redispatch table used for `Resume` targets. The source-level differential scope now includes `calls/gosub_basic`, `calls/gosub_repeated`, `control/gosub_nested_labels`, `control/gosub_loop_accumulate`, `control/consolidate_for_gosub_mix`, `error/consolidate_gosub_error_mix`, `error/gosub_return_without_gosub`, `error/gosub_return_without_gosub_resume_next`, and `error/gosub_return_without_gosub_label_handler`, proving LIFO return behavior, repeated GoSub reuse, loop/control composition, Resume Next composition, empty-stack `Return` seating VBA runtime error 3, and that the seated empty-stack error is observable through both Resume Next and label-handler `Resume` routes under `Executor::Jit`. Broader non-empty GoSub/Return matrices inside label-handler chains, cross-procedure invalid forms, typed primary entries, direct typed calls, dynamic-entry thunks, late-bound dispatch, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Current evidence addendum: the first direct static dispatch slice now bypasses the descriptor `rt_jit_call_proc_i32` helper for exact internal `CallProc` shapes that target either a no-argument `Sub` with no return destination, a no-argument `Function` with a supported scalar/String/Variant return copied into an exact or Variant destination, statement-style no-argument `Function` calls with ignored returns, a one- or two-argument `Sub Foo(ByVal x As Long, ...)` with already `i32`-lowerable actuals and no destination, or a one- or two-argument `Function Foo(ByVal x As Long, ...)` with already `i32`-lowerable actuals and a supported scalar/String/Variant return type copied or ignored. The caller asks the narrow direct-enter helper to perform callee activation/frame setup and stack-depth guard, calls the callee body by local Cranelift function reference, and then asks the matching direct-exit helper to clone any needed return value, pop/prune/restore the activation, and route any non-OK status through the caller fault pad. This proves the local-function direct-call path can preserve the existing ByVal copy-in, return-copy, ignored-return, activation, `ST_FAULT`/`ST_HALT`, and err-28 semantics for the current no-argument plus one-/two-`Long` boundary; wider scalar/String/Variant argument shapes, three-or-more-argument calls, mixed typed argument lists, ByRef/ParamArray direct typed calls, dynamic-entry thunks, late-bound dispatch, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

Implemented subset: Branch/Jump loop shapes, Bool/Byte/Integer/LongLong/Currency/Single/Double/Date slot helpers, direct OxIR `Logical`/`Not` lowering for typed `Bool`/`Long`/`Integer`/`LongLong` operands plus source-level `Bool`, `Long`, mixed fixed-integer, and LongLong logical ratchet rows including `And`/`Or`/`Eqv`/`Not` over LongLong operands and mixed fixed-integer LongLong logical expressions, narrow helper-mediated `Variant` `Logical`/`Not`/`Truthy`/`Compare` lowering through shared `rt_logical_v`/`rt_not_v`/`rt_truthy_v`/`rt_compare_v` plus helper-mediated `Variant` `Neg` through shared `rt_neg_v`, with source-level `Null` logical, truthiness, comparison, numeric Variant logical/comparison, explicit Variant arithmetic Null/mixed-Double, explicit Variant negation, Variant-to-current-scalar numeric coercion, Boolean numeric assignment, and source `Double` negation ratchet rows, `LongLong` conditional comparison/truthiness plus a mixed fixed-integer comparison ratchet row and checked add/sub/mul via shared i64 ABI shims, checked `Long`/`LongLong` integer-division and remainder via shared ABI shims with source-level `Long`/`LongLong` `\`/`Mod` coverage, checked `Integer` and `Byte` add/sub/mul via shared narrow ABI shims with source-level arithmetic/overflow ratchet rows, checked `Currency` add/sub/mul plus same-type Currency comparison/truthiness via scaled Currency lanes, checked scalar `Neg` lowering for direct `Long`/`Integer`/`Byte`/`LongLong`/`Currency`/`Single`/`Double` lanes with source-level `Long`/`LongLong`/`Currency`/`Single` negation rows, finite same-type `Single`/`Double` comparison/truthiness plus same-type `Date` comparison/truthiness via separate floating lanes, direct OxIR checked `Single`/`Double` add/sub/mul with source-level `Single` add/sub/mul assignment coverage, source-level `Double` add/sub/mul plus `Double` `/`/`^` and `Date` serial-add assignment shapes through a narrow `Widening`/`Variant` helper lane, narrow same-type `Long`/`LongLong`/`Currency`/`Single`/`Double`/`Date`/`Byte`/`Integer`/`Boolean` coercions for the current scalar slice, helper-mediated static calls using a stack descriptor array for the current `Long`/`LongLong`/`Currency`/`Single`/`Double`/`Date`/`Byte`/`Integer`/`Bool` scalar ByVal and exact scalar ByRef/return subset plus selected typed scalar/String returns boxed into `Variant` destinations plus a Win64 `LongPtr` ByVal/return row lowered through the LongLong lane, direct OxIR ByVal `Variant()` ParamArray SAFEARRAY pack/copy-in coverage plus source-level ParamArray `UBound` pack/empty, explicit first-dimension/invalid-dimension, Option Base 1 zero-base, simple local, Long/String globals, duplicate, and typed Long/LongLong/Win64 LongPtr/Integer/Byte/Boolean/String/fixed-length `String * 3`/Currency/Single/Double/Date local alias copy-out, fixed-array-element alias copy-out, Variant-held array element mutation, and local/global parenthesized plus local/global explicit call-site ByVal no-alias contrast rows and Variant-carrier `Array(...)` first-dimension `LBound`/`UBound` rows including explicit dimension, invalid-dimension error coverage, plus `VarType`/`TypeName` rows, dynamic Variant-array unallocated `LBound`/`UBound` error rows (`arrays/dynamic_lbound_unallocated_error`, `arrays/dynamic_ubound_unallocated_error`), dynamic/fixed Variant-array explicit-dimension bounds and invalid-dimension error rows (`arrays/dynamic_bounds_explicit_dim`, `arrays/dynamic_lbound_dim_zero_error`, `arrays/dynamic_ubound_dim_too_high_error`, `arrays/fixed_bounds_explicit_dim`, `arrays/fixed_lbound_dim_zero_error`, `arrays/fixed_ubound_dim_too_high_error`), fixed one-dimensional Variant-array `Dim` allocation, dynamic one-dimensional Variant-array `ReDim` without `Preserve` plus selected one-dimensional `ReDim Preserve` rows, and `ArraySet`/`ArrayGet` source rows with default-zero, explicit lower-bound, Option Base 1 lower-bound coverage, fixed-array subscript-error, dynamic `ReDim` expansion, reset-without-`Preserve`, shrink-to-subscript-error, negative-lower-bound, dynamic-bound-expression, dynamic Option Base 1 bounds, upper-less-than-lower error, `Preserve` upper-less-than-lower error, `Preserve` keep-value, `Preserve` unallocated defaulting, `Preserve` explicit-lower keep-value, `Preserve` shrink/expand-tail, and `Preserve` lower-bound-change error, fixed-size `ReDim`/`ReDim Preserve` error 10, fixed-array `Erase` reset/bounds-preservation, dynamic `Erase` deallocation, dynamic `Erase` post-bounds errors, and the current array row set (`arrays/bounds_error`, `arrays/zero_index`, `arrays/store_load`, `arrays/explicit_lower_bound`, `arrays/option_base_one_bounds`, `arrays/redim_expand`, `arrays/redim_without_preserve_resets`, `arrays/redim_shrink_bounds_error`, `arrays/redim_negative_lower_bound`, `arrays/redim_dynamic_bound_expression`, `arrays/redim_option_base_one_bounds`, `arrays/redim_upper_less_than_lower_error`, `arrays/redim_preserve_upper_less_than_lower_error`, `arrays/redim_fixed_variant_array_error`, `arrays/redim_preserve_explicit_lower_keeps_value`, `arrays/redim_preserve_fixed_variant_array_error`, `arrays/redim_preserve_keeps_values`, `arrays/redim_preserve_unallocated_defaults`, `arrays/redim_preserve_lower_bound_change_error`, `arrays/redim_preserve_shrink_expand_clears_tail`, `arrays/erase_fixed_reset`, `arrays/erase_fixed_bounds_preserved`, `arrays/erase_dynamic_bounds_error`, `arrays/erase_dynamic_lbound_unallocated_error`, `arrays/erase_dynamic_ubound_unallocated_error`), folded Optional ByVal `Variant` defaults, omitted Optional ByVal `Variant` `MISSING_ARG` sentinel paths, binder-materialized source-level Optional ByVal `Bool`/`Byte`/`Integer`/`Long`/`LongLong`/`Currency`/`Single`/`Double`/`Date`/`String` default/no-default rows, narrow verifier-valid `Variant` slot assignment/Box/return-call shapes, direct checked exact-tag `Unbox` helper coverage, direct OxIR `VariantChanged` helper coverage for copy-out guards, exact ByRef `Variant` copy-out shapes, narrow helper-mediated synthetic `VBA` library `CallExtern` coverage for the source-level `Abs(Null)`, `Abs(Empty)`, `Abs(Boolean)`, `Abs(Integer)`, `Abs(Integer.Min -> Long)`, `Abs(Long)`, `Abs(Long.Min -> Double)`, `Abs(LongLong)`, `Abs(Single)`, `Abs(Double)`, `Abs(Currency)`, `Int(Null)`, `Int(Empty)`, `Int(Boolean)`, `Int(Integer)`, `Int(Long)`, `Int(LongLong)`, `Int(Single)`, `Int(Double)`, `Int(Currency)`, `Int(Date)`, `Fix(Null)`, `Fix(Empty)`, `Fix(Boolean)`, `Fix(Integer)`, `Fix(Long)`, `Fix(LongLong)`, `Fix(Single)`, `Fix(Double)`, `Fix(Currency)`, `Fix(Date)`, `Sgn(0)`, `Sgn(Null)`, `Sgn(Empty)`, `Sgn(Boolean)`, `Sgn(Integer)`, `Sgn(Long)`, `Sgn(LongLong)`, `Sgn(Single)`, `Sgn(Double)`, `Sgn(Currency)`, `CBool`, `CByte`, `CInt`, `CLng`, `CLngLng`, `CLngPtr`, `CSng`, `CDbl`, `CCur`, `CDate`, `CDec`, `CStr`, `CVar`, `CVErr` valid/range-error rows, numeric-source `Hex`/`Oct`/`Str` String-result rows, non-null `Double`-source `Sqr`/`Sin`/`Cos`/`Log`/`Exp`/`Atn`/`Tan` unary math rows including `Sqr`/`Log` domain errors and `Exp` overflow through shared runtime error seating, explicit `Date`-source `Year`/`Month`/`Day`/`Hour`/`Minute`/`Second` date-part rows, scalar/Variant operand `VarType`/`TypeName`/`IsNumeric`/`IsDate`/`IsNull`/`IsEmpty`/`IsError` information rows plus dynamic/fixed-array and Variant-carrier `IsArray` rows and dynamic/fixed Variant-array plus Variant-carrier `Array(...)` `VarType`/`TypeName` rows including fixed post-Erase information, explicit `Date`-source `DateValue`/`TimeValue` rows, three-argument fixed-integer `DateSerial`/`TimeSerial` rows, fixed-integer `RGB`/`QBColor` color rows, the explicit fixed-integer `Error(number)` row, the `Len` and `LenB` rows over a String-bearing `Variant` source local, the pure wide `ChrW`/`AscW` Variant-string row, the fixed-integer `Space` row plus `Space(-1)` error row, the `LCase`/`UCase` Variant-string case-folding row, the `Val` Variant-string numeric row, the `Trim`/`LTrim`/`RTrim` Variant all-space row, the `StrReverse` Variant-string row, the `String(count, code)` fixed-integer char-code row plus negative-count error row, the `Left`/`Right` Variant-plus-count, unit-count first/last-character row, exact source-length row, and zero/overlong count-edge rows in both directions, the `LeftB`/`RightB` Variant-byte-count rows including byte counts 2 and 6, even count 4, zero/overlong byte-count edges in both directions, and exact byte-length count 10, the `Mid` Variant-start-with-optional-count rows including start-zero, negative-start, and negative-length error rows plus overlong-start, overlong-count suffix/middle/full-source, exact last-character, exact full-source-count, exact prefix-count, exact suffix-count, zero-length first/middle/end, and omitted-length suffix/full-source/overlong-start value rows, direct string-literal operand coverage for selected string-oriented library/`Like` gates including companion `LenB`/`AscW`/`LTrim`/`RTrim`/`LeftB`/`RightB`/`InStrRev` forms, direct literal `Left`/`Right` code-unit count edges, direct literal `Mid` start/count, value edges, and start-zero/negative-start/negative-length error edges, direct literal `LeftB`/`RightB` byte counts 2 and 6 plus zero/overlong/exact byte-count edges, and direct literal odd-byte `LeftB`/`RightB` count-1/count-3 observables, and statically typed `String` source-place operand coverage over the same descriptor-supported string bridge including companion `LenB`/`AscW`/`LTrim`/`RTrim`/`LeftB`/`RightB`/`InStrRev` forms, static `String` `Left`/`Right` code-unit count edges, static `String` `Mid` start/count, value edges, and start-zero/negative-start/negative-length error edges, and static `String` `LeftB`/`RightB` byte counts 2 and 6 plus zero/overlong/exact byte-count edges, plus the descriptor-backed Variant-operand scalar conversion bundle row, raw-bit packed `Single`/`Double`/`Date` ByVal descriptor values, mixed ByRef/ByVal scalar arguments, nested ByRef alias chasing, helper-mediated `CallProcRef` for known single-`AddressOf` targets and ambiguous same-signature `AddressOf` target sets through the same scalar-plus-Variant stack-descriptor subset as static calls plus direct exact String known-target `CallProcRef` return/ByRef coverage, narrow direct ambiguous same-signature String no-argument return, ByVal return, and ByRef mutation `CallProcRef` coverage, direct known-target `Long`/`String` return-to-`Variant` `CallProcRef` coverage, and direct ambiguous same-signature `Double`/`String` return-to-`Variant` `CallProcRef` coverage, the existing dynamic fallback for truly unknown-signature/runtime-only `ProcRef` targets covering one-/two-argument, i32-compatible ByVal carrier, and mixed ByRef/ByVal `Long`-only Sub calls plus `Long` and actual `Variant` LongOnly function-as-statement calls, `Long`-only function calls returning to exact `Long` and `Variant` destinations, selected actual `Variant` return-to-`Variant` Long-only units including one-/two-argument and mixed ByRef/ByVal arguments, exact String ByVal-only return/no-destination including global-, temp-, and fixed-length-source no-destination, function-as-statement including actual Variant-return, destination actual Variant-return units, and unknown-signature exact String ByRef mutation plus mixed String ByRef with selected Variant-to-String ByVal payload/error rows, direct String literal carriers, and String/FixedStr source-place carriers across the promoted no-destination, concrete String return-to-String/Variant destination, and actual Variant return-to-Variant shapes, and direct no-argument concrete scalar/String returns into exact matching scalar/String and `Variant` destinations plus no-argument scalar/String/actual Variant-returning function-as-statement rows with ignored returns and side-effect globals, shared activation save/restore shims, and a VM3-ceiling frame-count guard that seats error 28 through both direct helper invocation and the compiled static `CallProc` helper path before callee execution.

The implemented ParamArray call subset now also includes `params/paramarray_named_fixed_tail_pack`, covering `Call Capture(target := target, 3, 4)` where the binder maps the named fixed `target` argument to the ByRef slot and packs the following positional arguments into the trailing `ParamArray`. The same named fixed-tail subset now includes `params/paramarray_named_fixed_tail_alias_copyout`, proving a named fixed `target` can coexist with a tail l-value alias whose callee-side `items(0) = CLng(17)` copy-outs to the caller while the fixed target receives `UBound(items)+101`, plus `params/paramarray_named_fixed_tail_array_element_alias_copyout`, proving the same route preserves fixed-array-element tail aliases. `params/paramarray_named_fixed_tail_array_element_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_array_element_byval_no_alias` prove the fixed-array-element parenthesized and explicit call-site `ByVal` counterparts stay copy-only while the named fixed ByRef slot is still updated. `params/paramarray_named_fixed_tail_duplicate_alias_copyout` proves duplicate named-tail aliases keep final-write-wins copy-out ordering while the named fixed ByRef slot is updated independently; `params/paramarray_named_fixed_tail_duplicate_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_duplicate_byval_no_alias` prove duplicate local tail sources stay copy-only when both occurrences are parenthesized or explicitly passed `ByVal` through the same named fixed-tail route. `params/paramarray_named_fixed_tail_global_alias_copyout` proves module-global `Long` aliases use the same named fixed-tail route, `params/paramarray_named_fixed_tail_global_string_alias_copyout` proves the same route for module-global `String` aliases, and `params/paramarray_named_fixed_tail_global_longptr_alias_copyout`, `params/paramarray_named_fixed_tail_global_longptr_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_global_longptr_byval_no_alias` prove the same route for module-global Win64 `LongPtr` alias and no-alias tails. `params/paramarray_named_fixed_tail_global_typed_scalar_alias_bundle_copyout` broadens the module-global route across LongLong/Integer/Byte/Boolean/Currency/Single/Double/Date tail elements in one call; `params/paramarray_named_fixed_tail_global_typed_scalar_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_global_typed_scalar_byval_no_alias` prove the same module-global typed scalar bundle stays copy-only when parenthesized or explicitly passed `ByVal`. `params/paramarray_named_fixed_tail_global_fixed_string_alias_copyout` proves the same route for a module-global fixed-length `String * 3` tail under the current Linux ParamArray alias mirror; `params/paramarray_named_fixed_tail_global_fixed_string_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_global_fixed_string_byval_no_alias` prove the same fixed-string global tail stays copy-only when parenthesized or explicitly passed `ByVal`. `params/paramarray_named_fixed_tail_typed_long_string_alias_copyout` proves typed local `Long` and `String` aliases use the named fixed-tail route. `params/paramarray_named_fixed_tail_typed_longptr_alias_copyout`, `params/paramarray_named_fixed_tail_typed_longptr_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_typed_longptr_byval_no_alias` prove the same named-tail route for Win64 `LongPtr` alias and no-alias local tails. `params/paramarray_named_fixed_tail_typed_scalar_alias_bundle_copyout` broadens that typed local alias route across LongLong/Integer/Byte/Boolean/Currency/Single/Double/Date tail elements in one call; `params/paramarray_named_fixed_tail_typed_scalar_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_typed_scalar_byval_no_alias` prove the same non-String typed scalar bundle stays copy-only when parenthesized or explicitly passed `ByVal`; and `params/paramarray_named_fixed_tail_typed_fixed_string_alias_copyout` proves a typed local `String * 3` tail alias uses the same named-tail route. `params/paramarray_named_fixed_tail_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_byval_no_alias` prove the local parenthesized and explicit call-site `ByVal` tail counterparts stay copy-only while the named fixed ByRef slot is still updated; `params/paramarray_named_fixed_tail_global_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_global_byval_no_alias` prove the same copy-only contrast for module-global `Long` tails; `params/paramarray_named_fixed_tail_typed_long_string_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_typed_long_string_byval_no_alias` prove the same copy-only contrast for typed local `Long`/`String` tail pairs; `params/paramarray_named_fixed_tail_typed_fixed_string_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_typed_fixed_string_byval_no_alias` prove the same copy-only contrast for typed local fixed-length `String * 3` tails; and `params/paramarray_named_fixed_tail_global_string_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_global_string_byval_no_alias` prove the same copy-only contrast for module-global `String` tails. The ordinary non-ParamArray positional-after-named diagnostic remains covered separately.

The non-named ParamArray source subset now also includes `params/paramarray_omitted_tail_empty`, covering `Call Capture(lower, upper, emptyType, emptyName, second, , 7)` where the binder packs an omitted variadic slot as `Empty` at `items(0)`, keeps the later literal `7` at `items(1)` with its Integer carrier, and preserves zero-based `LBound`/`UBound` observations `0` and `1`. Named fixed-tail omitted combinations remain outside this Linux slice pending Windows/Excel oracle confirmation.

Direct named binding to the `ParamArray` parameter itself is now covered by `bind_roundtrip::named_argument_to_paramarray_parameter_is_bind_error` and the existing `conformance/tests/params_paramarray_named_error.bas` fixture shape, preserving the diagnostic path for `Call Capture(target := x, items := 5)` as `named argument to a ParamArray parameter`.

The implemented subset now also includes selected typed array `Erase` rows: `arrays/erase_fixed_long_reset`, `arrays/erase_fixed_long_rejects_string_after_reset`, `arrays/erase_fixed_typed_scalar_reset_bundle`, `arrays/erase_fixed_typed_scalar_multidim_reset_bundle`, `arrays/erase_fixed_typed_scalar_3d_reset_bundle`, `arrays/erase_fixed_typed_scalar_4d_reset_bundle`, `arrays/erase_dynamic_{boolean,byte,currency,date,double,integer,long,longlong,single,string}_unallocated_error`, `arrays/erase_dynamic_{boolean,byte,currency,date,double,integer,long,longlong,single,string}_multidim_unallocated_error`, `arrays/erase_dynamic_{boolean,byte,currency,date,double,integer,long,longlong,single,string}_3d_unallocated_error`, and `arrays/erase_dynamic_{boolean,byte,currency,date,double,integer,long,longlong,single,string}_4d_unallocated_error`, backed by the same runtime fixed-vs-dynamic SAFEARRAY dispatch as Variant arrays plus an element-kind reset path that preserves selected fixed one-, two-, three-, and four-dimensional typed storage after `Erase` and deallocates dynamic one-/two-/three-/four-dimensional typed arrays.

The implemented subset now also includes selected typed array read rows: `arrays/long_typed_store_load`, `arrays/long_read_to_variant`, `arrays/string_typed_store_load`, `arrays/fixed_string_typed_store_load`, `arrays/fixed_string_dynamic_typed_store_load`, `arrays/fixed_string_multidim_typed_store_load`, `arrays/fixed_string_dynamic_multidim_typed_store_load`, `arrays/fixed_string_3d_typed_store_load`, `arrays/fixed_string_dynamic_3d_typed_store_load`, `arrays/fixed_string_4d_typed_store_load`, `arrays/fixed_string_dynamic_4d_typed_store_load`, `arrays/typed_scalar_store_load_bundle`, `arrays/typed_scalar_dynamic_store_load_bundle`, `arrays/typed_scalar_multidim_store_load_bundle`, `arrays/typed_scalar_dynamic_multidim_store_load_bundle`, `arrays/typed_scalar_3d_store_load_bundle`, `arrays/typed_scalar_dynamic_3d_store_load_bundle`, `arrays/typed_scalar_4d_store_load_bundle`, and `arrays/typed_scalar_dynamic_4d_store_load_bundle`, backed by widened `ArrayGet` destination admission for matching scalar/String element temps and direct Variant destinations while keeping Variant-held arrays on Variant destinations. The fixed-length String slice covers selected fixed and dynamic one-/two-/three-/four-dimensional `String * 1`/`String * 3`/`String * 5` array `ArraySet`/`ArrayGet` rows with pad/truncate assignments into variable- and fixed-length String destinations.

The implemented subset now also includes selected typed array bounds rows: `arrays/typed_scalar_multidim_bounds_bundle`, `arrays/typed_scalar_dynamic_multidim_bounds_bundle`, `arrays/typed_scalar_multidim_bounds_dim_expr_bundle`, `arrays/typed_scalar_dynamic_multidim_bounds_dim_expr_bundle`, `arrays/typed_scalar_3d_bounds_bundle`, `arrays/typed_scalar_dynamic_3d_bounds_bundle`, `arrays/typed_scalar_4d_bounds_bundle`, `arrays/typed_scalar_dynamic_4d_bounds_bundle`, `arrays/typed_long_multidim_lbound_dim_zero_error`, `arrays/typed_long_multidim_ubound_dim_too_high_error`, `arrays/typed_long_dynamic_multidim_lbound_dim_zero_error`, `arrays/typed_long_dynamic_multidim_ubound_dim_too_high_error`, `arrays/typed_long_multidim_lbound_dim_expr_zero_error`, `arrays/typed_long_multidim_ubound_dim_expr_too_high_error`, `arrays/typed_long_dynamic_multidim_lbound_dim_expr_zero_error`, `arrays/typed_long_dynamic_multidim_ubound_dim_expr_too_high_error`, `arrays/typed_long_3d_lbound_dim_zero_error`, `arrays/typed_long_3d_ubound_dim_too_high_error`, `arrays/typed_long_dynamic_3d_lbound_dim_zero_error`, `arrays/typed_long_dynamic_3d_ubound_dim_too_high_error`, `arrays/typed_long_3d_lbound_dim_expr_zero_error`, `arrays/typed_long_3d_ubound_dim_expr_too_high_error`, `arrays/typed_long_dynamic_3d_lbound_dim_expr_zero_error`, `arrays/typed_long_dynamic_3d_ubound_dim_expr_too_high_error`, `arrays/typed_long_4d_lbound_dim_zero_error`, `arrays/typed_long_4d_ubound_dim_too_high_error`, `arrays/typed_long_dynamic_4d_lbound_dim_zero_error`, `arrays/typed_long_dynamic_4d_ubound_dim_too_high_error`, `arrays/typed_long_4d_lbound_dim_expr_zero_error`, `arrays/typed_long_4d_ubound_dim_expr_too_high_error`, `arrays/typed_long_dynamic_4d_lbound_dim_expr_zero_error`, and `arrays/typed_long_dynamic_4d_ubound_dim_expr_too_high_error`, proving fixed and dynamic two-dimensional `Boolean`, `Byte`, `Integer`, `Long`, `LongLong`, `Single`, `Double`, `Currency`, `Date`, and `String` arrays report both explicit dimensions through the shared `LBound`/`UBound` helper path with fixed dimensions and selected `Long`/`Integer` local dimension expressions, fixed/dynamic three-dimensional typed scalar arrays report dimensions 1 through 3 for the same family set, fixed/dynamic four-dimensional typed scalar arrays report dimensions 1 through 4 for the same family set, and fixed/dynamic `Long` arrays seat runtime error 9 for invalid dimensions 0 and too-high dimensions 3, 4, or 5 across selected two-/three-/four-dimensional constant and local-expression rows.

The implemented subset now also includes selected multi-dimensional `Variant` array rows: `arrays/multidim_indexing`, `arrays/redim_preserve_multidim_last_dimension`, and `arrays/redim_preserve_illegal_non_last_dim_error`, backed by stack-passed bound/subscript vectors, C-order flat indexing, and coordinate-preserving `ReDim Preserve` over dynamic `Variant()` arrays.

Latest fixed-length `String` `For Each` refinement: selected one-, two-, three-, and four-dimensional width-bundle rows `control/for_each_fixed_string_array_fixed_width_bundle`, `control/for_each_fixed_string_array_dynamic_width_bundle`, `control/for_each_fixed_string_array_fixed_multidim_width_bundle`, `control/for_each_fixed_string_array_dynamic_multidim_width_bundle`, `control/for_each_fixed_string_array_fixed_3d_width_bundle`, `control/for_each_fixed_string_array_dynamic_3d_width_bundle`, `control/for_each_fixed_string_array_fixed_4d_width_bundle`, and `control/for_each_fixed_string_array_dynamic_4d_width_bundle`, plus selected two-, three-, and four-dimensional `String * 3` rows `control/for_each_fixed_string_array_fixed_multidim_order`, `control/for_each_fixed_string_array_dynamic_multidim_order`, `control/for_each_fixed_string_array_fixed_3d_order`, `control/for_each_fixed_string_array_dynamic_3d_order`, `control/for_each_fixed_string_array_fixed_4d_order`, and `control/for_each_fixed_string_array_dynamic_4d_order`, are now part of the same SAFEARRAY snapshot subset as the original one-dimensional fixed-string rows. This supersedes earlier one-dimensional-only and `String * 3`-only fixed-length String shorthand in this section; fixed-length `String * N` arrays beyond selected fixed/dynamic one-/two-/three-/four-dimensional `String * 1`/`String * 3`/`String * 5` rows remain open.

Latest fixed-length `String` `For Each` post-completion refinement: selected fixed/dynamic one-, two-, three-, and four-dimensional bundle rows `control/for_each_fixed_string_array_fixed_item_after_completion_bundle`, `control/for_each_fixed_string_array_dynamic_item_after_completion_bundle`, `control/for_each_fixed_string_array_fixed_multidim_item_after_completion_bundle`, `control/for_each_fixed_string_array_dynamic_multidim_item_after_completion_bundle`, `control/for_each_fixed_string_array_fixed_3d_item_after_completion_bundle`, `control/for_each_fixed_string_array_dynamic_3d_item_after_completion_bundle`, `control/for_each_fixed_string_array_fixed_4d_item_after_completion_bundle`, and `control/for_each_fixed_string_array_dynamic_4d_item_after_completion_bundle` now prove post-exhaustion `item` is `Empty` for selected `String * 1`, `String * 3`, and `String * 5` fixed/dynamic array sources across ranks 1 through 4. Read the fixed-length String shorthand as including those selected post-completion bundle rows; broader widths, typed control variables, object `_NewEnum`, Windows/Excel locale confirmation, and real Excel oracle confirmation remain open.

The implemented subset now also includes selected `For Each` rows: `control/for_each_array_literal`, `control/for_each_array_literal_empty_skips`, `control/for_each_array_variable`, `control/for_each_array_variable_explicit_lower_bound`, `control/for_each_array_dynamic`, `control/for_each_array_dynamic_explicit_lower_bound`, `control/for_each_boolean_array_fixed`, `control/for_each_boolean_array_dynamic`, `control/for_each_boolean_array_fixed_multidim_order`, `control/for_each_boolean_array_dynamic_multidim_order`, `control/for_each_long_array_fixed`, `control/for_each_long_array_dynamic`, `control/for_each_long_array_fixed_multidim_order`, `control/for_each_long_array_dynamic_multidim_order`, `control/for_each_long_array_fixed_3d_order`, `control/for_each_long_array_fixed_4d_order`, `control/for_each_long_array_dynamic_3d_order`, `control/for_each_long_array_dynamic_4d_order`, `control/for_each_typed_scalar_3d_order_bundle`, `control/for_each_typed_scalar_dynamic_3d_order_bundle`, `control/for_each_typed_scalar_4d_order_bundle`, `control/for_each_typed_scalar_dynamic_4d_order_bundle`, `control/for_each_longlong_array_fixed`, `control/for_each_longlong_array_dynamic`, `control/for_each_longlong_array_fixed_multidim_order`, `control/for_each_longlong_array_dynamic_multidim_order`, `control/for_each_single_array_fixed`, `control/for_each_single_array_dynamic`, `control/for_each_single_array_fixed_multidim_order`, `control/for_each_single_array_dynamic_multidim_order`, `control/for_each_double_array_fixed`, `control/for_each_double_array_dynamic`, `control/for_each_double_array_fixed_multidim_order`, `control/for_each_double_array_dynamic_multidim_order`, `control/for_each_currency_array_fixed`, `control/for_each_currency_array_dynamic`, `control/for_each_currency_array_fixed_multidim_order`, `control/for_each_currency_array_dynamic_multidim_order`, `control/for_each_date_array_fixed`, `control/for_each_date_array_dynamic`, `control/for_each_date_array_fixed_multidim_order`, `control/for_each_date_array_dynamic_multidim_order`, `control/for_each_string_array_fixed`, `control/for_each_string_array_dynamic`, `control/for_each_string_array_fixed_multidim_order`, `control/for_each_string_array_dynamic_multidim_order`, `control/for_each_string_array_fixed_4d_order`, `control/for_each_string_array_dynamic_4d_order`, `control/for_each_fixed_string_array_fixed`, `control/for_each_fixed_string_array_dynamic`, `control/for_each_fixed_string_array_fixed_width_bundle`, `control/for_each_fixed_string_array_dynamic_width_bundle`, `control/for_each_fixed_string_array_fixed_multidim_width_bundle`, `control/for_each_fixed_string_array_dynamic_multidim_width_bundle`, `control/for_each_fixed_string_array_fixed_multidim_order`, `control/for_each_fixed_string_array_dynamic_multidim_order`, `control/for_each_fixed_string_array_fixed_3d_order`, `control/for_each_fixed_string_array_dynamic_3d_order`, `control/for_each_fixed_string_array_fixed_3d_width_bundle`, `control/for_each_fixed_string_array_dynamic_3d_width_bundle`, `control/for_each_fixed_string_array_fixed_4d_order`, `control/for_each_fixed_string_array_dynamic_4d_order`, `control/for_each_fixed_string_array_fixed_4d_width_bundle`, `control/for_each_fixed_string_array_dynamic_4d_width_bundle`, `control/for_each_integer_array_fixed`, `control/for_each_integer_array_dynamic`, `control/for_each_integer_array_fixed_multidim_order`, `control/for_each_integer_array_dynamic_multidim_order`, `control/for_each_byte_array_fixed`, `control/for_each_byte_array_dynamic`, `control/for_each_byte_array_fixed_multidim_order`, `control/for_each_byte_array_dynamic_multidim_order`, the selected typed `Boolean`/`Long`/`LongLong`/`Single`/`Double`/`Currency`/`Date`/`String`/`Integer`/`Byte` array `*_explicit_lower_bound` rows, and the selected Variant-array plus typed `Boolean`/`Long`/`LongLong`/`Single`/`Double`/`Currency`/`Date`/`String`/`Integer`/`Byte` array `*_item_after_completion` companions are backed by a per-run iterator-state map keyed by the iterator temp slot alias and helper calls that snapshot SAFEARRAY elements at `ForEachInit` and advance/write `item` plus `has_value` at `ForEachNext`, with zero-element literal skip, fixed/dynamic one-dimensional explicit-lower-bound visitation for selected Variant and typed array sources, selected fixed/dynamic one-dimensional typed `Boolean`/`Long`/`LongLong`/`Single`/`Double`/`Currency`/`Date`/`String`/`Integer`/`Byte` array visitation plus selected fixed/dynamic one-dimensional fixed-length `String * 3` basic visitation and selected one-/two-/three-/four-dimensional `String * 1`/`String * 5` width-bundle visitation with pad/truncate scoring, selected fixed/dynamic two-dimensional `Boolean`/`Long`/`String`/`Integer`/`Byte`/`LongLong`/`Single`/`Double`/`Currency`/`Date` and fixed-length `String * 3` array visitation order, selected fixed/dynamic three-dimensional `Long` array visitation order plus selected fixed/dynamic three-dimensional typed scalar/String and fixed-length `String * 3` visitation order plus selected fixed/dynamic four-dimensional `Long`/`String`, fixed-length `String * 3`, and fixed-length `String * 1`/`String * 5` width-bundle array visitation order plus selected fixed/dynamic four-dimensional typed scalar/String bundle visitation order, and source-level loop-variable post-completion `Empty` behavior matching VM3 for the selected Variant and `Boolean`/`Long`/`LongLong`/`Single`/`Double`/`Currency`/`Date`/`String`/`Integer`/`Byte` typed array sources; the current descriptor-supported scalar-source error matrix (`control/for_each_boolean_scalar_error`, `control/for_each_byte_scalar_error`, `control/for_each_currency_scalar_error`, `control/for_each_date_scalar_error`, `control/for_each_double_scalar_error`, `control/for_each_fixed_string_scalar_error`, `control/for_each_integer_scalar_error`, `control/for_each_long_scalar_error`, `control/for_each_longlong_scalar_error`, `control/for_each_single_scalar_error`, `control/for_each_string_scalar_error`, and `control/for_each_variant_scalar_error`) proves the same helper path seats runtime error 13 for Variant-held and explicit scalar sources covered by the existing descriptor operand bridge.

Latest typed scalar `For Each` post-completion refinement: selected fixed/dynamic two-, three-, and four-dimensional typed scalar/String bundle rows `control/for_each_typed_scalar_multidim_item_after_completion_bundle`, `control/for_each_typed_scalar_dynamic_multidim_item_after_completion_bundle`, `control/for_each_typed_scalar_3d_item_after_completion_bundle`, `control/for_each_typed_scalar_dynamic_3d_item_after_completion_bundle`, `control/for_each_typed_scalar_4d_item_after_completion_bundle`, and `control/for_each_typed_scalar_dynamic_4d_item_after_completion_bundle` now prove post-exhaustion `item` is `Empty` for selected `Boolean`, `Byte`, `Integer`, `Long`, `LongLong`, `Single`, `Double`, `Currency`, `Date`, and `String` fixed/dynamic array sources across ranks 2 through 4. Read the preceding post-completion shorthand as including these selected multidimensional bundle sources; broader element families, typed control variables, object `_NewEnum`, Windows/Excel locale confirmation, and real Excel oracle confirmation remain open.

Follow-up in this implemented subset also promotes one-argument `Round(number)` over an explicit `Double` source local plus narrow two-argument `Round(number, digits)` over explicit `Double` and `Long` source locals plus the negative fixed-digits runtime error 5 row, the one-argument `Weekday(Date)` row over an explicit `Date` source local plus exact two-argument `Weekday(date, firstday)` over explicit `Date` and fixed-integer `Long` source locals, one-argument default-long-name `MonthName`/`WeekdayName` rows over fixed-integer source locals, exact optional `MonthName(month, abbreviate)` and `WeekdayName(weekday, abbreviate, firstday)` rows over explicit `Long`/`Boolean`/`Long` source locals, three-argument `DateSerial`/`TimeSerial` rows over fixed-integer source locals plus the `DateSerial(10000, 1, 1)` runtime error 5 row over explicit `Long` source locals, `RGB`/`QBColor` color rows over fixed-integer source locals including high-component RGB clamping, low-blue and mid-gray RGB component rows, `QBColor` palette indices 1, 7, 12, and 15, and the `QBColor(99)` runtime error 5 row, fixed-integer `Error(number)` rows over `Long` source locals for known error 11, unknown error 12345, and invalid error -1, `Len` and `LenB` over a String-bearing `Variant` source local, a pure wide `ChrW`/`AscW` row through a Variant string, `Space` over an explicit fixed-integer source local plus the `Space(-1)` runtime error 5 row, `LCase`/`UCase` over a Variant string created by `ChrW`, `Val` over a Variant populated by `CStr(1234&)`, `Trim`/`LTrim`/`RTrim` over a Variant populated by `Space(3)`, `StrReverse` over a Variant populated by `CStr(1234&)`, `String(count, code)` over fixed-integer locals with `321` wrapping to `A` plus `String(-1, 65)` seating runtime error 5, `Left`/`Right` over a Variant populated by `CStr(...)` plus fixed-integer counts including count-one first/last-character, exact source-length, and zero/overlong count rows in both Left/Right directions, even-byte `LeftB`/`RightB` over the same Variant plus byte count `4`, unit-code-unit byte-count `LeftB`/`RightB` over the same Variant plus byte count `2`, three-code-unit byte-count `LeftB`/`RightB` over the same Variant plus byte count `6`, byte count-edge `LeftB`/`RightB` over that Variant plus byte counts `0` and `20` in both LeftB/RightB directions, and exact byte-length `LeftB`/`RightB` over distinct Variant sources plus byte count `10`, `Mid` over a Variant populated by `CStr(12345&)` plus fixed-integer start with optional count locals including the start-zero, negative-start, and negative-length runtime error 5 rows plus the overlong-start empty-string, overlong-count suffix-truncation, overlong-count middle-truncation, overlong-count full-source, exact last-character, exact full-source-count, exact prefix-count, exact suffix-count, zero-length first/middle/end empty-string, omitted-length full-source value, omitted-length suffix value, and omitted-length overlong-start empty-string rows, `inline/builtin_string_literal_variant_args` for selected direct string-literal operands across `Len`, `UCase`, `Val`, `Trim`, `StrReverse`, `Left`, `Mid`, `InStr`, `StrComp`, `Replace`, and `Like`, `inline/builtin_string_literal_companion_args` for direct string-literal `LenB`, `AscW`, `LTrim`, `RTrim`, `LeftB`, `RightB`, and `InStrRev`, `inline/builtin_string_literal_left_right_count_edges` for direct string-literal `Left`/`Right` code-unit count-one, zero/overlong, and exact-source-length rows, `inline/builtin_string_literal_mid_count_edges` for direct string-literal `Mid` omitted-length full-source/suffix/overlong-start plus zero/exact count rows, `inline/builtin_string_literal_mid_value_edges` for direct string-literal `Mid` overlong-count middle/full-source plus exact last-character/prefix rows, `inline/builtin_string_literal_mid_start_zero`, `inline/builtin_string_literal_mid_negative_start`, and `inline/builtin_string_literal_mid_negative_length` for direct string-literal `Mid` start-zero, negative-start, and negative-length error rows, `inline/builtin_string_literal_leftb_rightb_byte_counts` for direct string-literal `LeftB`/`RightB` byte counts `2` and `6`, `inline/builtin_string_literal_leftb_rightb_byte_count_edges`, `inline/builtin_string_literal_leftb_rightb_complement_byte_count_edges`, and `inline/builtin_string_literal_leftb_rightb_exact_byte_source_count` for direct string-literal zero/overlong and exact-byte-count rows, `inline/builtin_string_literal_leftb_rightb_odd_byte_exprs` for direct string-literal `LeftB`/`RightB` count-1/count-3 odd-byte observables, `inline/builtin_static_string_operands` for representation-preserving `String` source-place assignment plus the same string-oriented operand family, `inline/builtin_static_string_companion_operands` for statically typed `String` source-place `LenB`, `AscW`, `LTrim`, `RTrim`, `LeftB`, `RightB`, and `InStrRev`, `inline/builtin_static_string_left_right_count_edges` for statically typed `String` `Left`/`Right` count-one, zero/overlong, and exact-source-length rows, `inline/builtin_static_string_mid_count_edges` for statically typed `String` `Mid` omitted-length full-source/suffix/overlong-start plus zero/exact count rows, `inline/builtin_static_string_mid_value_edges` for statically typed `String` `Mid` overlong-count middle/full-source plus exact last-character/prefix rows, `inline/builtin_static_string_mid_start_zero`, `inline/builtin_static_string_mid_negative_start`, and `inline/builtin_static_string_mid_negative_length` for statically typed `String` `Mid` start-zero, negative-start, and negative-length error rows, `inline/builtin_static_string_leftb_rightb_byte_counts` for statically typed `String` `LeftB`/`RightB` byte counts `2` and `6`, `inline/builtin_static_string_leftb_rightb_byte_count_edges`, `inline/builtin_static_string_leftb_rightb_complement_byte_count_edges`, and `inline/builtin_static_string_leftb_rightb_exact_byte_source_count` for static `String` zero/overlong and exact-byte-count rows, `inline/builtin_static_string_left_negative_count`, `inline/builtin_static_string_right_negative_count`, `inline/builtin_static_string_leftb_negative_count`, and `inline/builtin_static_string_rightb_negative_count` for static `String` `Left`/`Right`/`LeftB`/`RightB` negative-count error rows, `inline/builtin_static_string_optional_args` for statically typed `String` source-place optional `InStr`/`InStrRev`/`StrComp`/`Replace` forms, `inline/builtin_string_result_destinations` for selected direct library results assigned to `String` globals (`CStr`, `Hex`, `ChrW`, `Space`, `Left`, `Mid`, and `Replace`), and `inline/string_destination_null_variant` for `Null` Variant assignment to a `String` destination seating runtime error 94; broader optional decimal-place beyond the promoted positive and negative fixed-digits rows, omitted or non-fixed-integer decimal-place, `Weekday` optional forms beyond exact `Weekday(date, firstday)`, omitted or non-fixed-integer `Weekday` firstday arguments, date-name optional shapes beyond the exact promoted two-/three-argument rows, omitted date-name optionals, `WeekdayName` two-argument forms, non-Boolean abbreviation operands, non-fixed-integer firstday operands, firstday/calendar arguments outside the promoted rows, non-Date or non-fixed-integer/Variant/string/numeric/floating carriers, zero-argument `Error()`/`Error$()`, `Error`/`Error$` String-destination shapes beyond the promoted fixed-integer known/unknown/invalid rows, non-fixed-integer Error operands, other Error message/catalog edge rows, ANSI `Chr`/`Asc`, `$` aliases and `String` destination shapes beyond the promoted `ChrW`/`Space`/case/trim rows, `$` aliases and `String` destination shapes beyond the promoted unsuffixed `Left`/`Mid`/`Replace` destination rows, string character arguments for `String`, statically typed `String` operands outside the promoted representation-preserving bridge, direct string constants outside the promoted string-oriented Variant operand gates, `Left`/`Right` Null dynamic payload rows beyond the promoted Null slice row plus Empty dynamic payload rows beyond the promoted Empty slice row plus non-String dynamic payload rows beyond the promoted numeric Long, Integer, LongLong, Byte, Currency, Date, Error, Decimal, Boolean, Double, and Single slice rows, `Left`/`Right` negative count rows beyond promoted Variant-source, direct-literal, and static-`String` `-1` rows plus UTF-16 code-unit edge rows, `LeftB`/`RightB` odd-byte rows beyond the promoted static `ABC`, direct literal `ABC`, and Variant source-place `ABC` count-1/count-3 observables and broader byte-slice rows beyond promoted Variant-source, direct-literal, and static-`String` count `0`/`2`/`4`/`6`/`10`/`20` rows, `Mid` statement form, `Mid` dynamic payload and remaining start/count rows beyond promoted start-zero, negative-start, negative-length, overlong-start, overlong-count suffix/middle/full-source, exact last-character, exact full-source-count, exact prefix-count, exact suffix-count, zero-length first/middle/end, omitted-length suffix/full-source/overlong-start, string-literal-Mid-count/value-edge and start-zero/negative-start/negative-length error rows, static-String-Mid-count/value-edge and start-zero/negative-start/negative-length error rows, and code-unit edge rows, scalar library-result destinations beyond the promoted `Len`/`LenB`/`InStr`/`Val`/`IsNull` row, `LenB` scalar storage-width and record-memory rows, `Space` negative count rows beyond promoted `-1`, `Space` large-allocation edge rows, `String` negative count rows beyond promoted `-1`, and `String` large-allocation edge rows, Null dynamic payload rows beyond the promoted Null slice row and beyond the promoted `String` destination error row, Empty dynamic payload rows beyond the promoted Empty slice row, non-String dynamic payload rows beyond the promoted numeric Long, Integer, LongLong, Byte, Currency, Date, Error, Decimal, Boolean, Double, and Single slice rows, locale/non-ASCII case-folding rows, broader `Val` parser edge rows, Trim-family mixed leading/trailing non-space and Unicode whitespace/tab rows, `StrReverse` Unicode combining/surrogate/code-unit edge rows, empty/surrogate/wrap/out-of-range wide-character edge rows, remaining date/time serial range/error edge rows beyond the promoted `DateSerial(10000, 1, 1)` row, remaining QBColor palette indices beyond 1, 7, 12, and 15, negative RGB component edges and broader RGB component combinations beyond high-clamp/low-blue/mid-gray, and Windows/Excel locale confirmation remain outside the current Linux evidence.

The selected scalar library-result destination evidence now also promotes `inline/builtin_scalar_result_destination_families`, `inline/builtin_scalar_result_destination_coercions`, `inline/builtin_scalar_result_destination_coercion_overflow`, and `inline/builtin_scalar_result_destination_null_error`, covering `Round`, `Year`, `DateValue`, `TimeValue`, `RGB`, `QBColor`, and `IsDate` results assigned to `Double`, `Long`, `Date`, and `Boolean` globals while preserving explicit source locals, selected cross-scalar/narrowing assignments from `Len`, `InStr`, `LenB`, and `Val` results into `Byte`, `Integer`, `Single`, `Double`, `Currency`, and `Boolean` globals, `Len` result overflow into a `Byte` global seating runtime error 6, and `Abs(Null)` into a `Long` global seating runtime error 94. Within the long boundary above, scalar library-result destination exclusions now mean rows beyond `inline/builtin_scalar_result_destinations`, `inline/builtin_scalar_result_destination_families`, `inline/builtin_scalar_result_destination_coercions`, `inline/builtin_scalar_result_destination_coercion_overflow`, and `inline/builtin_scalar_result_destination_null_error`.

The direct/static `Mid` boundary value evidence also promotes `inline/builtin_string_literal_mid_boundary_value_edges` and `inline/builtin_static_string_mid_boundary_value_edges`, covering counted overlong-start and zero-length first/last-position empty-string results through the same descriptor string-source/fixed start/count gates. Within the boundary above, `Mid` exclusions now mean rows beyond the promoted Variant-source, direct-literal, and static `String` count/value/boundary/error rows.

The Variant source-place odd-byte byte-slice evidence also promotes `inline/builtin_leftb_rightb_variant_odd_byte_exprs`, covering `text As Variant` assigned `"ABC"` and then passed to `LeftB(text, 1)`, `LeftB(text, 3)`, `RightB(text, 1)`, and `RightB(text, 3)`. This row is enabled by the narrow `Box` gate accepting `String` sources into `Variant` slots before the existing descriptor-supported string-source/fixed-count library path, and it does not claim broader `String` slot ABI behavior. Within the boundary above, odd-byte exclusions now mean rows beyond the promoted static-`String` `ABC`, direct-literal `ABC`, and `Variant` source-place `ABC` count-1/count-3 observables.

The static `String` negative-count slice evidence also promotes `inline/builtin_static_string_left_negative_count`, `inline/builtin_static_string_right_negative_count`, `inline/builtin_static_string_leftb_negative_count`, and `inline/builtin_static_string_rightb_negative_count`, covering a `String` source-place set to `"12345"` plus an explicit `Long` count local set to `-1`. These rows prove shared runtime error 5 seating for `Left`, `Right`, `LeftB`, and `RightB` through the representation-preserving `String` source-place bridge plus fixed-count gates without widening the lowerer. Within the negative-count boundary above, exclusions now mean carriers beyond the promoted Variant-source `Long -1`, direct-literal `Long -1`, and static-`String` `Long -1` rows.

The selected `String` destination evidence also promotes `inline/builtin_string_result_destinations` and `inline/string_destination_null_variant`, covering direct library-result Variant temps from `CStr`, `Hex`, `ChrW`, `Space`, `Left`, `Mid`, and `Replace` assigned to `String` globals plus `Null` Variant assignment to a `String` destination seating runtime error 94. These rows prove the current Variant-to-String coercion slot helper path only; fixed-length `String` storage is promoted separately below, while bare BSTR ABI and broader `String` destination shapes remain outside this Linux slice.

The fixed-length scalar `String` storage evidence promotes `inline/fixed_string_local_pad_truncate`, `inline/fixed_string_local_null_error`, `inline/fixed_string_global_pad_truncate`, and `inline/fixed_string_global_null_error`, covering `Dim fixed As String * 3` local default initialization, module-level `Public fixed As String * 3` default initialization, pad/truncate assignment behavior, and local/global `Null` assignment seating runtime error 94 through the new `FixedStr` coercion slot helper. These rows prove `OxTy::FixedStr` locals/globals can be admitted as BSTR-backed slots with VBA-style fixed-length pad/truncate at assignment time; fixed-string temps, UDT fixed-string fields, `LSet`/`RSet`, file I/O raw fixed-string layout, fixed-string parameters/returns/ByRef, bare BSTR typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence promotes `calls/string_byval_return`, `calls/string_byval_local_return`, `calls/string_byref`, `calls/string_mixed_byref_byval`, `calls/string_named_byval_return`, `calls/string_named_byval_numeric_coerce_return`, `calls/string_named_byval_cdate_month_name_literal_coerce_return`, `calls/string_named_byval_cdate_month_name_local_coerce_return`, `calls/string_named_mixed_byref_byval`, `calls/string_named_mixed_byref_byval_numeric_coerce`, `calls/string_byval_numeric_coerce_return`, `calls/string_byval_long_local_coerce_return`, `calls/string_byval_boolean_local_coerce_return`, `calls/string_byval_double_local_coerce_return`, `calls/string_byval_single_local_coerce_return`, `calls/string_byval_currency_local_coerce_return`, `calls/string_byval_integer_local_coerce_return`, `calls/string_byval_byte_local_coerce_return`, `calls/string_byval_longlong_local_coerce_return`, `calls/string_byval_date_local_coerce_return`, `calls/string_byval_cdate_month_name_literal_coerce_return`, `calls/string_byval_cdate_month_name_local_coerce_return`, `calls/string_byval_variant_local_coerce_return`, `calls/string_byval_variant_boolean_local_coerce_return`, `calls/string_byval_variant_double_local_coerce_return`, `calls/string_byval_variant_single_local_coerce_return`, `calls/string_byval_variant_currency_local_coerce_return`, `calls/string_byval_variant_integer_local_coerce_return`, `calls/string_byval_variant_byte_local_coerce_return`, `calls/string_byval_variant_longlong_local_coerce_return`, `calls/string_byval_variant_date_local_coerce_return`, `calls/string_byval_variant_string_local_coerce_return`, `calls/string_byval_variant_error_local_coerce_return`, `calls/string_byval_variant_decimal_local_coerce_return`, `calls/string_byval_variant_empty_local_coerce_return`, and `calls/string_byval_variant_null_local_coerce_error`, covering a `ByVal text As String` function returning a direct literal String argument and a String local argument into a `String` global, an exact `ByRef text As String` callee mutating a caller `String` local, a mixed `ByRef text As String, ByVal value As String` call writing the ByVal `"beta"` argument back through the ByRef alias, an out-of-order named `PickText(second:="beta", first:="alpha")` call returning the second formal parameter, an out-of-order named `PickText(second:=42&, first:="alpha")` call returning `"42"` through binder-resolved named ordering plus numeric-to-String ByVal coercion, out-of-order named `PickText(second:=CDate("February 28, 2026"), first:="alpha")` and local `PickText(second:=CDate(text), first:="alpha")` calls returning `"2/28/2026"` through the same named ordering plus month-name `CDate` result-to-String ByVal coercion, an out-of-order named `ReplaceText(value:="beta", text:=text)` call proving exact String ByRef aliasing plus ByVal carrier ordering, an out-of-order named `ReplaceText(value:=42&, text:=text)` call writing `"42"` through the ByRef `String` alias, and `Long` literal/local, `Boolean`/`Double`/`Single`/`Currency`/`Integer`/`Byte`/`LongLong`/`Date` local arguments, month-name `CDate` literal/local results, descriptor-backed `Variant` locals carrying Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, and Decimal payloads, plus Empty and Null Variant edge rows, to `ByVal text As String` returning `"42"`/`"43"`/`"True"`/`"12.5"`/`"12.5"`/`"12.3456"`/`"44"`/`"7"`/`"5000000012"`/`"1/15/2020"`/`"2/28/2026"`/`"2/28/2026"`/`"45"`/`"True"`/`"12.5"`/`"12.5"`/`"12.3456"`/`"44"`/`"7"`/`"5000000012"`/`"1/15/2020"`/`"alpha"`/`"Error 1234"`/`"12345"`, returning an empty string for Empty, and seating runtime error 94 for Null through the widened descriptor-backed `Coerce(Str)` helper path. These rows prove only the helper-mediated call descriptor path for exact String carriers, binder-resolved named ByVal and promoted mixed ByRef/ByVal `String` argument ordering, promoted named numeric and month-name `CDate` ByVal-to-String coercion before the helper-mediated static call descriptor, and the promoted Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong/Date source-to-String ByVal coercion rows plus month-name `CDate` literal/local result rows plus Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong/Date/String/Error/Decimal-payload Variant source rows plus Empty/Null Variant edge rows under current Linux VM3 runtime formatting; fixed-length strings, broader non-String-to-String parameter coercions beyond those source/CDate rows and edge rows, broader Variant payloads, broader named forms beyond the promoted exact, numeric ByVal/mixed, Date literal/local ByVal/mixed, and month-name `CDate` ByVal/mixed rows, ParamArray/named args beyond the promoted rows, bare BSTR typed primary entries, dynamic-entry thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_mixed_byref_byval_numeric_coerce`, covering positional `ReplaceText(text, 42&)` into `Sub ReplaceText(ByRef text As String, ByVal value As String)`. This row proves positional Long-to-String ByVal coercion can write `"42"` through the ByRef `String` alias before the helper-mediated static call returns; broader positional mixed non-String-to-String parameter coercions beyond the promoted numeric, Date, DateSerial, and numeric `CDate` rows, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_mixed_byref_byval_long_local_coerce`, `calls/string_mixed_byref_byval_boolean_local_coerce`, `calls/string_mixed_byref_byval_double_local_coerce`, `calls/string_mixed_byref_byval_single_local_coerce`, `calls/string_mixed_byref_byval_currency_local_coerce`, `calls/string_mixed_byref_byval_integer_local_coerce`, `calls/string_mixed_byref_byval_byte_local_coerce`, and `calls/string_mixed_byref_byval_longlong_local_coerce`, covering positional `ReplaceText(text, n)`/`ReplaceText(text, flag)` calls where the supplied ByVal `String` value comes from explicit `Long`, `Boolean`, `Double`, `Single`, `Currency`, `Integer`, `Byte`, and `LongLong` locals. These rows prove positional scalar-local-to-String ByVal coercion can write `"43"`, `"True"`, `"12.5"`, `"12.5"`, `"12.3456"`, `"44"`, `"7"`, and `"5000000012"` through the ByRef `String` alias before the helper-mediated static call returns; broader positional mixed non-String-to-String parameter coercions beyond the promoted scalar-local, Date, DateSerial, numeric `CDate`, valid string `CDate`, and invalid string `CDate` rows, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_mixed_byref_byval_variant_local_coerce`, `calls/string_mixed_byref_byval_variant_boolean_local_coerce`, `calls/string_mixed_byref_byval_variant_double_local_coerce`, `calls/string_mixed_byref_byval_variant_single_local_coerce`, `calls/string_mixed_byref_byval_variant_currency_local_coerce`, `calls/string_mixed_byref_byval_variant_integer_local_coerce`, `calls/string_mixed_byref_byval_variant_byte_local_coerce`, `calls/string_mixed_byref_byval_variant_longlong_local_coerce`, `calls/string_mixed_byref_byval_variant_date_local_coerce`, `calls/string_mixed_byref_byval_variant_string_local_coerce`, `calls/string_mixed_byref_byval_variant_error_local_coerce`, `calls/string_mixed_byref_byval_variant_decimal_local_coerce`, `calls/string_mixed_byref_byval_variant_empty_local_coerce`, and `calls/string_mixed_byref_byval_variant_null_local_coerce_error`, covering positional `ReplaceText(text, v)` calls where the supplied ByVal `String` value comes from descriptor-backed `Variant` locals carrying Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads. These rows prove positional Variant-payload-to-String ByVal coercion can write `"45"`, `"True"`, `"12.5"`, `"12.5"`, `"12.3456"`, `"44"`, `"7"`, `"5000000012"`, `"1/15/2020"`, `"alpha"`, `"Error 1234"`, `"12345"`, and `""` through the ByRef `String` alias, while the Null payload seats runtime error 94 before alias mutation; broader positional mixed Variant payloads beyond the promoted Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong/Date/String/Error/Decimal/Empty/Null rows, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_named_mixed_byref_byval_long_local_coerce`, `calls/string_named_mixed_byref_byval_boolean_local_coerce`, `calls/string_named_mixed_byref_byval_double_local_coerce`, `calls/string_named_mixed_byref_byval_single_local_coerce`, `calls/string_named_mixed_byref_byval_currency_local_coerce`, `calls/string_named_mixed_byref_byval_integer_local_coerce`, `calls/string_named_mixed_byref_byval_byte_local_coerce`, and `calls/string_named_mixed_byref_byval_longlong_local_coerce`, covering out-of-order named `ReplaceText(value:=n, text:=text)`/`ReplaceText(value:=flag, text:=text)` calls where the supplied ByVal `String` value comes from explicit `Long`, `Boolean`, `Double`, `Single`, `Currency`, `Integer`, `Byte`, and `LongLong` locals. These rows prove binder-resolved named ordering plus scalar-local-to-String ByVal coercion can write `"43"`, `"True"`, `"12.5"`, `"12.5"`, `"12.3456"`, `"44"`, `"7"`, and `"5000000012"` through the ByRef `String` alias before the helper-mediated static call returns; broader named mixed non-String-to-String parameter coercions beyond the promoted scalar-local, Date, DateSerial, numeric `CDate`, valid string `CDate`, and invalid string `CDate` rows, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_named_mixed_byref_byval_variant_local_coerce`, `calls/string_named_mixed_byref_byval_variant_boolean_local_coerce`, `calls/string_named_mixed_byref_byval_variant_double_local_coerce`, `calls/string_named_mixed_byref_byval_variant_single_local_coerce`, `calls/string_named_mixed_byref_byval_variant_currency_local_coerce`, `calls/string_named_mixed_byref_byval_variant_integer_local_coerce`, `calls/string_named_mixed_byref_byval_variant_byte_local_coerce`, `calls/string_named_mixed_byref_byval_variant_longlong_local_coerce`, `calls/string_named_mixed_byref_byval_variant_date_local_coerce`, `calls/string_named_mixed_byref_byval_variant_string_local_coerce`, `calls/string_named_mixed_byref_byval_variant_error_local_coerce`, `calls/string_named_mixed_byref_byval_variant_decimal_local_coerce`, `calls/string_named_mixed_byref_byval_variant_empty_local_coerce`, and `calls/string_named_mixed_byref_byval_variant_null_local_coerce_error`, covering out-of-order named `ReplaceText(value:=v, text:=text)` calls where the supplied ByVal `String` value comes from descriptor-backed `Variant` locals carrying Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads. These rows prove binder-resolved named ordering plus Variant-payload-to-String ByVal coercion can write `"45"`, `"True"`, `"12.5"`, `"12.5"`, `"12.3456"`, `"44"`, `"7"`, `"5000000012"`, `"1/15/2020"`, `"alpha"`, `"Error 1234"`, `"12345"`, and `""` through the ByRef `String` alias, while the Null payload seats runtime error 94 before alias mutation; broader named mixed Variant payloads beyond the promoted Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong/Date/String/Error/Decimal/Empty/Null rows, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_named_byval_long_local_coerce_return`, `calls/string_named_byval_boolean_local_coerce_return`, `calls/string_named_byval_double_local_coerce_return`, `calls/string_named_byval_single_local_coerce_return`, `calls/string_named_byval_currency_local_coerce_return`, `calls/string_named_byval_integer_local_coerce_return`, `calls/string_named_byval_byte_local_coerce_return`, and `calls/string_named_byval_longlong_local_coerce_return`, covering out-of-order named `PickText(second:=n, first:="alpha")`/`PickText(second:=flag, first:="alpha")` calls where the returned ByVal `String` value comes from explicit `Long`, `Boolean`, `Double`, `Single`, `Currency`, `Integer`, `Byte`, and `LongLong` locals. These rows prove binder-resolved named ordering plus scalar-local-to-String ByVal coercion can return `"43"`, `"True"`, `"12.5"`, `"12.5"`, `"12.3456"`, `"44"`, `"7"`, and `"5000000012"` through the helper-mediated static call descriptor; broader named pure ByVal non-String-to-String parameter coercions beyond the promoted scalar-local, Date, DateSerial, numeric `CDate`, valid string `CDate`, and invalid string `CDate` rows, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_named_byval_variant_local_coerce_return`, `calls/string_named_byval_variant_boolean_local_coerce_return`, `calls/string_named_byval_variant_double_local_coerce_return`, `calls/string_named_byval_variant_single_local_coerce_return`, `calls/string_named_byval_variant_currency_local_coerce_return`, `calls/string_named_byval_variant_integer_local_coerce_return`, `calls/string_named_byval_variant_byte_local_coerce_return`, `calls/string_named_byval_variant_longlong_local_coerce_return`, `calls/string_named_byval_variant_date_local_coerce_return`, `calls/string_named_byval_variant_string_local_coerce_return`, `calls/string_named_byval_variant_error_local_coerce_return`, `calls/string_named_byval_variant_decimal_local_coerce_return`, `calls/string_named_byval_variant_empty_local_coerce_return`, and `calls/string_named_byval_variant_null_local_coerce_error`, covering out-of-order named `PickText(second:=v, first:="alpha")` calls where the supplied ByVal `String` value comes from descriptor-backed `Variant` locals carrying Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads. These rows prove binder-resolved named ordering composes with Variant-payload-to-String ByVal coercion, returning `"45"`, `"True"`, `"12.5"`, `"12.5"`, `"12.3456"`, `"44"`, `"7"`, `"5000000012"`, `"1/15/2020"`, `"alpha"`, `"Error 1234"`, `"12345"`, and `""`, while the Null payload seats runtime error 94; broader named pure ByVal Variant payloads beyond the promoted Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong/Date/String/Error/Decimal/Empty/Null rows, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_mixed_byref_byval_dateserial_coerce` and `calls/string_mixed_byref_byval_cdate_numeric_coerce`, covering positional `ReplaceText(text, DateSerial(2020, 1, 15))` and `ReplaceText(text, CDate(43845#))` calls. These rows prove Date-producing library/conversion results can flow through positional ByVal `String` coercion and write `"1/15/2020"` through the ByRef `String` alias before the helper-mediated static call returns; broader positional mixed non-String-to-String parameter coercions beyond the promoted numeric, Date, DateSerial, numeric `CDate`, and month-name `CDate` rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_mixed_byref_byval_cdate_month_name_literal_coerce` and `calls/string_mixed_byref_byval_cdate_month_name_local_coerce`, covering positional `ReplaceText(text, CDate("February 28, 2026"))` and `ReplaceText(text, CDate(dateText))` calls. These rows prove positional month-name `CDate` result-to-String ByVal coercion can write `"2/28/2026"` through the ByRef `String` alias before the helper-mediated static call returns; broader positional mixed non-String-to-String parameter coercions beyond the promoted numeric, Date, DateSerial, numeric `CDate`, and month-name `CDate` rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is extended by `calls/string_named_mixed_byref_byval_cdate_month_name_literal_coerce` and `calls/string_named_mixed_byref_byval_cdate_month_name_local_coerce`, covering out-of-order named `ReplaceText(value:=CDate("February 28, 2026"), text:=text)` and `ReplaceText(value:=CDate(dateText), text:=text)` calls into `Sub ReplaceText(ByRef text As String, ByVal value As String)`. These rows prove binder-resolved named ordering plus month-name `CDate` result-to-String ByVal coercion can write `"2/28/2026"` back through the ByRef `String` alias before the helper-mediated static call returns; broader named forms beyond the promoted exact, numeric ByVal/mixed, Date literal/local ByVal/mixed, and month-name `CDate` ByVal/mixed rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_named_byval_date_literal_coerce_return`, `calls/string_named_byval_date_local_coerce_return`, `calls/string_named_mixed_byref_byval_date_literal_coerce`, and `calls/string_named_mixed_byref_byval_date_local_coerce`, covering out-of-order named `PickText(second:=#2020-01-15#, first:="alpha")`, `PickText(second:=d, first:="alpha")`, `ReplaceText(value:=#2020-01-15#, text:=text)`, and `ReplaceText(value:=d, text:=text)` calls. These rows prove binder-resolved named ordering plus Date-to-String ByVal coercion returns or writes `"1/15/2020"` through the helper-mediated static call descriptor under current Linux VM3 formatting; broader named forms beyond the promoted exact, numeric ByVal/mixed, Date literal/local ByVal/mixed, and month-name `CDate` ByVal/mixed rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_named_byval_dateserial_coerce_return`, `calls/string_named_byval_cdate_numeric_coerce_return`, `calls/string_named_mixed_byref_byval_dateserial_coerce`, and `calls/string_named_mixed_byref_byval_cdate_numeric_coerce`, covering out-of-order named `PickText(second:=DateSerial(2020, 1, 15), first:="alpha")`, `PickText(second:=CDate(43845#), first:="alpha")`, `ReplaceText(value:=DateSerial(2020, 1, 15), text:=text)`, and `ReplaceText(value:=CDate(43845#), text:=text)` calls. These rows prove binder-resolved named ordering plus Date-producing library/conversion result-to-String ByVal coercion returns or writes `"1/15/2020"` through the helper-mediated static call descriptor under current Linux VM3 formatting; broader named Date-producing expressions beyond the promoted Date literal/local, DateSerial, numeric `CDate`, and month-name `CDate` ByVal/mixed rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_named_byval_cdate_string_literal_coerce_return`, `calls/string_named_byval_cdate_string_local_coerce_return`, `calls/string_named_mixed_byref_byval_cdate_string_literal_coerce`, and `calls/string_named_mixed_byref_byval_cdate_string_local_coerce`, covering out-of-order named `PickText(second:=CDate("2020-01-15"), first:="alpha")`, `PickText(second:=CDate(text), first:="alpha")` with `text = "2020-01-15"`, `ReplaceText(value:=CDate("2020-01-15"), text:=text)`, and `ReplaceText(value:=CDate(dateText), text:=text)` with `dateText = "2020-01-15"` calls. These rows prove binder-resolved named ordering plus valid ISO string-literal and string-local `CDate` result-to-String ByVal coercion returns or writes `"1/15/2020"` through the helper-mediated static call descriptor under current Linux VM3 formatting; broader string parsing shapes beyond promoted ISO and month-name valid literal/local rows plus invalid literal/local error rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_named_byval_cdate_invalid_string_literal_error`, `calls/string_named_byval_cdate_invalid_string_local_error`, `calls/string_named_mixed_byref_byval_cdate_invalid_string_literal_error`, and `calls/string_named_mixed_byref_byval_cdate_invalid_string_local_error`, covering out-of-order named `PickText(second:=CDate("not-a-date"), first:="alpha")`, `PickText(second:=CDate(text), first:="alpha")` with `text = "not-a-date"`, `ReplaceText(value:=CDate("not-a-date"), text:=text)`, and `ReplaceText(value:=CDate(dateText), text:=text)` with `dateText = "not-a-date"` calls. These rows prove binder-resolved named ordering still seats runtime error 13 for invalid string-literal and string-local `CDate` operands before exact `String` ByVal return or ByRef mutation can complete under current Linux VM3/JIT parity; broader string parsing shapes beyond promoted ISO and month-name valid literal/local rows plus invalid literal/local error rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_byval_date_literal_coerce_return`, `calls/string_mixed_byref_byval_date_literal_coerce`, and `calls/string_mixed_byref_byval_date_local_coerce`, covering positional `EchoText(#2020-01-15#)`, `ReplaceText(text, #2020-01-15#)`, and `ReplaceText(text, d)` calls. These rows prove Date-to-String ByVal coercion for positional returned and ByRef-mutating exact `String` calls under current Linux VM3 formatting; broader non-String-to-String parameter coercions beyond the promoted scalar/source rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_byval_dateserial_coerce_return` and `calls/string_byval_cdate_numeric_coerce_return`, covering positional `EchoText(DateSerial(2020, 1, 15))` and `EchoText(CDate(43845#))` calls. These rows prove Date-producing library/conversion results can flow through a pure `ByVal text As String` function return path and return `"1/15/2020"` before the helper-mediated static call returns; broader positional ByVal Date-producing expressions beyond the promoted Date literal/local, DateSerial, numeric `CDate`, and month-name `CDate` rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_byval_cdate_string_literal_coerce_return`, `calls/string_byval_cdate_string_local_coerce_return`, `calls/string_mixed_byref_byval_cdate_string_literal_coerce`, and `calls/string_mixed_byref_byval_cdate_string_local_coerce`, covering positional `EchoText(CDate("2020-01-15"))`, `EchoText(CDate(text))` with `text = "2020-01-15"`, `ReplaceText(text, CDate("2020-01-15"))`, and `ReplaceText(text, CDate(dateText))` with `dateText = "2020-01-15"` calls. These rows prove valid ISO string-literal and string-local `CDate` results flow through positional exact `String` ByVal coercion and either return or write `"1/15/2020"` before the helper-mediated static call returns; broader string parsing shapes beyond promoted ISO and month-name valid literal/local rows plus invalid literal/local error rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The exact `String` procedure-call evidence is also extended by `calls/string_byval_cdate_invalid_string_literal_error`, `calls/string_byval_cdate_invalid_string_local_error`, `calls/string_mixed_byref_byval_cdate_invalid_string_literal_error`, and `calls/string_mixed_byref_byval_cdate_invalid_string_local_error`, covering positional `EchoText(CDate("not-a-date"))`, `EchoText(CDate(text))` with `text = "not-a-date"`, `ReplaceText(text, CDate("not-a-date"))`, and `ReplaceText(text, CDate(dateText))` with `dateText = "not-a-date"` calls. These rows prove invalid string-literal and string-local `CDate` operands seat runtime error 13 before exact `String` ByVal return or ByRef mutation can complete under current Linux VM3/JIT parity; broader string parsing shapes beyond promoted ISO and month-name valid literal/local rows plus invalid literal/local error rows, slash-date/locale-sensitive parsing, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The selected procedure-call return evidence promotes `calls/long_return_to_variant`, `calls/string_return_to_variant`, `calls/scalar_returns_to_variant`, `calls/variant_return_to_long`, `calls/variant_return_to_string_coerce`, `calls/variant_return_to_bool_coerce`, `calls/variant_return_to_double`, `calls/variant_return_to_string_boolean_payload`, `calls/variant_return_to_string_double_payload`, `calls/variant_return_to_string_single_payload`, `calls/variant_return_to_string_currency_payload`, `calls/variant_return_to_string_integer_payload`, `calls/variant_return_to_string_byte_payload`, `calls/variant_return_to_string_longlong_payload`, `calls/variant_return_to_string_string_payload`, `calls/variant_return_to_string_date_payload`, `calls/variant_return_to_string_error_payload`, `calls/variant_return_to_string_decimal_payload`, `calls/variant_return_empty_to_string`, `calls/variant_return_null_to_long_error`, and `calls/variant_return_null_to_string_error`, covering helper-mediated boxed copy-out of `Long`, `String`, `Bool`, `Byte`, `Integer`, `LongLong`, `Single`, `Double`, `Currency`, and `Date` function returns into `Variant` globals plus source-level `Function Give() As Variant` return values carrying `42&`, `2&`, `12.5#`, `12.5!`, `12.3456@`, `44%`, a `Byte` local set to `7`, `5000000012^`, `True`, `"alpha"`, `#2020-01-15#`, `CVErr(1234)`, `CDec(12345)`, default `Empty`, and `Null` assigned into `Long`, `Boolean`, `Double`, and `String` globals through the binder-lowered Variant temp plus destination coercion path. These rows prove boxed Variant destination copy-out for typed returns and the promoted Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong/String/Date/Error/Decimal/Empty/Null-payload Variant return-to-selected Long/Boolean/Double/String coercion and error-seating shapes under current Linux VM3 runtime formatting only; broader Variant return payloads/destinations, broad cross-type return coercions, Object/Array/Record returns, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and Excel oracle confirmation remain outside this Linux slice.

The positional supplied Optional scalar procedure-call evidence promotes `calls/optional_variant_explicit_local`, `calls/optional_long_explicit_local`, `calls/optional_double_explicit_local`, `calls/optional_currency_explicit_local`, `calls/optional_bool_explicit_local`, `calls/optional_byte_explicit_local`, `calls/optional_integer_explicit_local`, `calls/optional_longlong_explicit_local`, `calls/optional_single_explicit_local`, and `calls/optional_date_explicit_local`, covering explicit local arguments supplied to Optional ByVal `Variant`, `Long`, `Double`, `Currency`, `Boolean`, `Byte`, `Integer`, `LongLong`, `Single`, and `Date` parameters. These rows prove the helper-mediated static call descriptor uses supplied local arguments instead of binder-materialized defaults or no-default omitted values, returning `13`, `13`, `7.25`, `3.75@`, `False`, `9`, `34`, `5000000013^`, `2.5!`, and `#2000-01-03#`; supplied scalar coercion/error forms beyond the selected positional rows below, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The named supplied Optional scalar procedure-call evidence promotes `calls/optional_variant_named_explicit_local`, `calls/optional_long_named_explicit_local`, `calls/optional_double_named_explicit_local`, `calls/optional_currency_named_explicit_local`, `calls/optional_bool_named_explicit_local`, `calls/optional_byte_named_explicit_local`, `calls/optional_integer_named_explicit_local`, `calls/optional_longlong_named_explicit_local`, `calls/optional_single_named_explicit_local`, and `calls/optional_date_named_explicit_local`, covering explicit local arguments supplied by name to Optional ByVal `Variant`, `Long`, `Double`, `Currency`, `Boolean`, `Byte`, `Integer`, `LongLong`, `Single`, and `Date` parameters. The `AddOpt` rows intentionally use out-of-order `bonus:=...` before `n:=...`, while the single-Optional rows use `value:=actual`; they return `13`, `13`, `7.25`, `3.75@`, `False`, `9`, `34`, `5000000013^`, `2.5!`, and `#2000-01-03#`, proving binder-resolved named argument ordering composes with supplied Optional scalar locals before the helper-mediated static call descriptor. Additional named supplied scalar coercion/error forms are covered separately below, while explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The selected supplied Optional scalar coercion/error evidence promotes `calls/optional_long_explicit_double_coerce`, `calls/optional_double_explicit_long_coerce`, `calls/optional_currency_explicit_double_coerce`, `calls/optional_bool_explicit_long_coerce`, `calls/optional_byte_explicit_integer_coerce`, `calls/optional_integer_explicit_long_coerce`, `calls/optional_longlong_explicit_long_coerce`, `calls/optional_single_explicit_double_coerce`, `calls/optional_date_explicit_double_coerce`, and `calls/optional_long_explicit_null_coerce_error`, covering positional supplied locals whose static type differs from the Optional ByVal scalar target plus a direct `Null` error row. These rows prove scalar-to-scalar `Coerce` operands that the prior direct same-representation lowering rejected now route through the shared numeric Variant-helper descriptor path when needed, while exact/widening direct lowering remains unchanged for already-supported sources. They return `8`, `8#`, `2.5@`, `True`, `9`, `34`, `34^`, `2.5!`, and `#2000-01-03#`, and the Null-to-Long row seats runtime error 94; broader scalar conversion matrix, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional positional Null-supplied Optional scalar error evidence promotes `calls/optional_bool_explicit_null_coerce_error`, `calls/optional_byte_explicit_null_coerce_error`, `calls/optional_integer_explicit_null_coerce_error`, `calls/optional_longlong_explicit_null_coerce_error`, `calls/optional_single_explicit_null_coerce_error`, `calls/optional_double_explicit_null_coerce_error`, `calls/optional_currency_explicit_null_coerce_error`, and `calls/optional_date_explicit_null_coerce_error`, covering direct `PickOpt(Null)` calls for the remaining Optional ByVal scalar targets beyond the prior Long row. These rows prove the helper-mediated static call descriptor seats runtime error 94 uniformly for Null-to-Boolean/Byte/Integer/LongLong/Single/Double/Currency/Date supplied parameters before destination mutation; the named Null target matrix and positional/named Variant-payload Null target matrices are covered separately below, while the broader scalar conversion matrix, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional positional supplied Optional scalar coercion/error evidence promotes `calls/optional_long_explicit_boolean_coerce`, `calls/optional_long_explicit_empty_coerce`, `calls/optional_bool_explicit_empty_coerce`, `calls/optional_date_explicit_empty_coerce`, `calls/optional_double_explicit_currency_coerce`, `calls/optional_currency_explicit_long_coerce`, `calls/optional_single_explicit_long_coerce`, `calls/optional_byte_explicit_long_overflow`, `calls/optional_integer_explicit_long_overflow`, and `calls/optional_long_explicit_error_coerce_error`, covering direct Boolean/Empty/Error operands, additional Currency/Long scalar source locals, and narrowing overflow rows supplied positionally to Optional ByVal scalar parameters. These rows prove the current static call descriptor composes with both the direct Boolean/fixed-integer numeric coercion lanes and descriptor-backed helper coercion/error seating, returning `-1`, `0`, `False`, `#1899-12-30#`, `2.5#`, `2@`, and `34!`, while Byte and Integer overflows seat runtime error 6 and Error-to-Long seats runtime error 13; the additional in-range positional scalar conversion slice is covered separately below, while broader scalar conversion matrix, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional positional supplied Optional scalar conversion evidence promotes `calls/optional_byte_explicit_long_coerce`, `calls/optional_integer_explicit_byte_coerce`, `calls/optional_longlong_explicit_double_coerce`, `calls/optional_single_explicit_currency_coerce`, `calls/optional_double_explicit_single_coerce`, `calls/optional_currency_explicit_integer_coerce`, `calls/optional_bool_explicit_double_zero_coerce`, and `calls/optional_date_explicit_long_coerce`, covering in-range Long-to-Byte, Byte-to-Integer, Double-to-LongLong, Currency-to-Single, Single-to-Double, Integer-to-Currency, zero Double-to-Boolean, and Long-to-Date supplied locals. These rows prove the helper-mediated Optional ByVal scalar coercion path preserves in-range narrowing and widening conversion results across the current non-String scalar target matrix, returning `34`, `9`, `34^`, `2.5!`, `2.5#`, `2@`, `False`, and `#2000-01-03#`; the named scalar conversion slice plus positional and matching named Variant-payload equivalents are covered separately below, while remaining scalar conversion matrix, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The selected named supplied Optional scalar coercion/error evidence promotes `calls/optional_long_named_double_coerce`, `calls/optional_double_named_long_coerce`, `calls/optional_currency_named_double_coerce`, `calls/optional_bool_named_long_coerce`, `calls/optional_byte_named_integer_coerce`, `calls/optional_integer_named_long_coerce`, `calls/optional_longlong_named_long_coerce`, `calls/optional_single_named_double_coerce`, `calls/optional_date_named_double_coerce`, and `calls/optional_long_named_null_coerce_error`, covering `PickOpt(value:=actual)` and `PickOpt(value:=Null)` forms for the same scalar target/source matrix as the positional slice. These rows prove binder-resolved named argument binding composes with the shared numeric Variant-helper descriptor path for supplied Optional ByVal scalar coercion and runtime error 94 seating; the additional named in-range conversion slice is covered separately below, while broader scalar conversion matrix, broader multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional named supplied Optional scalar conversion evidence promotes `calls/optional_byte_named_long_coerce`, `calls/optional_integer_named_byte_coerce`, `calls/optional_longlong_named_double_coerce`, `calls/optional_single_named_currency_coerce`, `calls/optional_double_named_single_coerce`, `calls/optional_currency_named_integer_coerce`, `calls/optional_bool_named_double_zero_coerce`, and `calls/optional_date_named_long_coerce`, covering in-range Long-to-Byte, Byte-to-Integer, Double-to-LongLong, Currency-to-Single, Single-to-Double, Integer-to-Currency, zero Double-to-Boolean, and Long-to-Date locals supplied as `PickOpt(value:=actual)`. These rows prove binder-resolved named argument binding preserves the same helper-mediated Optional ByVal scalar coercion results as the positional slice, returning `34`, `9`, `34^`, `2.5!`, `2.5#`, `2@`, `False`, and `#2000-01-03#`; the positional and matching named Variant-payload equivalents are covered separately below, while remaining scalar conversion matrix, broader multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The selected multi-parameter named supplied Optional scalar coercion evidence promotes `calls/optional_long_named_arg_order_double_coerce`, `calls/optional_double_named_arg_order_long_coerce`, `calls/optional_currency_named_arg_order_integer_coerce`, and `calls/optional_bool_named_arg_order_double_zero_coerce`, covering out-of-order `bonus:=...`/`flag:=...` before `n:=...` calls where supplied Optional ByVal `Long`, `Double`, `Currency`, and `Boolean` parameters coerce from `Double`, `Long`, `Integer`, and zero `Double` locals. These rows prove binder-resolved named argument ordering composes with helper-mediated Optional scalar coercion before the static call descriptor, returning `13`, `7#`, `3.25@`, and `5`; broader scalar conversion matrix, broader multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional named Null-supplied Optional scalar error evidence promotes `calls/optional_bool_named_null_coerce_error`, `calls/optional_byte_named_null_coerce_error`, `calls/optional_integer_named_null_coerce_error`, `calls/optional_longlong_named_null_coerce_error`, `calls/optional_single_named_null_coerce_error`, `calls/optional_double_named_null_coerce_error`, `calls/optional_currency_named_null_coerce_error`, and `calls/optional_date_named_null_coerce_error`, covering `PickOpt(value:=Null)` calls for the remaining Optional ByVal scalar targets beyond the prior Long row. These rows prove binder-resolved named argument binding seats runtime error 94 uniformly for Null-to-Boolean/Byte/Integer/LongLong/Single/Double/Currency/Date supplied parameters before destination mutation; the positional and named Variant-payload Null target matrices are covered separately below, while the broader scalar conversion matrix, broader multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional named supplied Optional scalar coercion/error evidence promotes `calls/optional_long_named_boolean_coerce`, `calls/optional_long_named_empty_coerce`, `calls/optional_bool_named_empty_coerce`, `calls/optional_date_named_empty_coerce`, `calls/optional_double_named_currency_coerce`, `calls/optional_currency_named_long_coerce`, `calls/optional_single_named_long_coerce`, `calls/optional_byte_named_long_overflow`, `calls/optional_integer_named_long_overflow`, and `calls/optional_long_named_error_coerce_error`, covering `PickOpt(value:=True)`, `PickOpt(value:=Empty)`, `PickOpt(value:=CVErr(1234))`, named Currency/Long source locals, and named narrowing overflow rows supplied to Optional ByVal scalar parameters. These rows prove binder-resolved named argument binding composes with the same direct Boolean/Empty/Error operand paths plus helper/direct coercion lanes as the positional slice, returning `-1`, `0`, `False`, `#1899-12-30#`, `2.5#`, `2@`, and `34!`, while Byte and Integer overflows seat runtime error 6 and Error-to-Long seats runtime error 13; broader scalar conversion matrix, broader multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The selected Variant-payload supplied Optional scalar coercion/error evidence promotes `calls/optional_long_explicit_variant_double_coerce`, `calls/optional_double_explicit_variant_long_coerce`, `calls/optional_currency_explicit_variant_double_coerce`, `calls/optional_bool_explicit_variant_long_coerce`, `calls/optional_byte_explicit_variant_integer_coerce`, `calls/optional_integer_explicit_variant_long_coerce`, `calls/optional_longlong_explicit_variant_long_coerce`, `calls/optional_single_explicit_variant_double_coerce`, `calls/optional_date_explicit_variant_double_coerce`, and `calls/optional_long_explicit_variant_null_coerce_error`, covering positional `Variant` locals carrying Double, Long, Integer, and Null payloads supplied to Optional ByVal scalar parameters. These rows prove the same descriptor-backed numeric Variant-helper path handles runtime Variant payload coercion for supplied Optional scalar arguments, returning `8`, `8#`, `2.5@`, `True`, `9`, `34`, `34^`, `2.5!`, and `#2000-01-03#`, while the Null payload seats runtime error 94; the additional positional Variant-payload in-range conversion slice is covered separately below, while broader Variant payload matrix, broader scalar conversion matrix, multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional positional Variant-payload supplied Optional scalar conversion evidence promotes `calls/optional_byte_explicit_variant_long_coerce`, `calls/optional_integer_explicit_variant_byte_coerce`, `calls/optional_longlong_explicit_variant_double_coerce`, `calls/optional_single_explicit_variant_currency_coerce`, `calls/optional_double_explicit_variant_single_coerce`, `calls/optional_currency_explicit_variant_integer_coerce`, `calls/optional_bool_explicit_variant_double_zero_coerce`, and `calls/optional_date_explicit_variant_long_coerce`, covering `Variant` locals carrying in-range Long-to-Byte, Byte-to-Integer, Double-to-LongLong, Currency-to-Single, Single-to-Double, Integer-to-Currency, zero Double-to-Boolean, and Long-to-Date payloads supplied as `PickOpt(v)`. These rows prove descriptor-backed runtime Variant payload coercion preserves the same Optional ByVal scalar conversion results as the static positional and named slices, returning `34`, `9`, `34^`, `2.5!`, `2.5#`, `2@`, `False`, and `#2000-01-03#`; the matching named Variant-payload equivalents are covered separately below, while remaining Variant payload/scalar conversion matrix, multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional named Variant-payload supplied Optional scalar conversion evidence promotes `calls/optional_byte_named_variant_long_coerce`, `calls/optional_integer_named_variant_byte_coerce`, `calls/optional_longlong_named_variant_double_coerce`, `calls/optional_single_named_variant_currency_coerce`, `calls/optional_double_named_variant_single_coerce`, `calls/optional_currency_named_variant_integer_coerce`, `calls/optional_bool_named_variant_double_zero_coerce`, and `calls/optional_date_named_variant_long_coerce`, covering `Variant` locals carrying in-range Long-to-Byte, Byte-to-Integer, Double-to-LongLong, Currency-to-Single, Single-to-Double, Integer-to-Currency, zero Double-to-Boolean, and Long-to-Date payloads supplied as `PickOpt(value:=v)`. These rows prove binder-resolved named argument binding preserves the same descriptor-backed runtime Variant payload coercion results as the static named and positional Variant-payload slices, returning `34`, `9`, `34^`, `2.5!`, `2.5#`, `2@`, `False`, and `#2000-01-03#`; remaining Variant payload/scalar conversion matrix, multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional positional Variant-payload Null-supplied Optional scalar error evidence promotes `calls/optional_bool_explicit_variant_null_coerce_error`, `calls/optional_byte_explicit_variant_null_coerce_error`, `calls/optional_integer_explicit_variant_null_coerce_error`, `calls/optional_longlong_explicit_variant_null_coerce_error`, `calls/optional_single_explicit_variant_null_coerce_error`, `calls/optional_double_explicit_variant_null_coerce_error`, `calls/optional_currency_explicit_variant_null_coerce_error`, and `calls/optional_date_explicit_variant_null_coerce_error`, covering `Variant` locals assigned `Null` before positional `PickOpt(v)` calls for the remaining Optional ByVal scalar targets beyond the prior Long row. These rows prove descriptor-backed Variant payload coercion seats runtime error 94 uniformly for Null-to-Boolean/Byte/Integer/LongLong/Single/Double/Currency/Date supplied parameters before destination mutation; the named Variant-payload Null target matrix is covered separately below, while broader Variant payload matrix, broader scalar conversion matrix, multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional positional Variant-payload supplied Optional scalar coercion/error evidence promotes `calls/optional_long_explicit_variant_boolean_coerce`, `calls/optional_long_explicit_variant_empty_coerce`, `calls/optional_bool_explicit_variant_empty_coerce`, `calls/optional_date_explicit_variant_empty_coerce`, `calls/optional_double_explicit_variant_currency_coerce`, `calls/optional_currency_explicit_variant_long_coerce`, `calls/optional_single_explicit_variant_long_coerce`, `calls/optional_byte_explicit_variant_long_overflow`, `calls/optional_integer_explicit_variant_long_overflow`, and `calls/optional_long_explicit_variant_error_coerce_error`, covering positional `Variant` locals carrying Boolean, Empty, Currency, Long, and Error payloads supplied to Optional ByVal scalar parameters. These rows prove the descriptor-backed Variant payload coercion path composes with the additional Boolean/Empty/Error, Currency/Long, and narrowing overflow matrix, returning `-1`, `0`, `False`, `#1899-12-30#`, `2.5#`, `2@`, and `34!`, while Long-payload Byte and Integer overflows seat runtime error 6 and Error-to-Long seats runtime error 13; broader Variant payload matrix, broader scalar conversion matrix, multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The selected named Variant-payload supplied Optional scalar coercion/error evidence promotes `calls/optional_long_named_variant_double_coerce`, `calls/optional_double_named_variant_long_coerce`, `calls/optional_currency_named_variant_double_coerce`, `calls/optional_bool_named_variant_long_coerce`, `calls/optional_byte_named_variant_integer_coerce`, `calls/optional_integer_named_variant_long_coerce`, `calls/optional_longlong_named_variant_long_coerce`, `calls/optional_single_named_variant_double_coerce`, `calls/optional_date_named_variant_double_coerce`, and `calls/optional_long_named_variant_null_coerce_error`, covering `PickOpt(value:=v)` forms for the same Variant payload/source matrix as the positional slice. These rows prove binder-resolved named argument binding composes with runtime Variant payload coercion through the descriptor-backed numeric Variant-helper path for supplied Optional ByVal scalar parameters, including runtime error 94 seating for Null-to-Long; broader Variant payload matrix, broader scalar conversion matrix, broader multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional named Variant-payload Null-supplied Optional scalar error evidence promotes `calls/optional_bool_named_variant_null_coerce_error`, `calls/optional_byte_named_variant_null_coerce_error`, `calls/optional_integer_named_variant_null_coerce_error`, `calls/optional_longlong_named_variant_null_coerce_error`, `calls/optional_single_named_variant_null_coerce_error`, `calls/optional_double_named_variant_null_coerce_error`, `calls/optional_currency_named_variant_null_coerce_error`, and `calls/optional_date_named_variant_null_coerce_error`, covering `Variant` locals assigned `Null` before named `PickOpt(value:=v)` calls for the remaining Optional ByVal scalar targets beyond the prior Long row. These rows prove binder-resolved named argument binding preserves descriptor-backed Variant Null payload error seating across Null-to-Boolean/Byte/Integer/LongLong/Single/Double/Currency/Date supplied parameters before destination mutation; broader Variant payload matrix, broader scalar conversion matrix, broader multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional named Variant-payload supplied Optional scalar coercion/error evidence promotes `calls/optional_long_named_variant_boolean_coerce`, `calls/optional_long_named_variant_empty_coerce`, `calls/optional_bool_named_variant_empty_coerce`, `calls/optional_date_named_variant_empty_coerce`, `calls/optional_double_named_variant_currency_coerce`, `calls/optional_currency_named_variant_long_coerce`, `calls/optional_single_named_variant_long_coerce`, `calls/optional_byte_named_variant_long_overflow`, `calls/optional_integer_named_variant_long_overflow`, and `calls/optional_long_named_variant_error_coerce_error`, covering `PickOpt(value:=v)` forms where `Variant` locals carry Boolean, Empty, Currency, Long, and Error payloads. These rows prove binder-resolved named argument binding composes with the additional runtime Variant payload coercion/error matrix for supplied Optional ByVal scalar parameters, returning `-1`, `0`, `False`, `#1899-12-30#`, `2.5#`, `2@`, and `34!`, while Long-payload Byte and Integer overflows seat runtime error 6 and Error-to-Long seats runtime error 13; broader Variant payload matrix, broader scalar conversion matrix, broader multi-parameter named coercion ordering beyond promoted rows, explicit omitted sentinels not materialized by the binder, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The Optional `String` procedure-call evidence promotes `calls/optional_string_default`, `calls/optional_string_omitted`, `calls/optional_string_explicit_string_literal`, `calls/optional_string_explicit_string_local`, `calls/optional_string_explicit_empty_coerce`, `calls/optional_string_explicit_null_coerce_error`, `calls/optional_string_explicit_error_coerce`, `calls/optional_string_explicit_decimal_coerce`, `calls/optional_string_explicit_numeric_coerce`, `calls/optional_string_explicit_boolean_coerce`, `calls/optional_string_explicit_double_coerce`, `calls/optional_string_explicit_single_coerce`, `calls/optional_string_explicit_currency_coerce`, `calls/optional_string_explicit_integer_coerce`, `calls/optional_string_explicit_byte_coerce`, `calls/optional_string_explicit_longlong_coerce`, `calls/optional_string_explicit_date_literal_coerce`, `calls/optional_string_explicit_dateserial_coerce`, `calls/optional_string_explicit_cdate_numeric_coerce`, `calls/optional_string_explicit_cdate_string_literal_coerce`, `calls/optional_string_explicit_cdate_month_name_literal_coerce`, `calls/optional_string_explicit_cdate_month_name_local_coerce`, `calls/optional_string_explicit_cdate_invalid_string_literal_error`, `calls/optional_string_explicit_cdate_string_local_coerce`, `calls/optional_string_explicit_cdate_invalid_string_local_error`, `calls/optional_string_explicit_date_local_coerce`, `calls/optional_string_explicit_variant_coerce`, `calls/optional_string_explicit_variant_boolean_coerce`, `calls/optional_string_explicit_variant_double_coerce`, `calls/optional_string_explicit_variant_single_coerce`, `calls/optional_string_explicit_variant_currency_coerce`, `calls/optional_string_explicit_variant_integer_coerce`, `calls/optional_string_explicit_variant_byte_coerce`, `calls/optional_string_explicit_variant_longlong_coerce`, `calls/optional_string_explicit_variant_date_coerce`, `calls/optional_string_explicit_variant_string_coerce`, `calls/optional_string_explicit_variant_empty_coerce`, `calls/optional_string_explicit_variant_null_coerce_error`, `calls/optional_string_explicit_variant_error_coerce`, `calls/optional_string_explicit_variant_decimal_coerce`, `calls/optional_string_named_string_literal`, `calls/optional_string_named_string_local`, `calls/optional_string_named_empty_coerce`, `calls/optional_string_named_null_coerce_error`, `calls/optional_string_named_error_coerce`, `calls/optional_string_named_decimal_coerce`, `calls/optional_string_named_numeric_coerce`, `calls/optional_string_named_boolean_coerce`, `calls/optional_string_named_double_coerce`, `calls/optional_string_named_single_coerce`, `calls/optional_string_named_currency_coerce`, `calls/optional_string_named_integer_coerce`, `calls/optional_string_named_byte_coerce`, `calls/optional_string_named_longlong_coerce`, `calls/optional_string_named_date_literal_coerce`, `calls/optional_string_named_dateserial_coerce`, `calls/optional_string_named_cdate_numeric_coerce`, `calls/optional_string_named_cdate_string_literal_coerce`, `calls/optional_string_named_cdate_month_name_literal_coerce`, `calls/optional_string_named_cdate_month_name_local_coerce`, `calls/optional_string_named_cdate_invalid_string_literal_error`, `calls/optional_string_named_cdate_string_local_coerce`, `calls/optional_string_named_cdate_invalid_string_local_error`, `calls/optional_string_named_date_local_coerce`, `calls/optional_string_named_variant_coerce`, `calls/optional_string_named_variant_boolean_coerce`, `calls/optional_string_named_variant_double_coerce`, `calls/optional_string_named_variant_single_coerce`, `calls/optional_string_named_variant_currency_coerce`, `calls/optional_string_named_variant_integer_coerce`, `calls/optional_string_named_variant_byte_coerce`, `calls/optional_string_named_variant_longlong_coerce`, `calls/optional_string_named_variant_date_coerce`, `calls/optional_string_named_variant_string_coerce`, `calls/optional_string_named_variant_empty_coerce`, `calls/optional_string_named_variant_null_coerce_error`, `calls/optional_string_named_variant_error_coerce`, and `calls/optional_string_named_variant_decimal_coerce`, covering binder-materialized Optional ByVal `String` default `"alpha"` and omitted no-default empty-string rows plus supplied positional `PickOpt("beta")`/`PickOpt(text As String)`/`PickOpt(Empty)`/`PickOpt(Null)`/`PickOpt(CVErr(1234))`/`PickOpt(CDec(12345))`/`PickOpt(42&)`/`PickOpt(True)`/`PickOpt(12.5#)`/`PickOpt(12.5!)`/`PickOpt(12.3456@)`/`PickOpt(44%)`/`PickOpt(b As Byte)`/`PickOpt(5000000012^)`/`PickOpt(#2020-01-15#)`/`PickOpt(DateSerial(2020, 1, 15))`/`PickOpt(CDate(43845#))`/`PickOpt(CDate("2020-01-15"))`/`PickOpt(CDate("February 28, 2026"))`/`PickOpt(CDate(text As String = "February 28, 2026"))`/`PickOpt(CDate("not-a-date"))`/`PickOpt(CDate(text As String))`/`PickOpt(CDate(text As String = "not-a-date"))`/`PickOpt(d As Date)`/`PickOpt(v)` and named `PickOpt(value:="beta")`/`PickOpt(value:=text As String)`/`PickOpt(value:=Empty)`/`PickOpt(value:=Null)`/`PickOpt(value:=CVErr(1234))`/`PickOpt(value:=CDec(12345))`/`PickOpt(value:=42&)`/`PickOpt(value:=True)`/`PickOpt(value:=12.5#)`/`PickOpt(value:=12.5!)`/`PickOpt(value:=12.3456@)`/`PickOpt(value:=44%)`/`PickOpt(value:=b As Byte)`/`PickOpt(value:=5000000012^)`/`PickOpt(value:=#2020-01-15#)`/`PickOpt(value:=DateSerial(2020, 1, 15))`/`PickOpt(value:=CDate(43845#))`/`PickOpt(value:=CDate("2020-01-15"))`/`PickOpt(value:=CDate("February 28, 2026"))`/`PickOpt(value:=CDate(text As String = "February 28, 2026"))`/`PickOpt(value:=CDate("not-a-date"))`/`PickOpt(value:=CDate(text As String))`/`PickOpt(value:=CDate(text As String = "not-a-date"))`/`PickOpt(value:=d As Date)`/`PickOpt(value:=v)` calls with direct String literal/local sources, direct Empty/Null/Error/Decimal expression sources, Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong literal/local, direct Date literal, DateSerial result, numeric CDate result, valid ISO and month-name string-literal CDate results, invalid string-literal CDate parse error, valid ISO and month-name string-local CDate results, invalid string-local CDate parse error, and Date local sources, and Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong/Date/String/Error/Decimal/Empty/Null-payload `Variant` sources overriding the declared default through the same descriptor-backed String/coercion helper path, including Null seating runtime error 94 and invalid string-literal and string-local CDate seating runtime error 13 before destination mutation. Broader Optional `String` forms with non-String coercions beyond the promoted Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong literal/local, direct Date literal/DateSerial/numeric CDate/valid ISO and month-name string-literal CDate/invalid string-literal CDate/valid ISO and month-name string-local CDate/invalid string-local CDate/local, and Long/Boolean/Double/Single/Currency/Integer/Byte/LongLong/Date/String/Error/Decimal/Empty/Null-payload Variant supplied rows, broader named argument shapes beyond promoted rows, explicit omitted sentinels not materialized by the binder, Date supplied payloads beyond the promoted Date literal, DateSerial, numeric CDate, valid ISO and month-name string-literal CDate, invalid string-literal CDate, valid ISO and month-name string-local CDate, invalid string-local CDate, Date local, and Variant-Date rows, broader string parsing shapes beyond the promoted ISO and month-name valid literal/local rows plus invalid literal/local rows, fixed-length strings, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The selected multi-parameter named Optional `String` evidence promotes `calls/optional_string_named_arg_order_long_coerce`, `calls/optional_string_named_arg_order_date_local_coerce`, `calls/optional_string_named_arg_order_variant_decimal_coerce`, and `calls/optional_string_named_arg_order_null_coerce_error`, covering out-of-order `PickText(second:=..., first:="alpha")` calls where the supplied Optional ByVal `String` parameter coerces from a `Long` local, `Date` local, Decimal-payload `Variant`, and direct `Null`. These rows prove binder-resolved named argument ordering composes with descriptor-backed Optional `String` coercion and runtime error seating before the static call descriptor, returning `"42"`, `"1/15/2020"`, and `"12345"`, while the Null row seats runtime error 94; broader named Optional `String` ordering/coercion shapes beyond promoted rows, explicit omitted sentinels not materialized by the binder, fixed-length strings, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional Date-producing multi-parameter named Optional `String` evidence promotes `calls/optional_string_named_arg_order_dateserial_coerce`, `calls/optional_string_named_arg_order_cdate_numeric_coerce`, `calls/optional_string_named_arg_order_cdate_month_name_local_coerce`, and `calls/optional_string_named_arg_order_cdate_invalid_string_literal_error`, covering out-of-order `PickText(second:=..., first:="alpha")` calls where the supplied Optional ByVal `String` parameter receives `DateSerial(2020, 1, 15)`, `CDate(43845#)`, `CDate(text)` with `text = "February 28, 2026"`, and `CDate("not-a-date")`. These rows prove binder-resolved named argument ordering composes with Date-producing library/conversion results before Optional `String` coercion, returning `"1/15/2020"` for DateSerial and numeric CDate, returning `"2/28/2026"` for the month-name local CDate, and seating runtime error 13 for invalid string-literal CDate; broader named Optional `String` Date-producing and string-parsing shapes beyond promoted rows, slash-date/locale-sensitive parsing, explicit omitted sentinels not materialized by the binder, fixed-length strings, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional scalar/edge multi-parameter named Optional `String` evidence promotes `calls/optional_string_named_arg_order_empty_coerce`, `calls/optional_string_named_arg_order_error_coerce`, `calls/optional_string_named_arg_order_boolean_local_coerce`, and `calls/optional_string_named_arg_order_currency_local_coerce`, covering out-of-order `PickText(second:=..., first:="alpha")` calls where the supplied Optional ByVal `String` parameter receives direct `Empty`, direct `CVErr(1234)`, a Boolean local set to `True`, and a Currency local set to `12.3456@`. These rows prove binder-resolved named argument ordering composes with scalar and direct error/empty Optional `String` coercion before the static call descriptor, returning an empty string, `"Error 1234"`, `"True"`, and `"12.3456"`; broader named Optional `String` scalar/edge coercions beyond promoted rows, explicit omitted sentinels not materialized by the binder, fixed-length strings, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional numeric-width multi-parameter named Optional `String` evidence promotes `calls/optional_string_named_arg_order_double_coerce`, `calls/optional_string_named_arg_order_single_coerce`, `calls/optional_string_named_arg_order_integer_coerce`, `calls/optional_string_named_arg_order_byte_local_coerce`, and `calls/optional_string_named_arg_order_longlong_coerce`, covering out-of-order `PickText(second:=..., first:="alpha")` calls where the supplied Optional ByVal `String` parameter receives direct `Double` literal `12.5#`, direct `Single` literal `12.5!`, direct `Integer` literal `44%`, a Byte local set to `7`, and direct `LongLong` literal `5000000012^`. These rows prove binder-resolved named argument ordering composes with additional numeric-width Optional `String` coercion before the static call descriptor, returning `"12.5"`, `"12.5"`, `"44"`, `"7"`, and `"5000000012"`; broader named Optional `String` numeric-width and scalar carriers beyond promoted rows, explicit omitted sentinels not materialized by the binder, fixed-length strings, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The representative Variant-payload multi-parameter named Optional `String` evidence promotes `calls/optional_string_named_arg_order_variant_boolean_coerce`, `calls/optional_string_named_arg_order_variant_double_coerce`, `calls/optional_string_named_arg_order_variant_date_coerce`, `calls/optional_string_named_arg_order_variant_string_coerce`, `calls/optional_string_named_arg_order_variant_error_coerce`, and `calls/optional_string_named_arg_order_variant_empty_coerce`, covering out-of-order `PickText(second:=v, first:="alpha")` calls where the supplied Optional ByVal `String` parameter receives a `Variant` local carrying Boolean `True`, Double `12.5#`, Date `#2020-01-15#`, String `"beta"`, Error `CVErr(1234)`, and Empty payloads. These rows prove binder-resolved named argument ordering composes with representative Variant-payload Optional `String` coercion before the static call descriptor, returning `"True"`, `"12.5"`, `"1/15/2020"`, `"beta"`, `"Error 1234"`, and an empty string; additional numeric-width Variant payload carriers are promoted separately below, while explicit omitted sentinels not materialized by the binder, fixed-length strings, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The additional numeric-width Variant-payload multi-parameter named Optional `String` evidence promotes `calls/optional_string_named_arg_order_variant_single_coerce`, `calls/optional_string_named_arg_order_variant_currency_coerce`, `calls/optional_string_named_arg_order_variant_integer_coerce`, `calls/optional_string_named_arg_order_variant_byte_coerce`, and `calls/optional_string_named_arg_order_variant_longlong_coerce`, covering out-of-order `PickText(second:=v, first:="alpha")` calls where the supplied Optional ByVal `String` parameter receives a `Variant` local carrying Single `12.5!`, Currency `12.3456@`, Integer `44%`, Byte `7`, and LongLong `5000000012^` payloads. These rows prove binder-resolved named argument ordering composes with the remaining promoted numeric-width Variant-payload Optional `String` coercions before the static call descriptor, returning `"12.5"`, `"12.3456"`, `"44"`, `"7"`, and `"5000000012"`; remaining Variant payload carriers beyond the promoted Decimal, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Empty, and Null rows, explicit omitted sentinels not materialized by the binder, fixed-length strings, typed primary entries, dynamic thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this Linux slice.

The direct `CallProcRef` String evidence adds `oxvba-jit` unit coverage for known single-`AddressOf` targets: exact String ByVal return copies `"alpha"` to the caller destination, and exact String ByRef mutation writes `"beta"` back through the alias path. It also adds narrow ambiguous same-signature coverage for a no-argument String-return target set that returns `"beta"` from the actual runtime target after signature validation, a ByVal String argument/return target set that echoes `"alpha"` from the actual runtime target, and exact ByRef String mutation writing `"beta"` back through the alias path. The unknown-signature/runtime-only lane now also has direct no-argument concrete return coverage into exact matching destinations, including String into exact `String` and Double into exact `Double`, plus no-argument scalar/String/actual Variant-returning function-as-statement units whose returns are ignored while side-effect globals prove dispatch, plus narrow exact String ByVal argument/return units into exact `String` and `Variant` destinations, including a two-argument String ByVal local-plus-literal unit returning the second argument into exact `String` and `Variant` destinations, one- and two-argument String-payload Variant argument rows returning concrete String into exact `String` and `Variant` destinations, selected all-Variant two-argument Variant-payload rows for Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads across no-destination Sub, concrete `String` return to exact `String` and `Variant` destinations, actual `Variant` return-to-Variant shapes, and ignored String/actual Variant function-as-statement shapes, a mixed static String plus String-payload Variant argument row returning concrete String into exact `String` and `Variant` destinations, selected mixed static String plus Variant-payload rows for Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads across no-destination Sub, concrete `String` return to exact `String` and `Variant` destinations, actual `Variant` return-to-Variant shapes, and ignored String/actual Variant function-as-statement shapes, one- and two-argument String ByVal actual Variant return-to-Variant units including one- and two-argument String-payload Variant argument rows plus a mixed static String/String-payload Variant argument row, one- and two-argument String ByVal no-destination Sub units storing local, literal, global-source, temp-source, fixed-length local, String-payload Variant arguments, selected no-destination Variant-to-String coercion/error rows for Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads, selected return/destination Variant-to-String coercion/error rows for Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads across concrete `String` returns into exact `String` and `Variant` destinations plus actual `Variant` return-to-Variant, and a mixed static String/String-payload Variant argument row in globals, String- and actual Variant-returning function-as-statement units, including mixed static String/String-payload Variant argument rows, whose returns are ignored while String global side effects prove execution, plus exact `ByRef text As String` mutation and mixed `ByRef text As String, ByVal value As String` mutation across no-destination, concrete String return-to-String/Variant destination, and actual Variant return-to-Variant rows plus selected ignored String/actual Variant function-as-statement rows, with the mixed ByRef rows coercing Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, and Empty Variant payloads to `"42"`, `"True"`, `"12.5"`, `"12.5"`, `"12.3456"`, `"44"`, `"7"`, `"5000000012"`, `"1/15/2020"`, `"omega"`, `"Error 1234"`, `"12345"`, and `""` before writing through the alias, seating Null payloads as runtime error 94, accepting a statically typed String source-place value `"gamma"` through the same ByVal copy-in lane, and accepting direct String literal `"delta"` plus fixed-length String source-place `"xy "` carriers across no-destination, concrete String return-to-String/Variant destination, and actual Variant return-to-Variant rows, plus ignored String/actual Variant function-as-statement rows for statically typed String source-place, direct literal, and fixed-length carriers. A direct mixed Long/String ByVal compile-decline unit guards the boundary between the String-only and LongOnly dynamic lanes. This is direct OxIR evidence only; source-level ProcRef invocation, broader unknown String argument/parameter shapes beyond the exact ByVal units, no-destination Sub units, and promoted exact/mixed ByRef mutation, return, and selected function-as-statement rows, broader Variant-payload String argument shapes beyond the promoted String-payload rows plus selected no-destination, selected return/destination, selected mixed static/Variant coercion/error, and selected mixed ByRef Variant-payload function-as-statement/static String/literal/fixed rows, broader unknown String/concrete/Variant return shapes beyond the no-argument exact-destination and exact ByVal units plus selected return/destination, selected ByRef return/actual-Variant-return, and selected coercion/error/function-as-statement rows, broader ambiguous String parameter/return/mutation shapes beyond those exact units, typed-indirect dispatch, dynamic-entry thunks, direct typed calls, and Excel oracle confirmation remain outside this Linux slice.

The direct `CallProcRef` typed-return-to-Variant evidence adds `oxvba-jit` unit coverage for known single-`AddressOf` targets returning `Long` and `String` values into `Variant` locals, plus ambiguous same-signature `Double -> Double` and no-argument String-return target sets returning into `Variant` locals after runtime signature validation. It also covers direct unknown-signature/runtime-only no-argument concrete scalar/String return units into `Variant` destinations, including Long, String, LongLong, Currency, Single, Double, Date, Byte, Integer, and Bool returns, one-/two-argument Long-only, mixed Long ByRef/ByVal, and one-/two-argument String-ByVal-only actual `Variant` return targets carrying boxed Long/String payloads, plus exact Long-only and String ByVal argument/return-to-Variant units including the two-argument String ByVal local-plus-literal row, proving the narrow dynamic return-kind guard and helper-mediated boxing path without opening arbitrary dynamic signatures. Source-level ProcRef invocation, unknown-signature String argument shapes beyond the exact ByVal unit/ByRef/parameter shapes, unknown-signature concrete scalar/String/Variant return-to-Variant shapes beyond the no-argument and exact Long-only/String ByVal units, broader ambiguous String return-to-Variant shapes beyond the no-argument unit, broad return coercion, typed-indirect dispatch, dynamic-entry thunks, direct typed calls, and Excel oracle confirmation remain outside this Linux slice.

The direct unknown-signature/runtime-only `CallProcRef` Long-only evidence adds `oxvba-jit` units where `__GlobalInit` loads a global `ProcRef` outside the calling procedure: an `EchoLong(value As Long) As Long` call returns the supplied `42` into exact Long and `Variant` destinations, including a Long-payload Variant ByVal actual into both destination shapes, an `EchoVariant(value As Long) As Variant` call boxes the supplied `42` into an actual Variant return, including a Long-payload Variant ByVal actual, an `AddLongs(left As Long, right As Long) As Long` call returns `42` into an exact Long destination, an `AddLongsVariant(left As Long, right As Long) As Variant` call boxes the checked sum `42` into a Variant return, an `AddAndStore(ByRef value As Long, ByVal delta As Long) As Long` call mutates a caller Long local from `40` to `42` and returns `42`, an `AddAndStoreVariant(ByRef value As Long, ByVal delta As Long) As Variant` call mutates the caller Long local from `40` to `42` and boxes `42` into a Variant return, `StoreValue(value As Long)` Sub calls write `42`, `-1`, or `7` from Long, Integer, Boolean literal, Byte/Integer/Boolean source-place, and Long/Integer/Byte/Boolean-payload Variant ByVal actual carriers to a Long global with no destination slot, and `StoreSum(left As Long, right As Long)` writes `42` from two ByVal Long descriptors, `StoreAndReturn(value As Long) As Long` writes `42` to a Long global while its return is ignored by a statement-style call, `StoreAndReturnVariant(value As Long) As Variant` writes `42` to the same Long global while boxing an ignored actual Variant return, `AddAndStoreSub(ByRef value As Long, ByVal delta As Long)` mutates a caller Long local from `40` to `42` with no destination slot, and a `SetValue(ByRef value As Long)` Sub mutates a caller Long local to `42`. It also has a compile-decline guard for LongLong, Currency, Single, Double, and Date ByVal actuals against this unknown Long-only fallback. This proves dynamic Long return, actual Variant return-to-Variant, no-return, function-as-statement ignored Long and actual Variant returns, and exact Long ByRef markers forward validated Long-only arguments without relying on a statically known target, including the current multi-argument descriptor iteration row, Variant-payload Long actual concrete and actual Variant return rows, a two-ByVal Long actual Variant-return function, a mixed ByRef/ByVal no-destination Sub, a mixed ByRef/ByVal Long-only function, and a mixed ByRef/ByVal actual Variant-return function; non-Long parameters, non-Long ByRef mutation, mixed non-Long argument shapes, String argument shapes beyond the exact ByVal return unit, broader Variant-payload parameter coercions beyond the promoted Long/Integer/Byte/Boolean Long-parameter row, Variant-return non-empty calls beyond the exact Long-only and String-return units, actual Variant-return targets outside the no-argument and Long-only argument units, source-level ProcRef invocation, typed-indirect dispatch, dynamic-entry thunks, direct typed calls, and Excel oracle confirmation remain outside this Linux slice.

The direct unknown-signature/runtime-only concrete scalar return evidence also adds no-argument exact-destination units for `LongLong`, `Currency`, `Single`, `Double`, `Date`, `Byte`, `Integer`, and `Bool` returns. This proves the dynamic no-argument exact-return marker admits non-Long concrete scalar returns only when the runtime target's declared return type matches the caller's exact destination; non-empty non-Long calls, cross-type return coercion, actual `Variant` return targets, typed-indirect dispatch, dynamic-entry thunks, direct typed calls, and Excel oracle confirmation remain outside this Linux slice.

The selected scalar library-result destination evidence promotes `inline/builtin_scalar_result_destinations`, `inline/builtin_scalar_result_destination_families`, `inline/builtin_scalar_result_destination_coercions`, `inline/builtin_scalar_result_destination_coercion_overflow`, and `inline/builtin_scalar_result_destination_null_error`, covering direct `Len`, `LenB`, `InStr`, `Val`, and `IsNull` results assigned to `Long`, `Double`, and `Boolean` globals, selected `Round`, `Year`, `DateValue`, `TimeValue`, `RGB`, `QBColor`, and `IsDate` results assigned to `Double`, `Long`, `Date`, and `Boolean` globals, selected `Len`/`InStr`/`LenB`/`Val` scalar result assignments into `Byte`, `Integer`, `Single`, `Double`, `Currency`, and `Boolean` globals, the selected `Len`-to-`Byte` runtime error 6 overflow row, and the selected `Abs(Null)`-to-`Long` runtime error 94 row through the current library-result temp plus scalar coercion helper path. Broader result coercion/narrowing, scalar destination types, and library result families remain outside this Linux slice.

The selected string-typed alias evidence also promotes `inline/builtin_string_typed_aliases`, `inline/builtin_string_typed_alias_null_error`, and `inline/builtin_string_typed_alias_destinations`, covering `LCase$`, `UCase$`, `Trim$`, `Left$`, `Right$`, `Mid$`, `ChrW$`, `Space$`, and `String$` through the JIT `CallExtern` library bridge, plus `Left$(Null, 2)` seating runtime error 94 before the unsuffixed library result propagation path can return `Null`, and selected dollar-suffixed alias results assigned to `String` globals including `LeftB$` and `RightB$`. These rows prove the alias policy flag, shared runtime helper, and current Variant-to-String destination helper only for the promoted alias spellings and fixed argument shapes; `Chr$`, `Command$`, `Format$` shapes beyond the source-level one-argument CStr-passthrough row, optional/broader alias forms, fixed-length strings, broader `String` destination shapes, and `Error$` forms beyond the promoted fixed-integer destination row remain outside this Linux slice.

The fixed-integer direct `Error(number)` destination evidence also promotes `inline/builtin_error_text_result_destinations`, covering explicit `Long` source locals for known error 11 and unknown error 12345 assigned to `String` globals. This proves the current `ErrorText` fixed-integer gate can combine the non-alias library bridge with the Variant-to-String destination helper for those two value rows only.

The fixed-integer `Error$` destination evidence also promotes `inline/builtin_error_text_alias_destinations`, covering explicit `Long` source locals for known error 11 and unknown error 12345 assigned to `String` globals. This proves the current `ErrorText` fixed-integer gate can combine the alias-aware bridge with the Variant-to-String destination helper for those two value rows only.

The invalid fixed-integer Error-to-String evidence also promotes `inline/builtin_error_text_result_destination_invalid` and `inline/builtin_error_text_alias_destination_invalid`, covering explicit `Long` source local `-1` assigned through direct `Error(number)` and `Error$` forms to `String` globals. These rows prove runtime error 5 seating through the same non-alias and alias-aware bridges plus the current destination helper path; zero-argument `Error()`/`Error$()`, non-fixed-integer operands, and broader error-message/catalog edge rows remain outside this Linux slice.

The fixed-integer `Space(count)` evidence also includes `inline/builtin_space_negative_count`, covering an explicit `Long` source local set to `-1` and proving shared runtime error 5 seating through the same one-argument fixed-integer gate.

The fixed-integer `String(count, code)` evidence also includes `inline/builtin_string_repeat_charcode_wrap_expr`, covering `String(2, 322&)` over explicit `Long` source locals and proving wrap to `BB` through the same shared runtime gate.

The fixed-integer `String(count, code)` error evidence also includes `inline/builtin_string_repeat_negative_count`, covering explicit `Long` source locals for `String(-1, 65)` and proving shared runtime error 5 seating through the same two-argument fixed-integer gate.

The `Left`/`Right` count-edge evidence also includes `inline/builtin_left_right_variant_complement_count_edges`, covering `Left(text, 10)` and `Right(text, 0)` over the same String-bearing `Variant` source plus explicit `Long` count locals and proving full-source overlong `Left` plus zero-count empty `Right` through the same fixed-count gate.

The `Left`/`Right` unit-count evidence also includes `inline/builtin_left_right_variant_unit_count`, covering `Left(text, 1)` and `Right(text, 1)` over the same String-bearing `Variant` source plus an explicit `Long` count local and proving first- and last-code-unit extraction through the same fixed-count gate.

The `Left`/`Right` exact source-length evidence also includes `inline/builtin_left_right_variant_exact_source_count`, covering `Left(left_text, 5)` and `Right(right_text, 5)` over distinct String-bearing `Variant` sources plus an explicit `Long` count local and proving full-source return at exact source length through the same fixed-count gate.

The direct literal code-unit slice evidence also includes `inline/builtin_string_literal_left_right_count_edges`, covering `Left("12345", 1)`, `Right("12345", 1)`, `Left("12345", 0)`, `Right("67890", 10)`, `Left("12345", 5)`, and `Right("98765", 5)` through the same `OxConst::Str` descriptor lane plus fixed-integer count gate.

The direct literal `Mid` evidence also includes `inline/builtin_string_literal_mid_count_edges`, covering `Mid("12345", 1)`, `Mid("12345", 5)`, `Mid("12345", 6)`, `Mid("12345", 2, 0)`, `Mid("12345", 1, 5)`, and `Mid("12345", 2, 4)` through the same `OxConst::Str` descriptor lane plus fixed start/optional-count gate.

The direct literal `Mid` value-edge evidence also promotes `inline/builtin_string_literal_mid_value_edges`, covering `Mid("12345", 2, 10)`, `Mid("12345", 1, 10)`, `Mid("12345", 5, 1)`, and `Mid("12345", 1, 2)`. This row proves overlong-count interior/full-source truncation and exact last-character/prefix selection through the same `OxConst::Str` descriptor lane plus fixed start/count gate.

The direct literal `Mid` boundary value evidence also promotes `inline/builtin_string_literal_mid_boundary_value_edges`, covering `Mid("12345", 6, 2)`, `Mid("12345", 1, 0)`, and `Mid("12345", 5, 0)`. This row proves counted overlong-start and zero-length first/last-position empty-string results through the same `OxConst::Str` descriptor lane plus fixed start/count gate.

The direct literal `Mid` error evidence also promotes `inline/builtin_string_literal_mid_start_zero`, `inline/builtin_string_literal_mid_negative_start`, and `inline/builtin_string_literal_mid_negative_length`, covering direct `"12345"` sources plus explicit `Long` start/count locals for `Mid("12345", 0)`, `Mid("12345", -1, 2)`, and `Mid("12345", 2, -1)`. These rows prove shared runtime error 5 seating through the same `OxConst::Str` descriptor lane plus fixed start/count gates.

The static `String` code-unit slice evidence also includes `inline/builtin_static_string_left_right_count_edges`, covering `Left(text, 1)`, `Right(text, 1)`, `Left(text, 0)`, `Right(overText, 10)`, `Left(text, 5)`, and `Right(exactRight, 5)` over statically typed `String` source places through the same representation-preserving source bridge plus fixed-integer count gate.

The static `String` `Mid` evidence also includes `inline/builtin_static_string_mid_count_edges`, covering `Mid(text, 1)`, `Mid(text, 5)`, `Mid(text, 6)`, `Mid(text, 2, 0)`, `Mid(text, 1, 5)`, and `Mid(text, 2, 4)` over a statically typed `String` source place through the same representation-preserving source bridge plus fixed start/optional-count gate.

The static `String` `Mid` value-edge evidence also promotes `inline/builtin_static_string_mid_value_edges`, covering `Mid(text, 2, 10)`, `Mid(text, 1, 10)`, `Mid(text, 5, 1)`, and `Mid(text, 1, 2)` over a statically typed `String` source place. This row proves overlong-count interior/full-source truncation and exact last-character/prefix selection through the same representation-preserving source bridge plus fixed start/count gate.

The static `String` `Mid` boundary value evidence also promotes `inline/builtin_static_string_mid_boundary_value_edges`, covering `Mid(text, 6, 2)`, `Mid(text, 1, 0)`, and `Mid(text, 5, 0)` over a statically typed `String` source place. This row proves counted overlong-start and zero-length first/last-position empty-string results through the same representation-preserving source bridge plus fixed start/count gate.

The static `String` `Mid` error evidence also promotes `inline/builtin_static_string_mid_start_zero`, `inline/builtin_static_string_mid_negative_start`, and `inline/builtin_static_string_mid_negative_length`, covering a `String` source-place set to `"12345"` plus explicit `Long` start/count locals for `Mid(text, 0)`, `Mid(text, -1, 2)`, and `Mid(text, 2, -1)`. These rows prove shared runtime error 5 seating through the same representation-preserving `String` source-place bridge plus fixed start/count gates.

The Null dynamic slice evidence also includes `inline/builtin_string_null_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains `Null`; all unsuffixed Variant-returning forms propagate `Null` through the existing Variant source-place descriptor lane plus fixed count/start gates. Remaining dynamic payload work means Null rows beyond the promoted Null slice row, Empty rows beyond the promoted Empty slice row, and non-String rows beyond the promoted numeric Long, Integer, LongLong, Byte, Currency, Date, Error, Decimal, Boolean, Double, and Single slice rows.

The Empty dynamic slice evidence also includes `inline/builtin_string_empty_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains `Empty`; all six unsuffixed Variant-returning forms coerce `Empty` to empty strings through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Empty` in the snapshot.

The numeric dynamic slice evidence also includes `inline/builtin_string_numeric_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains `Long` payload `12345`; the same six unsuffixed Variant-returning forms coerce that numeric payload to string results through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Long` in the snapshot.

The Boolean dynamic slice evidence also includes `inline/builtin_string_boolean_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains Boolean payload `True`; the same six unsuffixed Variant-returning forms coerce that Boolean payload to string results through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Boolean` in the snapshot.

The Double dynamic slice evidence also includes `inline/builtin_string_double_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains Double payload `12345#`; the same six unsuffixed Variant-returning forms coerce that Double payload to string results through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Double` in the snapshot.

The Single dynamic slice evidence also includes `inline/builtin_string_single_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains Single payload `12345!`; the same six unsuffixed Variant-returning forms coerce that Single payload to string results through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Single` in the snapshot.

The Integer dynamic slice evidence also includes `inline/builtin_string_integer_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains Integer payload `12345%`; the same six unsuffixed Variant-returning forms coerce that Integer payload to string results through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Integer` in the snapshot.

The LongLong dynamic slice evidence also includes `inline/builtin_string_longlong_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains LongLong payload `12345^`; the same six unsuffixed Variant-returning forms coerce that LongLong payload to string results through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `LongLong` in the snapshot.

The Byte dynamic slice evidence also includes `inline/builtin_string_byte_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains Byte payload `CByte(123)`; the same six unsuffixed Variant-returning forms coerce that Byte payload to string results through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Byte` in the snapshot.

The Currency dynamic slice evidence also includes `inline/builtin_string_currency_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains Currency payload `12345@`; the same six unsuffixed Variant-returning forms coerce that Currency payload to string results through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Currency` in the snapshot.

The Date dynamic slice evidence also includes `inline/builtin_string_date_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains Date payload `#2020-01-15#`; the same six unsuffixed Variant-returning forms coerce that Date payload to General Date string results through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Date` in the snapshot. Excel/VBA locale confirmation remains deferred to the Windows/Excel post-implementation round.

The Error dynamic slice evidence also includes `inline/builtin_string_error_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains Error payload `CVErr(1234)`; the same six unsuffixed Variant-returning forms coerce that Error payload to `Error 1234` string slices through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Error` in the snapshot.

The Decimal dynamic slice evidence also includes `inline/builtin_string_decimal_slice_args`, covering `Left(n, 2)`, `Right(n, 2)`, `LeftB(n, 2)`, `RightB(n, 2)`, `Mid(n, 1)`, and `Mid(n, 1, 2)` where `n As Variant` contains Decimal payload `CDec(12345)`; the same six unsuffixed Variant-returning forms coerce that Decimal subtype to `12345` string slices through the existing Variant source-place descriptor lane plus fixed count/start gates, while the local carrier remains `Decimal` in the snapshot.

The `Mid` error evidence also includes `inline/builtin_mid_variant_negative_length`, covering the same String-bearing `Variant` source with explicit `Long` start `2` and count `-1`, and `inline/builtin_mid_variant_negative_start`, covering explicit `Long` start `-1` and count `2`; both prove shared runtime error 5 seating through the same fixed start/count gate.

The `Mid` start/count edge evidence also includes `inline/builtin_mid_variant_overlong_start`, covering explicit `Long` start `6` and count `2` over the same String-bearing `Variant` source and proving the overlong-start empty-string result through the same fixed start/count gate. It also includes `inline/builtin_mid_variant_overlong_count`, covering explicit `Long` start `5` and count `10` over the same source and proving the overlong-count suffix truncation result through that gate, `inline/builtin_mid_variant_overlong_count_middle`, covering explicit `Long` start `2` and count `10` and proving interior overlong-count truncation through that gate, `inline/builtin_mid_variant_overlong_count_full_source`, covering explicit `Long` start `1` and count `10` and proving full-source overlong-count truncation through that gate, `inline/builtin_mid_variant_exact_last_char`, covering explicit `Long` start `5` and count `1` and proving exact last-character selection through that gate, `inline/builtin_mid_variant_exact_full_source_count`, covering explicit `Long` start `1` and count `5` and proving exact full-source count selection through that gate, `inline/builtin_mid_variant_exact_prefix_count`, covering explicit `Long` start `1` and count `2` and proving exact prefix-count selection through that gate, `inline/builtin_mid_variant_exact_suffix_count`, covering explicit `Long` start `2` and count `4` and proving exact suffix-count selection through that gate, `inline/builtin_mid_variant_zero_length`, covering explicit `Long` start `1` and count `0`, `inline/builtin_mid_variant_zero_length_middle`, covering explicit `Long` start `2` and count `0`, and `inline/builtin_mid_variant_zero_length_at_end`, covering explicit `Long` start `5` and count `0`, proving the zero-length empty-string result at first, interior, and last source code-unit positions through that gate, `inline/builtin_mid_variant_omitted_length_full_source`, covering explicit `Long` start `1` with omitted count and proving the omitted-length full-source result through that gate, `inline/builtin_mid_variant_omitted_length_suffix`, covering explicit `Long` start `5` with omitted count and proving the omitted-length final-suffix result through that gate, plus `inline/builtin_mid_variant_omitted_length_overlong_start`, covering explicit `Long` start `6` with omitted count and proving the omitted-length overlong-start empty-string result through that gate.

The `Hex`/`Oct` string-result evidence also includes `inline/builtin_hex_oct_negative_width_exprs`, covering explicit `Integer`, `Long`, and `LongLong` source locals set to `-1` and proving the fixed-width two's-complement results `FFFF`, `FFFFFFFF`, `FFFFFFFFFFFFFFFF`, `177777`, `37777777777`, and `1777777777777777777777` through the same shared runtime gate.

The byte-slice evidence also includes `inline/builtin_leftb_rightb_odd_byte_exprs`, covering statically typed `String` source `ABC` with `LeftB`/`RightB` byte counts `1` and `3`, and proving `Len`/`LenB` plus `AscW` observables for incomplete-byte and shifted-code-unit BSTR slices.

The byte-slice unit-code-unit evidence also includes `inline/builtin_leftb_rightb_variant_unit_code_unit_byte_count`, covering `LeftB(text, 2)` and `RightB(text, 2)` over the same String-bearing `Variant` source plus an explicit `Long` byte-count local and proving first- and last-code-unit extraction through the same fixed-count gate.

The byte-slice three-code-unit evidence also includes `inline/builtin_leftb_rightb_variant_three_code_unit_byte_count`, covering `LeftB(text, 6)` and `RightB(text, 6)` over the same String-bearing `Variant` source plus an explicit `Long` byte-count local and proving three-code-unit prefix/suffix extraction through the same fixed-count gate.

The direct literal byte-slice evidence also includes `inline/builtin_string_literal_leftb_rightb_byte_counts`, covering `LeftB("12345", 2)`, `RightB("12345", 2)`, `LeftB("12345", 6)`, and `RightB("12345", 6)` through the same `OxConst::Str` descriptor lane plus fixed-integer count gate.

The direct literal byte-slice edge evidence also promotes `inline/builtin_string_literal_leftb_rightb_byte_count_edges`, `inline/builtin_string_literal_leftb_rightb_complement_byte_count_edges`, and `inline/builtin_string_literal_leftb_rightb_exact_byte_source_count`, covering direct string literals with byte counts `0`, `20`, and exact byte count `10`. These rows prove empty `LeftB("12345", 0)`, full-source `RightB("12345", 20)`, full-source `LeftB("12345", 20)`, empty `RightB("12345", 0)`, and exact-byte-count `LeftB("12345", 10)`/`RightB("98765", 10)` through the same `OxConst::Str` descriptor lane plus fixed-integer count gate.

The direct literal odd-byte byte-slice evidence also includes `inline/builtin_string_literal_leftb_rightb_odd_byte_exprs`, covering `LeftB("ABC", 1)`, `LeftB("ABC", 3)`, `RightB("ABC", 1)`, and `RightB("ABC", 3)` through the same `OxConst::Str` descriptor lane plus fixed-integer count gate. The row proves `Len`/`LenB` observables `0`/`1` for count `1`, `1`/`3` for count `3`, `AscW` `65` for the three-byte left slice, and `AscW` `17152` for the shifted three-byte right slice.

The Variant source-place odd-byte byte-slice evidence also includes `inline/builtin_leftb_rightb_variant_odd_byte_exprs`, covering `LeftB(text, 1)`, `LeftB(text, 3)`, `RightB(text, 1)`, and `RightB(text, 3)` where `text As Variant` is populated from `"ABC"` via `Box`. It proves the same `Len`/`LenB` observables `0`/`1` and `1`/`3`, `AscW` `65` and `17152`, while preserving the local source carrier as `ABC` plus the four local slice carriers `""`, `"A"`, `""`, and `U+4300`.

The static `String` byte-slice evidence also includes `inline/builtin_static_string_leftb_rightb_byte_counts`, covering `LeftB(text, 2)`, `RightB(text, 2)`, `LeftB(text, 6)`, and `RightB(text, 6)` over a statically typed `String` source place through the same representation-preserving source bridge plus fixed-integer count gate.

The static `String` byte-slice edge evidence also promotes `inline/builtin_static_string_leftb_rightb_byte_count_edges`, `inline/builtin_static_string_leftb_rightb_complement_byte_count_edges`, and `inline/builtin_static_string_leftb_rightb_exact_byte_source_count`, covering a statically typed `String` source place with byte counts `0`, `20`, and exact byte count `10`. These rows prove empty `LeftB(text, 0)`, full-source `RightB(text, 20)`, full-source `LeftB(text, 20)`, empty `RightB(text, 0)`, and exact-byte-count `LeftB(leftText, 10)`/`RightB(rightText, 10)` through the same representation-preserving source bridge plus fixed-integer count gate.

The byte-slice count-edge evidence also includes `inline/builtin_leftb_rightb_variant_byte_count_edges`, covering `LeftB(text, 0)` and `RightB(text, 20)` over the same String-bearing `Variant` source plus explicit `Long` byte-count locals and proving zero-byte empty-string plus overlong-byte full-source results through the same fixed-count gate.

The byte-slice complementary count-edge evidence also includes `inline/builtin_leftb_rightb_variant_complement_byte_count_edges`, covering `LeftB(text, 20)` and `RightB(text, 0)` over the same String-bearing `Variant` source plus explicit `Long` byte-count locals and proving overlong-byte full-source plus zero-byte empty-string results through the same fixed-count gate.

The byte-slice exact byte-length evidence also includes `inline/builtin_leftb_rightb_variant_exact_byte_source_count`, covering `LeftB(left_text, 10)` and `RightB(right_text, 10)` over distinct String-bearing `Variant` sources plus an explicit `Long` byte-count local and proving full-source return at exact five-code-unit byte length through the same fixed-count gate.

The byte-slice error evidence also includes `inline/builtin_leftb_negative_count` and `inline/builtin_rightb_negative_count`, covering a String-bearing `Variant` source populated by `CStr(12345&)` with explicit `Long` count `-1` and proving shared runtime error 5 seating for both `LeftB` and `RightB` through the same fixed-count gate.

The code-unit slice error evidence also includes `inline/builtin_left_negative_count` and `inline/builtin_right_negative_count`, covering the same String-bearing `Variant` source and explicit `Long` count `-1`, and proving shared runtime error 5 seating for both `Left` and `Right` through the same fixed-count gate.

The direct literal slice error evidence also includes `inline/builtin_string_literal_left_negative_count`, `inline/builtin_string_literal_right_negative_count`, `inline/builtin_string_literal_leftb_negative_count`, and `inline/builtin_string_literal_rightb_negative_count`, covering `Left("12345", count)`, `Right("12345", count)`, `LeftB("12345", count)`, and `RightB("12345", count)` with an explicit `Long` count local set to `-1`. These rows prove shared runtime error 5 seating through the same `OxConst::Str` descriptor lane plus fixed-integer count gate.

The static `String` slice error evidence also includes `inline/builtin_static_string_left_negative_count`, `inline/builtin_static_string_right_negative_count`, `inline/builtin_static_string_leftb_negative_count`, and `inline/builtin_static_string_rightb_negative_count`, covering `Left(text, count)`, `Right(text, count)`, `LeftB(text, count)`, and `RightB(text, count)` where `text As String` is set to `"12345"` and the explicit `Long` count local is `-1`. These rows prove shared runtime error 5 seating through the same representation-preserving `String` source-place bridge plus fixed-integer count gate.

The implemented subset also now includes the two-argument `InStr`/`InStrRev` row over `Variant` operands populated by `CStr(123123&)` and `CStr(23&)`, returning `2` and `5` through the shared `rt_lib_invoke` path; the two-argument `StrComp` row over `Variant` operands populated by `CStr(12345&)` and `CStr(12346&)`, returning equality `0` and binary less-than `-1`; the three-argument `Replace` row over `Variant` operands populated by `CStr(123123&)`, `CStr(23&)`, and `CStr(99&)`, returning `199199`; and the source-level `Like` operator row over `Variant` operands populated by `CStr(12345&)` and `CStr(12346&)`, returning `True` and `False` through the narrow `Builtin(Like)` `CallNative` lane. The same descriptor-supported string source bridge now covers direct string literals, direct literal companion forms, direct literal `Left`/`Right` count-edge variants, direct literal `Mid` start/count, value-edge, and error-edge variants, direct literal `LeftB`/`RightB` byte-count and zero/overlong/exact byte-count variants, direct literal odd-byte `LeftB`/`RightB` variants, statically typed `String` source places, statically typed `String` companion forms, static `String` `Left`/`Right` count-edge variants, static `String` `Mid` start/count, value-edge, and error-edge variants, static `String` `LeftB`/`RightB` byte-count and zero/overlong/exact byte-count variants, the Null Variant source-place slice row, the Empty Variant source-place slice row, the numeric Variant source-place slice row, the Integer Variant source-place slice row, the LongLong Variant source-place slice row, the Byte Variant source-place slice row, the Currency Variant source-place slice row, the Date Variant source-place slice row, the Error Variant source-place slice row, the Decimal Variant source-place slice row, the Boolean Variant source-place slice row, the Double Variant source-place slice row, the Single Variant source-place slice row, promoted optional-argument string-search/replacement rows for both direct literals and statically typed `String` source places with fixed-integer or explicit omitted optional `InStr`/`InStrRev`/`StrComp`/`Replace` arguments, and selected `String` destination rows for `CStr`, `Hex`, `ChrW`, `Space`, `Left`, `Mid`, `Replace`, and `Null` Variant error seating. Broader optional string-search/replacement shapes beyond those promoted literal/static-String fixed-integer/omitted rows, named args, ByRef external args, `$` aliases and `String` destinations beyond the promoted result/Null rows, `Like` wildcard/charlist rows, broader `CallNative` built-ins, `Declare`/native external calls, direct string constants outside the promoted string-oriented Variant operand gates, statically typed `String` operands outside the promoted representation-preserving bridge, Null dynamic payload rows beyond the promoted Null slice row and promoted `String` destination error row, Empty dynamic payload rows beyond the promoted Empty slice row, non-String dynamic payload rows beyond the promoted numeric Long, Integer, LongLong, Byte, Currency, Date, Error, Decimal, Boolean, Double, and Single slice rows, and broader non-String dynamic payload rows remain outside the current Linux evidence.

This Linux implementation round does not have PowerShell or Excel/VBA available; Excel oracle work is deferred to a post-implementation Windows/Excel round. M4-4 remains `in-progress`: typed primary entries/direct typed calls, dynamic-entry thunks, late-bound entry coverage, ParamArray alias/no-alias shapes beyond the promoted simple local, Long/String globals, duplicate local, typed Long/LongLong/Win64 LongPtr/Integer/Byte/Boolean/String/fixed-length `String * 3`/Currency/Single/Double/Date locals, fixed-array-element, Variant-held array mutation, local/global parenthesized rows, local/global explicit call-site ByVal rows, and named fixed-tail simple/fixed-array-element/duplicate/global-Long/String alias plus global-Long no-alias, global Win64 LongPtr, typed scalar bundle, and fixed-length String alias/no-alias, typed Long/String local alias/no-alias, typed Win64 LongPtr local alias/no-alias, typed scalar bundle, typed fixed-length String alias/no-alias, and local/selected global-String parenthesized/explicit-ByVal rows and broader source-level ParamArray beyond the current pack/copy-in plus first-dimension `UBound`, explicit-dimension/error, and Option Base 1 zero-base rows, array indexing/`ReDim` beyond the current fixed one-dimensional Variant-array `Dim` allocation, dynamic one-dimensional Variant-array `ReDim` without `Preserve` plus selected one-dimensional `ReDim Preserve` rows, and one-subscript `ArraySet`/`ArrayGet` rows with default-zero, explicit lower-bound, Option Base 1 lower-bound coverage, fixed-array subscript-error, dynamic `ReDim` expansion, reset-without-`Preserve`, shrink-to-subscript-error, negative-lower-bound, dynamic-bound-expression, dynamic Option Base 1 bounds, upper-less-than-lower error, `Preserve` upper-less-than-lower error, `Preserve` keep-value, `Preserve` unallocated defaulting, `Preserve` explicit-lower keep-value, `Preserve` shrink/expand-tail, and `Preserve` lower-bound-change error rows, dimension operands for bounds beyond the current fixed-integer first-dimension and invalid-dimension rows plus dynamic/fixed explicit-dimension, dynamic unallocated `LBound`/`UBound` error rows, selected fixed/dynamic two-dimensional typed bounds bundle rows, selected fixed/dynamic three-dimensional typed bounds bundle rows, selected fixed/dynamic four-dimensional typed bounds bundle rows, and selected fixed/dynamic two-/three-/four-dimensional Long invalid-dimension rows, broader multi-dimensional arrays beyond the selected fixed Variant-array indexing and selected typed read/bounds fixtures, broader dynamic Variant-array Preserve matrices beyond the selected last-dimension and non-last-dimension rows, selected fixed/dynamic two-dimensional typed scalar `ArrayGet` bundle rows, selected fixed/dynamic three-dimensional typed scalar `ArrayGet` bundle rows, selected fixed/dynamic four-dimensional typed scalar `ArrayGet` bundle rows, broader fixed-length String array `ArraySet`/`ArrayGet` rows beyond selected fixed/dynamic one-/two-/three-/four-dimensional `String * 1`/`String * 3`/`String * 5` pad/truncate rows, selected fixed one-/two-/three-dimensional typed `Erase` rows, selected dynamic one-/two-/three-dimensional typed `Erase` rows, selected two-dimensional typed `Boolean`/`Long`/`String`/`Integer`/`Byte`/`LongLong`/`Single`/`Double`/`Currency`/`Date` `For Each` order rows, selected fixed/dynamic one-/two-/three-/four-dimensional fixed-length `String * 1`/`String * 3`/`String * 5` `For Each` rows, and selected fixed/dynamic three-dimensional typed `Long` `For Each` order rows plus selected fixed/dynamic three-dimensional typed scalar/String `For Each` order bundle rows plus selected fixed/dynamic four-dimensional typed `Long`/`String` `For Each` order rows plus selected fixed/dynamic four-dimensional typed scalar/String bundle rows, and non-Variant array carriers beyond the current dynamic/fixed-array and Variant-carrier `Array(...)` default/explicit first-dimension `LBound`/`UBound`, invalid-dimension error, `VarType`/`TypeName`, fixed post-Erase information, and `IsArray` rows, Optional/Omitted beyond the current Optional ByVal `Variant` plus binder-materialized Optional ByVal scalar/String source rows, selected supplied Optional ByVal scalar exact/named-local rows, selected positional/named scalar coercion/error rows, selected positional/named Variant-payload scalar coercion/error rows, selected supplied Optional ByVal `String` positional/named coercion rows, selected fixed-length scalar `String` storage rows, broader signatures beyond the current helper-mediated scalar-plus-Variant/String subset, broader Variant/mixed arithmetic/logical/truthiness/comparison/coercion lowering, fully typed-indirect/dynamic-signature `CallProcRef`, broader `CallExtern`/`CallNative` beyond the current synthetic VBA library unary math/scalar conversion/CVErr/string-result/Round/Round-digits/Round-negative-digits/one-argument math/date-part/date-part-optional/date-name/date-name-optional/information/information-IsArray/date-value/three-argument date-time-serial/date-serial-error/color/color-rgb-components/color-palette/color-error/Error-text/Error-edge/Len-LenB-Variant/ChrW-AscW/Space/LCase-UCase/Val/Trim-family/StrReverse/String/Left-Right/LeftB-RightB/InStr-InStrRev/StrComp/Replace/Like/Mid/string-literal/string-literal-companion/string-literal-left-right-count/string-literal-mid-count/static-String/static-String-companion/static-String-left-right-count/static-String-mid-count/static-String-byte-count/string-optional/static-String-optional rows, and full deep-recursion parity with a big-stack/non-recursive execution design are not complete.

Boundary addendum: the long `in-progress` boundary above now includes the selected intermediate omitted Optional Variant `IsMissing` row, the scalar-false `IsObject(17)` row, the source-level `inline/stdlib_variant_predicates` and `inline/coercion_null_empty_error_predicates` rows, and the corresponding `information-IsMissing`/`information-IsObject` synthetic library admissions as current Linux evidence. Remaining Optional/Omitted work refers to shapes beyond this promoted row and the already listed Optional ByVal Variant/scalar/String rows; remaining `CallExtern`/`CallNative` work refers to library shapes beyond the promoted information-function gate.

Within the named fixed-tail shorthand, the fixed-array-element slice now includes both the alias row and the parenthesized/explicit call-site `ByVal` no-alias contrast rows: `params/paramarray_named_fixed_tail_array_element_alias_copyout`, `params/paramarray_named_fixed_tail_array_element_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_array_element_byval_no_alias`.

Within the same named fixed-tail shorthand, the duplicate-local slice now includes the alias row plus the parenthesized/explicit call-site `ByVal` no-alias contrast rows: `params/paramarray_named_fixed_tail_duplicate_alias_copyout`, `params/paramarray_named_fixed_tail_duplicate_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_duplicate_byval_no_alias`.

Within the same named fixed-tail shorthand, the typed scalar bundle slice now includes the alias row plus the parenthesized/explicit call-site `ByVal` no-alias contrast rows: `params/paramarray_named_fixed_tail_typed_scalar_alias_bundle_copyout`, `params/paramarray_named_fixed_tail_typed_scalar_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_typed_scalar_byval_no_alias`.

Within the same named fixed-tail shorthand, the Win64 `LongPtr` local slice now includes the alias row plus the parenthesized/explicit call-site `ByVal` no-alias contrast rows: `params/paramarray_named_fixed_tail_typed_longptr_alias_copyout`, `params/paramarray_named_fixed_tail_typed_longptr_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_typed_longptr_byval_no_alias`.

Within the same named fixed-tail shorthand, the module-global Win64 `LongPtr` slice now includes the alias row plus the parenthesized/explicit call-site `ByVal` no-alias contrast rows: `params/paramarray_named_fixed_tail_global_longptr_alias_copyout`, `params/paramarray_named_fixed_tail_global_longptr_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_global_longptr_byval_no_alias`.

Within the same named fixed-tail shorthand, the module-global typed scalar bundle slice now includes the alias row plus the parenthesized/explicit call-site `ByVal` no-alias contrast rows: `params/paramarray_named_fixed_tail_global_typed_scalar_alias_bundle_copyout`, `params/paramarray_named_fixed_tail_global_typed_scalar_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_global_typed_scalar_byval_no_alias`.

Within the same named fixed-tail shorthand, the module-global fixed-length `String * 3` slice now includes the alias row plus the parenthesized/explicit call-site `ByVal` no-alias contrast rows: `params/paramarray_named_fixed_tail_global_fixed_string_alias_copyout`, `params/paramarray_named_fixed_tail_global_fixed_string_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_global_fixed_string_byval_no_alias`. These rows prove Linux VM3/JIT parity for the current ParamArray alias mirror only; real Excel/VBA fixed-length copy-out truncation remains deferred to the Windows/Excel oracle round.

Within that open-scope shorthand, selected `For Each` now means only SAFEARRAY source snapshots from `Array(...)` including the zero-element literal row, fixed `Variant` array places including the explicit lower-bound row, dynamic `Variant()` array places including the explicit lower-bound row, selected fixed/dynamic one-dimensional typed `Boolean`/`Long`/`LongLong`/`Single`/`Double`/`Currency`/`Date`/`String`/`Integer`/`Byte` array places, selected fixed/dynamic one-/two-/three-/four-dimensional fixed-length `String * 1`/`String * 3`/`String * 5` array places, selected fixed/dynamic two-dimensional `Boolean`/`Long`/`String`/`Integer`/`Byte`/`LongLong`/`Single`/`Double`/`Currency`/`Date` array order rows and selected fixed/dynamic three-dimensional `Long` array order rows plus selected fixed/dynamic three-dimensional typed scalar/String bundle order rows plus selected fixed/dynamic four-dimensional `Long`/`String` array order rows plus selected fixed/dynamic four-dimensional typed scalar/String bundle order rows as covered by `control/for_each_array_literal`, `control/for_each_array_literal_empty_skips`, `control/for_each_array_variable`, `control/for_each_array_variable_explicit_lower_bound`, `control/for_each_array_dynamic`, `control/for_each_array_dynamic_explicit_lower_bound`, `control/for_each_boolean_array_fixed`, `control/for_each_boolean_array_dynamic`, `control/for_each_boolean_array_fixed_multidim_order`, `control/for_each_boolean_array_dynamic_multidim_order`, `control/for_each_long_array_fixed`, `control/for_each_long_array_dynamic`, `control/for_each_long_array_fixed_multidim_order`, `control/for_each_long_array_dynamic_multidim_order`, `control/for_each_long_array_fixed_3d_order`, `control/for_each_long_array_fixed_4d_order`, `control/for_each_long_array_dynamic_3d_order`, `control/for_each_long_array_dynamic_4d_order`, `control/for_each_typed_scalar_3d_order_bundle`, `control/for_each_typed_scalar_dynamic_3d_order_bundle`, `control/for_each_typed_scalar_4d_order_bundle`, `control/for_each_typed_scalar_dynamic_4d_order_bundle`, `control/for_each_longlong_array_fixed`, `control/for_each_longlong_array_dynamic`, `control/for_each_longlong_array_fixed_multidim_order`, `control/for_each_longlong_array_dynamic_multidim_order`, `control/for_each_single_array_fixed`, `control/for_each_single_array_dynamic`, `control/for_each_single_array_fixed_multidim_order`, `control/for_each_single_array_dynamic_multidim_order`, `control/for_each_double_array_fixed`, `control/for_each_double_array_dynamic`, `control/for_each_double_array_fixed_multidim_order`, `control/for_each_double_array_dynamic_multidim_order`, `control/for_each_currency_array_fixed`, `control/for_each_currency_array_dynamic`, `control/for_each_currency_array_fixed_multidim_order`, `control/for_each_currency_array_dynamic_multidim_order`, `control/for_each_date_array_fixed`, `control/for_each_date_array_dynamic`, `control/for_each_date_array_fixed_multidim_order`, `control/for_each_date_array_dynamic_multidim_order`, `control/for_each_string_array_fixed`, `control/for_each_string_array_dynamic`, `control/for_each_string_array_fixed_multidim_order`, `control/for_each_string_array_dynamic_multidim_order`, `control/for_each_string_array_fixed_4d_order`, `control/for_each_string_array_dynamic_4d_order`, `control/for_each_fixed_string_array_fixed`, `control/for_each_fixed_string_array_dynamic`, `control/for_each_fixed_string_array_fixed_width_bundle`, `control/for_each_fixed_string_array_dynamic_width_bundle`, `control/for_each_fixed_string_array_fixed_multidim_order`, `control/for_each_fixed_string_array_dynamic_multidim_order`, `control/for_each_fixed_string_array_fixed_multidim_width_bundle`, `control/for_each_fixed_string_array_dynamic_multidim_width_bundle`, `control/for_each_fixed_string_array_fixed_3d_order`, `control/for_each_fixed_string_array_dynamic_3d_order`, `control/for_each_fixed_string_array_fixed_3d_width_bundle`, `control/for_each_fixed_string_array_dynamic_3d_width_bundle`, `control/for_each_fixed_string_array_fixed_4d_order`, `control/for_each_fixed_string_array_dynamic_4d_order`, `control/for_each_fixed_string_array_fixed_4d_width_bundle`, `control/for_each_fixed_string_array_dynamic_4d_width_bundle`, `control/for_each_integer_array_fixed`, `control/for_each_integer_array_dynamic`, `control/for_each_integer_array_fixed_multidim_order`, `control/for_each_integer_array_dynamic_multidim_order`, `control/for_each_byte_array_fixed`, `control/for_each_byte_array_dynamic`, `control/for_each_byte_array_fixed_multidim_order`, `control/for_each_byte_array_dynamic_multidim_order`, the selected typed `Boolean`/`Long`/`LongLong`/`Single`/`Double`/`Currency`/`Date`/`String`/`Integer`/`Byte` array `*_explicit_lower_bound` rows, and the selected Variant-array plus typed `Boolean`/`Long`/`LongLong`/`Single`/`Double`/`Currency`/`Date`/`String`/`Integer`/`Byte` array `*_item_after_completion` rows, plus the current descriptor-supported scalar source type-mismatch matrix covered by `control/for_each_boolean_scalar_error`, `control/for_each_byte_scalar_error`, `control/for_each_currency_scalar_error`, `control/for_each_date_scalar_error`, `control/for_each_double_scalar_error`, `control/for_each_fixed_string_scalar_error`, `control/for_each_integer_scalar_error`, `control/for_each_long_scalar_error`, `control/for_each_longlong_scalar_error`, `control/for_each_single_scalar_error`, `control/for_each_string_scalar_error`, and `control/for_each_variant_scalar_error`; source-carrier families outside that matrix, Collection/object `_NewEnum`, project-object enumeration, typed arrays/elements beyond the selected `Boolean`/`Long`/`LongLong`/`Single`/`Double`/`Currency`/`Date`/`String`/`Integer`/`Byte` rows, fixed-length `String * N` array shapes beyond the selected fixed/dynamic one-/two-/three-/four-dimensional `String * 1`/`String * 3`/`String * 5` rows, broader multidimensional enumeration order beyond the selected two-dimensional typed rows, selected three-dimensional Long rows, selected three-dimensional typed scalar/String bundle rows, and selected four-dimensional Long/String rows and selected four-dimensional typed scalar/String bundle rows, and Excel oracle confirmation remain open.

Current fixed-length String `For Each` boundary refinement: the selected set also includes `control/for_each_fixed_string_array_fixed_multidim_order`, `control/for_each_fixed_string_array_dynamic_multidim_order`, `control/for_each_fixed_string_array_fixed_3d_order`, `control/for_each_fixed_string_array_dynamic_3d_order`, `control/for_each_fixed_string_array_fixed_4d_order`, `control/for_each_fixed_string_array_dynamic_4d_order`, the one-/two-/three-/four-dimensional width-bundle rows through `control/for_each_fixed_string_array_fixed_4d_width_bundle` and `control/for_each_fixed_string_array_dynamic_4d_width_bundle`, and the selected one-/two-/three-/four-dimensional post-completion bundle rows through `control/for_each_fixed_string_array_fixed_4d_item_after_completion_bundle` and `control/for_each_fixed_string_array_dynamic_4d_item_after_completion_bundle`. Read the preceding fixed-length String `For Each` shorthand as selected fixed/dynamic one-/two-/three-/four-dimensional `String * 1`/`String * 3`/`String * 5` rows; fixed-length `String * N` arrays beyond those rows, typed control variables, object `_NewEnum`, Windows/Excel locale confirmation, and real Excel oracle confirmation remain open.

Current typed scalar `For Each` post-completion boundary refinement: the selected set also includes `control/for_each_typed_scalar_multidim_item_after_completion_bundle`, `control/for_each_typed_scalar_dynamic_multidim_item_after_completion_bundle`, `control/for_each_typed_scalar_3d_item_after_completion_bundle`, `control/for_each_typed_scalar_dynamic_3d_item_after_completion_bundle`, `control/for_each_typed_scalar_4d_item_after_completion_bundle`, and `control/for_each_typed_scalar_dynamic_4d_item_after_completion_bundle`, covering fixed/dynamic ranks 2 through 4 for the selected scalar/String typed array families. Typed control variables, object `_NewEnum`, broader element families, Windows/Excel locale confirmation, and real Excel oracle confirmation remain open.

The front-end compatibility guard now rejects statically known array-source `For Each` loops whose control variable is not `Variant`, using VBA's `For Each control variable must be Variant or Object` compile diagnostic. This prevents typed-scalar or object array-control conveniences from becoming JIT parity claims; object-typed collection controls remain outside this Linux JIT slice.

Within that open-scope shorthand, the current promoted `CallExtern` list also includes selected `String` destination rows, selected scalar library-result destination rows, selected string-typed `$` alias rows, and fixed-integer direct/alias `Error` destination rows: `inline/builtin_string_result_destinations`, `inline/string_destination_null_variant`, `inline/builtin_scalar_result_destinations`, `inline/builtin_scalar_result_destination_families`, `inline/builtin_scalar_result_destination_coercions`, `inline/builtin_scalar_result_destination_coercion_overflow`, `inline/builtin_scalar_result_destination_null_error`, `inline/builtin_string_typed_aliases`, `inline/builtin_string_typed_alias_null_error`, `inline/builtin_string_typed_alias_destinations`, `inline/builtin_error_text_result_destinations`, `inline/builtin_error_text_alias_destinations`, `inline/builtin_error_text_result_destination_invalid`, and `inline/builtin_error_text_alias_destination_invalid`. The current promoted non-`CallExtern` fixed-length storage rows are `inline/fixed_string_local_pad_truncate`, `inline/fixed_string_local_null_error`, `inline/fixed_string_global_pad_truncate`, and `inline/fixed_string_global_null_error`.

Within that open-scope shorthand, the current promoted library row list now includes the Null, Empty, Long, Integer, LongLong, Byte, Currency, Date, Error, Decimal, Boolean, Double, and Single slice payload rows; remaining dynamic payload work is broader Null, Empty, and non-String shapes outside the promoted unsuffixed slice rows.

### M4-5 — Error model (L)
Fault-code convention on every fallible op; FaultDispatch lowering (§3.4); Resume/ResumeNext/Resume-label; On Error mode sets; GoSub/Return + err 3; `Erl`; `Err.Raise` defaults + inheritance; R10 Exit clears; R13 `GoTo -1`; R14 `End`.
**Verify:** the entire error-family corpus green on all axes (the largest thematic slice); `error_loop` bench recorded (fault-path tax visible early). **Depends:** M4-4.

### M4-6 — Strings, refcounting, temp lifetime (M)
BSTR ops via shims; O1–O8 discipline live; `clear_temps_from`; LSet/RSet/fixed strings; Mid-statement.
**Verify:** string corpus green **including the balance axis on every program compiled so far** (leaks would first appear here — the gate is already merciless); nightly ASAN job enabled. **Depends:** M4-5.

### M4-6.5 — Fuzz differential online (∥ from M4-3, S)
Generator per §9; finds land as permanent corpus programs.

### M4-7 — Arrays and records (M)
SAFEARRAY element load/store (typed + Variant lanes), Bound, ReDim(Preserve), Erase, unallocated semantics; record field get/set, With receivers.
**Verify:** array+record corpus green + balance; `array_loop`/`udt_fields` benches recorded. **Depends:** M4-6.
**Split-in from M4-1:** bead `bd-h4oh.9.1` threads record-layout identities into OxIR typing and removes the temporary `Object(Untyped) <- Variant` assign-normalization exception; evidence is recorded in `docs/evidence/jit/JIT_M4_RECORD_LAYOUT_STRICT_ASSIGN_20260706.md`.

### M4-8 — Objects, classes, lifecycle (L)
New/AsNew, predeclared singletons, Is/TypeOf, member dispatch on project classes, Release{may_terminate}, DrainTerminations fixpoint through ProcInvoker (Terminate runs **compiled** code re-entrantly), project RaiseEvent → WithEvents fan-out.
**Verify:** lifecycle micro-corpus green under `Executor::Jit` incl. the Terminate-timing axis; drain re-entrancy (a Terminate dropping the last ref of another object) covered. **Depends:** M4-7.

Current continuation (2026-07-07): `bd-h4oh.10.17` delivered the first non-fallback class/JIT
slice: one active-project class, `Set w = New Widget`, `Class_Initialize` setting an instance
field, and a no-argument property/method read now execute under `Executor::Jit` and match VM3.
The JIT path builds shared runtime class descriptors, allocates descriptor-backed `ObjectRef`
instances through `oxvba-rt-abi`, invokes initializer/member bodies through compiled hidden-`Me`
frames, and preserves deterministic unsupported diagnostics for the later child-bead shapes.
An unset receiver on the same member-read path now also matches VM3 by seating VBA error 91 under
`On Error Resume Next`. `AsNew`, predeclared singletons, cross-project construction, events,
object-valued properties/default members, argument-bearing member dispatch, arrays/UDT fields, and
`Class_Terminate` remain outside this slice.

Current evidence addendum (2026-07-07): `bd-h4oh.10.18` extends that active-project class slice to
reference identity and typed introspection. The JIT now matches VM3 without fallback for compatible
`Set` assignment across active-project class, `Object`, and `Variant` object slots; `Set ... =
Nothing`; `Is` / `Is Nothing`; descriptor-backed `TypeOf ... Is ...`; and `TypeName` on statically
typed active-project class/interface values. The promoted source-level differential rows live in
`crates/oxvba-differential/tests/jit_project_objects.rs`. Untyped/COM receivers, `As New`,
predeclared singletons, cross-project classes, events, `Class_Terminate`, default members,
object-valued property breadth, and argument-bearing member dispatch remain outside this slice and
stay split to the follow-up beads below.

Current evidence addendum (2026-07-07): `bd-h4oh.10.19` extends project member dispatch beyond the
initial no-argument read. The JIT now lowers active-project `ComCallLate` through call-site argument
descriptors plus name descriptors, resolves the project member by descriptor metadata, reorders
named arguments against visible parameters after hidden `Me`, seeds a compiled hidden-`Me` member
frame, and matches VM3 without fallback for method calls, optional arguments, ParamArray, ByRef
aliasing, indexed `Property Get`/`Property Let`, and a basic `Property Set` route. The promoted
rows are in `crates/oxvba-differential/tests/jit_project_objects.rs`. Default-member dispatch,
broader object-valued property/default-member compatibility, `As New`, predeclared singletons,
cross-project classes, events, and `Class_Terminate` remain split to the follow-up beads.

Current evidence addendum (2026-07-07): `bd-h4oh.10.20` extends active-project member dispatch to
default members and object-valued properties. The JIT now admits default-member `ComCallLate`
selectors by resolving descriptor-backed `VB_UserMemId = 0` metadata, preserves invoke-kind
selection for default-member value and write routes, and mirrors VM3's Variant array-index fallback
when a `Variant` slot actually holds a project object. VM3/JIT differential rows cover Object and
Variant-held project receivers for default-member `Property Get`/`Property Let` plus object-valued
default-member `Property Set`. `As New`, predeclared singletons, cross-project classes, events, and
`Class_Terminate` remain split to the follow-up beads.

Current evidence addendum (2026-07-07): `bd-h4oh.10.21` completed the active-project `As New`
JIT slice for locals and class fields. The JIT now registers project-class `As New` slots without
eager construction, instantiates on first read/`Is Nothing`, keeps pre-access `Set ... = Nothing`
lazy, and re-instantiates after post-access `Set ... = Nothing` for both locals and class fields.
Imported/cross-project `As New`, predeclared singletons, events, and `Class_Terminate` remain split
to later beads.

Current evidence addendum (2026-07-07): `bd-h4oh.10.22` completed the active-project
`VB_PredeclaredId` singleton JIT slice. The JIT now lowers active-project `Predeclared` and
`PredeclaredSet` through shared runtime ABI singleton get/set helpers, matching VM3 for default
instance persistence, statement-form method dispatch, `Set ClassName = Nothing`, replacement via
`Set ClassName = New ClassName`, held old references after reset, and initializer-failure retry.
Cross-project predeclared singletons remain split to `bd-h4oh.10.24`; COM/export default instances
remain outside this JIT class slice.

Current evidence addendum (2026-07-07): `bd-h4oh.10.23` completed the JIT class termination
boundary slice. Compiled `StmtBoundary` now clears statement temps and calls the shared termination
drain; compiled `DrainTerminations` calls the same drain directly; explicit `AddRef`/`Release`
refcount-effect instructions lower through JIT helpers rather than unsupported diagnostics. The
shared drain now snapshots and restores `ErrEngine` while invoking suppressed `Class_Terminate`
callbacks, so termination faults do not overwrite the caller's `Err.Number`. VM3/JIT differential
rows cover `Set ... = Nothing` termination before the next statement, callee-local procedure-exit
termination, initializer-failure cleanup/retry, and cascaded field release to child termination.
Cross-project classes remain split to `bd-h4oh.10.24`; project events remain split to
`bd-h4oh.10.25`; COM server/export release behavior remains out of M4-8 scope.

Current evidence addendum (2026-07-07): `bd-h4oh.10.24` is complete. The JIT image carrier now
compiles every linked `OxProgram`, keeps per-program function tables, tags frames with owning
program indexes, and resolves globals through the owning frame so referenced-project methods and
initializers read/write their own project state. `NewExtern`, `PredeclaredExtern`, and
`PredeclaredExternSet` now resolve referenced-project class imports to linked `(program, class)`
descriptors and reuse the project object/predeclared runtime helpers; imported `VBA`/COM library
coclasses remain an explicit unsupported path. VM3/JIT rows now cover `New Lib.Widget`, referenced
same-type `Set` assignment, referenced property dispatch, cross-bundle `TypeName`, cross-bundle
`TypeOf ... Is`, referenced incompatible-class assignment diagnostics, and referenced predeclared
singleton method/property dispatch with no VM fallback.

Class/JIT follow-up bead order:
1. `bd-h4oh.10.17` - active-project construction and property-read slice. Complete 2026-07-07.
2. `bd-h4oh.10.18` - active-project object identity, reference ownership, `Is`, `TypeOf`, and
   `TypeName`. Complete 2026-07-07.
3. `bd-h4oh.10.19` - active-project method/property dispatch breadth, hidden `Me`, named/positional
   arguments, optional parameters, and `ParamArray`. Complete 2026-07-07.
4. `bd-h4oh.10.20` - default members and object-valued property compatibility. Complete 2026-07-07.
5. `bd-h4oh.10.21` - lazy active-project `As New` locals and fields. Complete 2026-07-07.
6. `bd-h4oh.10.22` - active-project `VB_PredeclaredId` singleton construction and reset. Complete
   2026-07-07.
7. `bd-h4oh.10.23` - `Class_Terminate`, release ownership, and termination drains. Complete
   2026-07-07.
8. `bd-h4oh.10.24` - referenced-project class descriptors and construction. Complete
   2026-07-07; linked-image execution, `NewExtern`, referenced same-type `Set`,
   incompatible-assignment diagnostics, referenced predeclared singleton dispatch,
   cross-bundle `TypeName`, and cross-bundle `TypeOf` are green.
9. `bd-h4oh.10.25` - project `WithEvents` and `RaiseEvent` fan-out in JIT. Active
   2026-07-07.
10. `bd-h4oh.10.26` - class metadata/package contract residual audit after project events.
11. `bd-h4oh.10.27` - binder and lowering class semantics residual suite.
12. `bd-h4oh.10.28` - VM3/JIT class execution parity sweep for the accepted project-class subset.
13. `bd-h4oh.10.29` - M4-8 terminal docs, unsupported diagnostics, and residual handoff.

Class-related COM export/server readiness is intentionally not part of this M4-8 chain. The
deferred handoff bead is `bd-h4oh.15.1` under M4-13, where generated COM vtables and AOT/export
packaging belong. M4-8 may use Linux descriptor/layout tests as evidence for internal class
runtime facts, but it must not claim live COM export parity.

### M4-9 — COM late/early, Declare, pointer helpers (L)
ComCall lowering (rich HRESULT→Err through the same shims vm3 uses); late IDispatch + early vtable transports (axis-5 counts must match); typed-arg writebacks (axis 6); Declare lane + `last_dll_error`; GetObject; pointer helpers.
**Verify:** COM corpus slice green; **live com_matrix legs green on the JIT** in- and out-of-proc. **Depends:** M4-8.

### M4-10 — Events, sessions, product surface (M)
COM WithEvents subscriptions + event pump delivering into compiled handlers (agile sink, thread-aware arg order); `ProjectRuntimeSession` on the JIT backend; comhost env selection; CLI `--backend jit` end-to-end incl. `.oxi` load-and-compile.
**Verify:** live com_matrix events V7/V8 green on the JIT; session twins green. **Depends:** M4-9.

### M4-11 — FULL PARITY GATE (S–M)
Close the scope ratchet: declines == 0; full corpus, all 6 axes + balance, 0 mismatches; `jit_golden` identical; fuzz ≥ 100k cumulative cases clean or findings landed; ASAN nightly clean; CI within budget.
**Verify:** the numbers above, recorded in `docs/evidence/jit/` (W9-style parity proof). **Depends:** M4-10, M4-6.5.

### M4-12 — Phase B: typed fast paths (L)
Register promotion of non-escaped scalars (consumes M4-1); inline Long/Integer/Double add/sub/mul/cmp with **exact overflow parity** vs the `oxvba-eval` kernels; inline array element access + bounds check; direct global-slot addressing; **direct typed calls into native COM vtables and Declare targets on statically-descriptored sites** (NativeThunk reverse direction, replacing libffi there). Currency/Variant/String/objects stay helper-called.
**Verify:** the §10 perf gate AND the entire M4-11 gate re-run unchanged (fast paths ship only behind green differentials; the numeric micro-corpus is the overflow-parity net); COM transport-count axis unchanged (same vtable slots, different call mechanism). **Depends:** M4-11.

### M4-13 — JIT-generated COM vtable implementations + AOT blob/loader (L)
NativeThunk vtable tier: per-member typed native slots for arbitrary TypeLib shapes, replacing comhost's bounded hand-written dual tier (HRESULT + IErrorInfo mapping, wire conversions via the `oxvba-com` helpers); `ObjectModule`/serialized-blob backend behind the `Backend` trait; author-side stub PE with loader + host import table (rt-abi staticlib); load-and-run parity. Conditionally per §14: binary `.oxi` format folded in here if the `image_load` bench shows JSON dominating (one serialization decision, not two).
**Verify:** com_matrix early-bound legs green against JIT-generated vtables (a live COM client calls OxVba-served typed slots); corpus differential green against AOT-loaded code (subset acceptable for runtime-host reasons; full where feasible). **Depends:** M4-12.

### M4-14 — AOT export packaging (M)
Option B thunk exports for the WrappedComServer `.dll`; Option A PE-surgery export table for generic native export (arbitrary signatures via NativeThunk); signing/W^X/AV posture documented.
**Verify:** live COM smoke against an AOT-exported COM server; an exported generic `.dll` callable from a host harness. **Depends:** M4-13.

---

## §13 Risk register

| # | Risk | L / I | Mitigation |
|---|---|---|---|
| 1 | **BSTR/ObjectRef/SafeArray leaks & UAF in generated code** | High / Critical | Handle-balance axis per program per commit, established on vm3 in M4-0 before the JIT exists; helper-mediated alloc/free in audited Rust shims through Phase A; O1–O8 discipline with the temp-release contract gated at M4-6 before arrays/objects; nightly ASAN + poison-on-release canaries; fuzz shrinker turns leaks into permanent corpus programs. |
| 2 | **R1–R14 divergence** | Med / High | Shared `ErrEngine` cells + routing — one implementation; extraction proven by byte-identical golden; the error-family corpus is the largest thematic slice and gates M4-5; the rules encoded in IR structure are shared by construction. |
| 3 | **No Win64 unwinding ⇒ status checks on every fallible site** | Certain / Med (perf) | Accepted as the model — it matches the IR's explicit fault edges (vm3 performs the same check interpretively). Cost = one branch per fallible op, quantified by the `error_loop` bench from M4-5. Phase B reduces check density by proving pure ops. Do not attempt SEH. |
| 4 | **Re-entrancy: event sink / Terminate drain → compiled code mid-run** | Med / High | Single ProcInvoker spine + shared `draining`/`pumping` guards in ExecState — the same fixpoint discipline as vm3 by construction; live com_matrix events on the JIT (M4-10) is the ground-truth gate; drain-fires-event-raises-error covered by corpus program. |
| 5 | **Native-stack overflow on deep recursion** (vm3 heap frames → native frames) | Med / High (process crash vs err 28) | Frame-depth ceiling matched to vm3's + prologue watermark guard ⇒ err 28 parity (M4-4); big-stack thread; cranelift stack probes as backstop; recursion corpus program. |
| 6 | **Cranelift version churn** | Med / Med | Pin one release family; cranelift confined to one crate; upgrades only at milestone boundaries with a full gate re-run; `Backend` trait limits `JITModule`-specific surface to one file. |
| 7 | **Numeric edges: NaN payloads, overflow flags, banker's rounding, Currency i128** | Med / Med | NaN canonicalization on; Currency permanently helper-called on the i128 kernel; conversions/rounding permanently helper-called; Phase-B inline arith gated on the numeric micro-corpus + fuzz; one-kernel-two-facades makes lane divergence structurally impossible. |
| 8 | **vm3/JIT lockstep drift as vm3 absorbs remaining Tier-3/4/5 fixes** | High / Med | Process rule: every vm3 semantic change lands with a corpus program in the same commit ⇒ the differential auto-covers the JIT; golden re-bless diffs make changes reviewable; the scope ratchet prevents silent JIT decline of new constructs. |
| 9 | **Compile-time blowup on pathological functions** | Low–Med / Med | Per-function compile time tracked in the bench compile group; lazy-compile fallback pre-designed behind the fn-table indirection — contained change if a real project hits it. |
| 10 | **`.oxi` JSON load dominating session start** | Med / Low–Med | `image_load` bench makes it visible from M4-0; binary format conditionally folded into M4-13 (§14). |
| 11 | **Silent-fallback rot** (JIT-v1's death) | Med / High (methodology) | Mixed execution exists only under the `jit-debug` feature + env deny-list, loudly logged, unreachable from product config; product decline is whole-program `Unsupported` tracked by a monotone ratchet. |
| 12 | **CI time doubling** | Med / Low | Measured in M4-0 before commitment; one execution feeds both differential and golden assertions; program-parallel runner if > 5 min; fuzz/ASAN nightly. |
| 13 | **AOT AV/SmartScreen** (packer shape) | Med / Med (product feel) | Sign the stub; strict W^X (RW→copy→RX); reserved-section patching fallback if the overlay form gets flagged. |

---

## §14 Explicit non-goals for M4

1. **Speculation / profiling tier** — M6. Seams preserved: patchable fn-table entries, centralized StmtBoundary lowering, ProcInvoker.
2. **Inline caches for late-bound COM** — transport-dominated; revisit with M6 profiling data.
3. **Multi-threading** — ExecState is `!Send`; the termination queue stays thread-local; one session per thread, as today.
4. **Debugger (DAP) integration** — but the StmtBoundary hook site is never compiled away: in Phase A boundaries are real (temp release + drain check makes them semantically load-bearing); Phase B may elide only the *drain check* where a statement provably creates no object references, keeping the boundary as a labeled site. A future DAP tier recompiles with hooks rather than patching.
5. **AddressOf → native-callback thunks beyond vm3 parity** — currently declined by vm3; the JIT declines identically so the differential stays symmetric.
6. **Grammar-complete VBA fuzzing** — the narrow scalar/control-flow generator (§9) is in scope; object/COM/event generation is not.
7. **Binary `.oxi` format** — *conditionally in scope*: if the M4-0 `image_load` bench shows JSON parse dominating session start, fold a binary image format into M4-13, whose AOT blob already needs serialized metadata/descriptor-arena design — one format decision, not two. Otherwise deferred.

---

## Implementation-time confirmations (carried forward)

1. **ParamArray element-alias copy-out**: confirmed in M4-1. `ArrayLiteral { aliases }` carries enough for caller-side static copy-out; no IR extension was needed.
2. **`Me`-receiver convention**: mirror vm3's class-method call path exactly (hidden receiver parameter placement per §6.1).
3. **Cranelift version pin**: M4-3 pins `cranelift-codegen`, `cranelift-frontend`, `cranelift-jit`, `cranelift-module`, and `cranelift-native` to `0.133.1`. `cargo info cranelift-jit` reports `rust-version = 1.94`; the local toolchain used for the M4-3 checks is `rustc 1.95.0`. The dependencies are declared in the workspace and consumed only by `oxvba-jit`, with cargo-tree checks guarding VM3/Cranelift separation.
4. **M4-4 unknown-signature ProcRef boundary**: the Linux direct-OxIR slice now includes narrow exact `String` ByVal-only argument units for runtime-only global ProcRefs loaded in `__GlobalInit`, covering one- and two-argument returns into exact `String` and `Variant` destinations, one- and two-argument String-payload Variant argument rows, selected all-Variant two-argument Variant-payload rows for Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads across no-destination Sub, concrete `String` return to exact `String` and `Variant` destinations, actual `Variant` return-to-Variant shapes, and ignored String/actual Variant function-as-statement shapes, plus a mixed static String/String-payload Variant argument row returning concrete String into exact `String` and `Variant` destinations, selected mixed static String plus Variant-payload rows for Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads across no-destination Sub, concrete `String` return to exact `String` and `Variant` destinations, actual `Variant` return-to-Variant shapes, and ignored String/actual Variant function-as-statement shapes, one- and two-argument actual `Variant` return-to-`Variant` units including one- and two-argument String-payload Variant argument rows plus a mixed static String/String-payload Variant argument row, one- and two-argument no-destination `Sub` dispatch including module-level, temporary, fixed-length, String-payload Variant sources, selected no-destination Variant-to-String coercion/error rows for Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads, selected return/destination Variant-to-String coercion/error rows for Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null payloads across concrete `String` returns into exact `String` and `Variant` destinations plus actual `Variant` return-to-Variant, a mixed static String/String-payload Variant argument row, statement-style function calls with ignored String and actual Variant returns including mixed static String/String-payload Variant argument rows, and exact `String` ByRef mutation plus mixed `String` ByRef and selected ByVal String-compatible carriers, including Variant payloads for Long, Boolean, Double, Single, Currency, Integer, Byte, LongLong, Date, String, Error, Decimal, Empty, and Null, static String source-places, direct String literals, and fixed-length String source-places, across no-destination, concrete String return-to-String/Variant destination, and actual Variant return-to-Variant rows, plus selected ignored String/actual Variant function-as-statement rows for Variant payloads and ignored String/actual Variant function-as-statement rows for statically typed String source-place, direct literal, and fixed-length carriers. The no-argument runtime-only lane also covers statement-style scalar, String, and actual Variant-returning function dispatch with ignored returns and side-effect globals. The adjacent Long-only dynamic lane covers one- and two-argument, i32-compatible ByVal carriers including Byte, Integer, and Boolean source-places plus Long/Integer/Byte/Boolean-payload Variant actuals for a Long parameter with no-destination, exact Long/Variant destination, and actual Variant-return rows, mixed ByRef/ByVal no-destination `Sub` dispatch, statement-style function calls with ignored Long and actual Variant returns, exact `Long` and `Variant` destinations with arguments, plus one-/two-argument and mixed ByRef/ByVal actual `Variant` return-to-`Variant` units, with LongLong/Currency/Single/Double/Date ByVal actuals guarded as compile-decline boundaries. Broader unknown-signature String argument/ByRef shapes beyond the promoted exact ByVal, exact ByRef, mixed ByRef plus selected String-compatible ByVal carrier rows, broader Variant-payload parameter coercions, non-empty Variant-destination unknown calls outside the exact Long/String/Variant-return units plus selected StringCandidate concrete/actual-Variant return/destination/function-as-statement and selected coercion/error rows, typed-indirect dispatch, dynamic-entry thunks, direct typed calls, Windows/Excel locale confirmation, and real Excel oracle confirmation remain outside this implementation round.
5. **M4-4 stack guard boundary**: Linux evidence now covers error 28 seating through the shared frame-count guard when invoked directly and when a compiled static `CallProc` reaches the same helper with the JIT frame stack already at VM3's 50,000-frame ceiling. True source-level deep-recursion parity remains open until the accepted big-stack/non-recursive execution design is implemented and Windows/Excel oracle confirmation is available.
6. **M4-4 ParamArray pack/copy-in boundary**: Linux direct-OxIR evidence now covers `ArrayLiteral` lowering into a zero-based Variant SAFEARRAY carrier, static copy-in to a trailing ByVal dynamic `Variant()` variadic parameter, and alias-bearing descriptors for simple local plus Long/String globals, typed Long/LongLong/Win64 LongPtr/Integer/Byte/Boolean/String/fixed-length `String * 3`/Currency/Single/Double/Date locals, and fixed-array-element ParamArray tails. The current source-level promotion covers the existing populated and empty ParamArray `UBound(items)` conformance rows plus `params/paramarray_bounds_explicit_dim`, `params/paramarray_lbound_dim_zero_error`, `params/paramarray_ubound_dim_too_high_error`, `params/paramarray_option_base_one_bounds`, `params/paramarray_alias_copyout`, `params/paramarray_global_alias_copyout`, `params/paramarray_global_parenthesized_no_alias`, `params/paramarray_global_byval_no_alias`, `params/paramarray_global_string_alias_copyout`, `params/paramarray_array_element_alias_copyout`, `params/paramarray_duplicate_alias_copyout`, `params/paramarray_typed_scalar_alias_copyout`, `params/paramarray_typed_longlong_alias_copyout`, `params/paramarray_typed_longptr_alias_copyout`, `params/paramarray_typed_integer_alias_copyout`, `params/paramarray_typed_byte_alias_copyout`, `params/paramarray_typed_boolean_alias_copyout`, `params/paramarray_typed_string_alias_copyout`, `params/paramarray_typed_fixed_string_alias_copyout`, `params/paramarray_typed_currency_alias_copyout`, `params/paramarray_typed_single_alias_copyout`, `params/paramarray_typed_double_alias_copyout`, `params/paramarray_typed_date_alias_copyout`, `params/paramarray_parenthesized_no_alias`, `params/paramarray_byval_no_alias`, and `params/paramarray_variant_array_element_mutation`; this proves narrow one-dimensional dynamic `Variant()` bound lowering for omitted and explicit dimension `1`, error 9 for invalid dimensions `0` and `2`, Option Base 1 zero-base bounds, one-dimensional `ArraySet` write-back from `items(0)`/`items(1)` to caller locals/globals, typed Long/LongLong/Win64 LongPtr/Integer/Byte/Boolean/String/fixed-length `String * 3`/Currency/Single/Double/Date locals, and fixed-array elements including duplicate simple-local aliases with final-write-wins behavior, nested mutation of an array stored in a ParamArray element, and local/global parenthesized plus local/global explicit call-site `ByVal` tail arguments staying copy-only under direct element mutation. Scalar arguments are still declined unless they arrive through the packed dynamic Variant-array carrier. Broader typed scalar alias types beyond Long/LongLong/Win64 LongPtr/Integer/Byte/Boolean/String/fixed-length `String * 3`/Currency/Single/Double/Date, compound alias copy-out beyond fixed-array elements and Variant-held arrays, broader ByVal/no-alias contrasts, non-Variant variadic parameters, ByRef variadic parameters, multi-dimensional ParamArray rows and broader multi-dimensional arrays beyond the selected fixed Variant-array indexing plus dynamic Variant-array Preserve-last-dimension/error rows, array indexing beyond the promoted one-subscript write-back row, and broader source-level array operation coverage remain open.
   The same Linux source-level promotion now also includes `params/paramarray_global_string_parenthesized_no_alias` and `params/paramarray_global_string_byval_no_alias`, proving module-global `String` parenthesized and explicit call-site `ByVal` ParamArray tail arguments stay copy-only under direct element mutation.
   The same Linux source-level promotion now also includes `params/paramarray_omitted_tail_empty`, proving an ordinary positional call can contain an omitted variadic tail slot that is packed as `Empty` in `items(0)` while later tail elements keep their index and carrier. Named fixed-tail omitted combinations remain outside this Linux slice.
7. **M4-4 fixed-length String ParamArray oracle boundary**: `params/paramarray_typed_fixed_string_alias_copyout` proves current Linux VM3/JIT parity for `Dim x As String * 3`, `Call Mutate(x)`, callee-side `items(0) = "abcdef"`, and caller observation of `after As String = "abcdef"` through the current ParamArray alias mirror. The named fixed-tail local and module-global fixed-string rows extend the same Linux-only alias mirror evidence to `params/paramarray_named_fixed_tail_typed_fixed_string_alias_copyout` and `params/paramarray_named_fixed_tail_global_fixed_string_alias_copyout`, with parenthesized and explicit `ByVal` named-tail rows proving copy-only no-alias behavior. This is not an Excel/VBA oracle claim; whether real VBA applies fixed-length storage truncation at ParamArray alias copy-out remains deferred to the post-implementation Windows/Excel round.

8. **M4-4 ParamArray named fixed-tail boundary**: `params/paramarray_named_fixed_tail_pack` proves the current project-procedure binder and JIT path accept a named fixed argument followed by positional ParamArray tail elements, then pack those tail elements into the zero-based `Variant()` carrier. `params/paramarray_named_fixed_tail_alias_copyout` extends that slice to a tail l-value alias and proves callee-side mutation of `items(0)` copy-outs to the caller while the named fixed ByRef slot is independently updated. `params/paramarray_named_fixed_tail_array_element_alias_copyout` proves the same named fixed-tail route preserves fixed-array-element tail aliases and copy-outs, while `params/paramarray_named_fixed_tail_array_element_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_array_element_byval_no_alias` prove parenthesized and explicit call-site `ByVal` fixed-array-element tail sources remain copy-only. `params/paramarray_named_fixed_tail_duplicate_alias_copyout` proves duplicate aliases in the named fixed-tail route preserve final-write-wins copy-out ordering, while `params/paramarray_named_fixed_tail_duplicate_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_duplicate_byval_no_alias` prove duplicate local tail sources remain copy-only when parenthesized or explicitly passed `ByVal`. `params/paramarray_named_fixed_tail_global_alias_copyout` proves module-global `Long` aliases use the same named fixed-tail route, `params/paramarray_named_fixed_tail_global_string_alias_copyout` proves module-global `String` aliases use the same route, `params/paramarray_named_fixed_tail_global_longptr_alias_copyout`, `params/paramarray_named_fixed_tail_global_longptr_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_global_longptr_byval_no_alias` prove module-global Win64 `LongPtr` alias/no-alias tails use the same route, `params/paramarray_named_fixed_tail_global_typed_scalar_alias_bundle_copyout` proves the same route across selected module-global typed scalar tails, `params/paramarray_named_fixed_tail_global_typed_scalar_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_global_typed_scalar_byval_no_alias` prove the same module-global typed scalar bundle remains copy-only when parenthesized or explicitly passed `ByVal`, `params/paramarray_named_fixed_tail_typed_long_string_alias_copyout` proves typed local `Long` and `String` aliases use the same route, `params/paramarray_named_fixed_tail_typed_longptr_alias_copyout`, `params/paramarray_named_fixed_tail_typed_longptr_parenthesized_no_alias`, and `params/paramarray_named_fixed_tail_typed_longptr_byval_no_alias` prove Win64 `LongPtr` local alias/no-alias tails use the same route, `params/paramarray_named_fixed_tail_typed_scalar_alias_bundle_copyout` proves the same route across selected non-String typed scalar locals, `params/paramarray_named_fixed_tail_typed_scalar_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_typed_scalar_byval_no_alias` prove the same non-String typed scalar bundle remains copy-only when parenthesized or explicitly passed `ByVal`, and `params/paramarray_named_fixed_tail_typed_fixed_string_alias_copyout` proves typed fixed-length `String * 3` local aliases use the same route. `params/paramarray_named_fixed_tail_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_byval_no_alias` prove the same tail form remains copy-only when the local tail source is parenthesized or explicitly passed `ByVal`; `params/paramarray_named_fixed_tail_global_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_global_byval_no_alias` prove the same copy-only contrast for module-global `Long` tail sources; `params/paramarray_named_fixed_tail_typed_long_string_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_typed_long_string_byval_no_alias` prove the same copy-only contrast for typed local `Long`/`String` pairs; `params/paramarray_named_fixed_tail_typed_fixed_string_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_typed_fixed_string_byval_no_alias` prove the same copy-only contrast for typed fixed-length `String * 3` tail sources; `params/paramarray_named_fixed_tail_global_string_parenthesized_no_alias` and `params/paramarray_named_fixed_tail_global_string_byval_no_alias` prove the same copy-only contrast for module-global `String` tail sources. The ordinary non-ParamArray positional-after-named diagnostic remains covered separately, and `bind_roundtrip::named_argument_to_paramarray_parameter_is_bind_error` now covers direct named binding to the `ParamArray` parameter itself using the `params_paramarray_named_error.bas` shape. Broader named/omitted ParamArray source forms and Excel/VBA oracle confirmation remain deferred.
9. **M4-4 information/date-string operand boundary**: the Linux source-level promotion now includes `inline/introspection_vartype_isnumeric_tags`, with the JIT information gate admitting `StringLiteral` operands through the variant-descriptor path for `vbNullString`/`Const(Str(""))` alongside the previously promoted scalar/Variant/Variant-array information operands. It also includes `inline/string_vbnullstring_predicates`, proving a Variant local assigned `vbNullString` retains an empty String payload and returns false for `IsEmpty`, `IsNull`, and `IsError`. The adjacent date-string promotion includes `inline/stdlib_date_string_policy`, with direct string-literal operands for `DateValue`/`TimeValue` and Date/Variant operands for one-argument date-part built-ins. Broader `DateValue`/`TimeValue` string source-place/Variant operands, slash-date/locale-sensitive parsing, non-Date Variant date-part carriers beyond promoted `DateValue`/`CDate` results, string source-place/default-member, object, and record information operands, non-constant `TypeOfIs`, and Windows/Excel oracle confirmation remain deferred.
10. **M4-4 ordinary ByRef copy-in boundary**: source-level ordinary calls now have a JIT descriptor for `ByVal` actuals supplied to default/explicit `ByRef` parameters. The promoted rows are `calls/consolidate_nested_call_chain`, `control/consolidate_for_select_call`, `control/consolidate_while_byref_mix`, `error/error_resume_function_propagation`, and `error/error_nested_mode_transitions`; they prove callee-local copy-in/no-copy-back behavior across scalar/String/Variant slot families already admitted by the static call subset, nested calls, selected control flow, and current label-handler/Resume dispatch. True typed primary-entry calls, dynamic-entry thunks, late-bound calls, broader ByRef copy-in/coercion matrices, GoSub/Return redispatch, and Windows/Excel oracle confirmation remain deferred.
11. **M4-4 GoSub/Return redispatch boundary**: source-level GoSub/Return now has a JIT per-activation LIFO stack and dynamic runtime-block redispatch. The promoted rows are `calls/gosub_basic`, `calls/gosub_repeated`, `control/gosub_nested_labels`, `control/gosub_loop_accumulate`, `control/consolidate_for_gosub_mix`, `error/consolidate_gosub_error_mix`, `error/gosub_return_without_gosub`, `error/gosub_return_without_gosub_resume_next`, and `error/gosub_return_without_gosub_label_handler`; they prove repeated and nested labels, loop composition, Resume Next composition, empty-stack `Return` error 3 seating, and empty-stack handler dispatch through Resume Next plus label-handler `Resume`. Broader non-empty GoSub/Return matrices inside label-handler chains, cross-procedure invalid forms, direct typed calls, typed primary entries, dynamic-entry thunks, late-bound calls, and Windows/Excel oracle confirmation remain deferred.
12. **M4-4 direct static-call subset boundary**: exact internal `CallProc` shapes targeting a no-argument `Sub` with no destination, a no-argument `Function` with a supported scalar/String/Variant return destination, statement-style no-argument `Function` calls with ignored returns, a one- or two-argument `Sub Foo(ByVal x As Long, ...)` with already `i32`-lowerable actuals and no destination, a one- or two-argument `Function Foo(ByVal x As Long, ...)` with already `i32`-lowerable actuals and a supported scalar/String/Variant return type copied or ignored, or an exact one-argument `Sub`/`Function Foo(ByRef x As Long)` with an exact `Long` place actual now call the callee body through a local Cranelift function reference after a narrow runtime enter helper installs the callee frame and activation. The remaining ordinary static-call shapes admitted by the current scalar/String/Variant/Optional/ParamArray descriptor subset now enter through `rt_jit_direct_enter_proc_i32`, which materializes the callee frame from descriptors and then calls the callee body by local Cranelift function reference instead of letting `rt_jit_call_proc_i32` do an indirect function-pointer call. Statically unique `CallProcRef` targets now first validate the live ProcRef slot with `rt_jit_expect_proc_ref_i32`, then reuse the same descriptor-enter/body-call/exit sequence, leaving ambiguous same-signature and unknown-signature ProcRef calls on the ProcRef helper path. The ByVal `Long` fast paths lower the actuals to `i32`, enter through the matching one-/two-`i32` direct helper, and seed the callee's parameter locals as `Long` `Variant`s; the exact `ByRef Long` fast path installs a live callee-local alias to the caller slot; descriptor-enter static calls preserve the existing descriptor copy-in, alias, omitted, String, Variant, and ParamArray materialization policy; functions clone the return only when writing into a caller destination. This proves the current direct static dispatch paths keep stack guard error 28, ByVal copy-in, exact ByRef live mutation, descriptor materialization, return-copy/ignored-return, activation save/restore, `ST_FAULT`, and `ST_HALT` semantics aligned with the former static helper. Static call argument/return shapes outside the current descriptor-supported subset, true typed primary entries for broader parameterized signatures, dynamic-entry thunks, late-bound calls, and Windows/Excel oracle confirmation remain deferred.
