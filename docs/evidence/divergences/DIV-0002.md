# DIV-0002: For Loop Unsupported (Closed)

- Scope impact: outside `mvp-int32-core-v1` profile scope.
- Fixture: `conformance/divergences/for_loop.bas`
- Reproduction command:
  - `cargo run -q -p oxvba-cli -- run conformance/divergences/for_loop.bas --dump-slots`
- Observed OxVBA result:
  - `SLOTS:5` (current)
- Office VBA expected behavior:
  - Compiles and executes loop iteration semantics.
- Tracking status:
  - Closed in `mvp-controlflow-v2`.
  - Historical failure (before closure): `oxvba: execution failed: type error: unsupported statement: For i = 1 To 3`
