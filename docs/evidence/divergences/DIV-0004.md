# DIV-0004: WithEvents and RaiseEvent Class Event Model Not Yet Executable

- Scope impact: PMR class event semantics (`CCT-041`, `ODG-039`).
- Fixture: `conformance/tests/project_model_withevents_requires_class_graph.bas`; `conformance/tests/project_model_raiseevent_requires_class_graph.bas`
- Reproduction command:
  - `cargo test -p oxvba-compiler compile_withevents_declaration_surfaces_project_model_diagnostic -- --nocapture`
  - `cargo test -p oxvba-compiler compile_raiseevent_statement_surfaces_project_model_diagnostic -- --nocapture`
  - `./scripts/run-pmr-project-model-oracle.ps1`
- Observed OxVBA result:
  - Compile-time diagnostic gates: `PMR-E-WITHEVENTS-MODULE-KIND-UNRESOLVED` and `PMR-E-RAISEEVENT-CLASS-MODEL-REQUIRED`.
  - `compile_project` path now preserves these gates explicitly (`compile_project_preserves_withevents_diagnostic_gate`, `compile_project_preserves_raiseevent_diagnostic_gate`) instead of silently dropping event semantics during lowering.
- Office VBA expected behavior:
  - Class-level `WithEvents` + `RaiseEvent` flows execute with deterministic handler ordering under reassignment.
- Tracking status:
  - Open divergence recorded from oracle capture `pmr_project_model_20260303T070427Z` (`RunProbe` observed `1,3,`).
  - Follow-up queue: `PMR-FUP-001`, `PMR-FUP-002` in `WORKSET_2026-03-03_PMR_FOLLOWUP_QUEUE_FROM_OBSERVATIONS.md`.
