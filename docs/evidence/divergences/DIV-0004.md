# DIV-0004: WithEvents and RaiseEvent Class Event Model Not Yet Executable

- Scope impact: PMR class event semantics (`CCT-041`, `ODG-039`).
- Fixture: `conformance/tests/project_model_withevents_requires_class_graph.bas`; `conformance/tests/project_model_raiseevent_requires_class_graph.bas`
- Reproduction command:
  - `cargo test -p oxvba-compiler compile_project_rejects_withevents_in_procedural_module -- --nocapture`
  - `cargo test -p oxvba-compiler compile_project_rejects_raiseevent_undeclared_event -- --nocapture`
  - `cargo test -p oxvba-compiler compile_project_allows_raiseevent_for_declared_event -- --nocapture`
  - `./scripts/run-pmr-project-model-oracle.ps1`
- Observed OxVBA result:
  - Project-aware compile semantics are implemented:
    - procedural `WithEvents` is rejected with `PMR-E-WITHEVENTS-MODULE-KIND`,
    - non-class or undeclared `RaiseEvent` is rejected with `PMR-E-RAISEEVENT-MODULE-KIND` / `PMR-E-RAISEEVENT-UNDECLARED`,
    - declared class-event `RaiseEvent` compiles.
- Office VBA expected behavior:
  - Class-level `WithEvents` + `RaiseEvent` flows execute with deterministic handler ordering under reassignment.
- Tracking status:
  - Divergence is now narrowed to runtime event dispatch and ordering parity (`RunProbe` ordering behavior such as `1,3,`) rather than compile-time gate behavior.
  - Follow-up queue: `PMR-FUP-001`, `PMR-FUP-002` in `WORKSET_2026-03-03_PMR_FOLLOWUP_QUEUE_FROM_OBSERVATIONS.md`.
