# DIV-0003: Implements Interface Coverage Not Yet Executable (Closed For Current Baseline)

- Scope impact: PMR class/interface semantics (`CCT-040`, `ODG-038`).
- Fixture: `conformance/tests/project_model_implements_requires_class_graph.bas`
- Reproduction command:
  - `cargo test -p oxvba-compiler compile_project_rejects_implements_missing_member_coverage -- --nocapture`
  - `cargo test -p oxvba-compiler compile_project_allows_implements_interface_from_referenced_project -- --nocapture`
  - `cargo test -p oxvba-host formal_event_runtime_implements_prefixed_member_executes_in_class_flow -- --nocapture`
  - `./scripts/run-pmr-project-model-oracle.ps1`
- Observed OxVBA result:
  - Project-aware compile semantics are implemented:
    - valid interface coverage compiles,
    - missing coverage is diagnosed with `PMR-E-IMPLEMENTS-MEMBER-MISSING`.
  - Runtime class flow now executes Implements-prefixed members in deterministic host lane (`formal_event_runtime_implements_prefixed_member_executes_in_class_flow`).
- Office VBA expected behavior:
  - Valid `Implements` class members compile and run; missing/incorrect coverage is diagnosed with interface-specific rules.
- Tracking status:
  - Closed for the originally recorded mismatch shape (`CCT-040-A` compile/runtime baseline).
  - Residual oracle uncertainty for multi-interface edge cases remains tracked under `CCT-040` / `ODG-038` (non-divergence uncertainty, not the original hard mismatch).
