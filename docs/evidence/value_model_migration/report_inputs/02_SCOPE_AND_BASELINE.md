# Scope And Baseline

Status: placeholder

Required content:

1. fixed baseline tag: `pre-value-model-migration-2026-04-20`
2. baseline commit selected for the final run set
3. migrated candidate commit selected for the final run set
4. value families in scope:
   - `BSTR`
   - `VARIANT`
   - COM interface identity
   - event payload transport
   - struct / UDT / native layout-sensitive boundaries
5. references to the fact packs and the workset.
