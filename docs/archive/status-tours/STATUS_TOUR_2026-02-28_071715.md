# STATUS_TOUR_2026-02-28_071715.md

## Snapshot
- Ladder status: `v56` gate is green (`docs/evidence/profiles/v56/gate_report.md`).
- Conformance corpus: `94` fixtures on both VM and JIT cells.
- Formal lane: non-blocking run is green at `mvp-language-stdlib-consolidation-gate-v56`; strict WSL Kani async run is active in background.

## Async Kani (Non-Blocking, Live)
Current run (started, non-dialog-blocking):
```text
async formal status: name=v56-kani-full status=running pid=42412
stdout: C:\Work\DnaCalc\OxVba\temp\async\formal-kani\v56-kani-full\stdout.log
stderr: C:\Work\DnaCalc\OxVba\temp\async\formal-kani\v56-kani-full\stderr.log
```

Liveness polling is now autonomous (every 10 minutes):
- watcher script: `scripts/watch-formal-kani-async.ps1`
- watcher log: `temp/async/formal-kani/v56-kani-full/liveness.log`

Example log fragment:
```text
[2026-02-28T05:15:57Z] watcher start name=v56-kani-full poll_seconds=600
```

## What We Can Run Now (Examples)

### 1) Date/Time core (`v48`)
Source (`conformance/tests/stdlib_date_add_diff.bas`):
```vb
Sub Main()
Dim a
Dim b
a = DateAdd(1, 3, DateSerial(2026, 2, 28))
b = DateDiff(1, DateSerial(2026, 2, 28), a)
End Sub
```
Output (VM + JIT):
```text
SLOTS:20260231,3
```

### 2) Dispatch boundary subset (`v55`)
Source (`conformance/tests/com_dispatch_createobject_invoke.bas`):
```vb
Sub Main()
Dim x
x = DispatchInvoke(CreateObject(11), 2, 3)
End Sub
```
Output (VM + JIT):
```text
SLOTS:5016
```

### 3) Lifecycle hooks (`v54`)
Source (`conformance/tests/class_lifecycle_resume_next_ok.bas`):
```vb
Sub Main()
Dim x
x = 3
End Sub
Sub Class_Initialize()
On Error Resume Next
Err.Raise 5
End Sub
Sub Class_Terminate()
On Error Resume Next
Err.Raise 7
End Sub
```
Output (VM + JIT):
```text
SLOTS:3
```

### 4) Consolidated mixed path (`v56`)
Source (`conformance/tests/consolidate_language_stdlib_mix.bas`):
```vb
Sub Main()
Dim a
Dim b
a = DateDiff(1, DateSerial(2026, 2, 28), DateAdd(1, 3, DateSerial(2026, 2, 28)))
b = DispatchInvoke(CreateObject(2), 3, a)
End Sub
Sub Class_Initialize()
On Error Resume Next
Err.Raise 5
End Sub
```
Output (VM + JIT):
```text
SLOTS:3,5008
```

## What We Still Cannot Run (or Not Fully Yet)

### Unsupported syntax/features (hard failure right now)
Line continuation example:
```vb
Sub Main()
Dim x
x = 1 + _
2
End Sub
```
Observed result:
```text
oxvba: execution failed: type error: unsupported statement: x = 1 + _
```

`With` block example:
```vb
Sub Main()
Dim x
With x
End With
End Sub
```
Observed result:
```text
oxvba: execution failed: type error: unsupported statement: With x
```

Forms/object-model surface example:
```vb
Sub Main()
Call UserForm1.Show
End Sub
```
Observed result:
```text
oxvba: execution failed: type error: unsupported statement: Call UserForm1.Show
```

### Declared as not complete in plan
From plan/coverage trajectory:
- full conditional compilation semantics (`#If`, `#Const`) not implemented yet
- full forms runtime/designer deferred
- full non-Windows COM library interop deferred

## End-to-End Walkthrough (Source -> Bind -> Bytecode -> VM/JIT)

### Source
We start from VBA-like source (example above in `consolidate_language_stdlib_mix.bas`).

### Bind / semantic resolution
`resolve.rs` now recognizes the expanded intrinsic surface (`v48..v55`), e.g.:
```text
1193: fn parse_stdlib_intrinsic_call_expr(
1203: if !matches!(
1219:             | "dateserial"
1223:             | "dateadd"
1235:             | "fv"
1238:             | "array"
1247:             | "shell"
1250:             | "collectionadd"
1254:             | "createobject"
1255:             | "dispatchinvoke"
1266: let arity_ok = match name.as_str() {
```

### IL / emission (current executable IL = bytecode)
Compiler emission lowers each intrinsic family to concrete instructions:
```text
753: ("dateserial", [year, month, day]) => IntrinsicDateSerialDigits
775: ("dateadd", [interval, number, date]) => IntrinsicDateAddDigits
828: ("fv", [rate, nper, pmt]) => IntrinsicFvI32
906: ("array", args) => LoadConstI32 { value: ARRAY_TAG_BASE + args.len() }
936: ("shell", [command]) => IntrinsicShellHost
947: ("collectionadd", [count, item]) => IntrinsicCollectionAdd
991: ("createobject", [prog_id]) => IntrinsicCreateObjectHost
998: ("dispatchinvoke", [object, member, arg]) => IntrinsicDispatchInvokeHost
```

Bytecode instruction set additions are explicit in `bytecode.rs` (`IntrinsicDateSerialDigits`, `IntrinsicFvI32`, `IntrinsicCollectionAdd`, `IntrinsicDispatchInvokeHost`, etc.).

### Interpreter execution
VM implements these instructions directly:
```text
172: Instruction::IntrinsicDateSerialDigits { ... } => ...
206: Instruction::IntrinsicDateAddDigits { ... } => ...
230: Instruction::IntrinsicAbsI32 { ... } => ...
432: Instruction::IntrinsicCollectionAdd { ... } => ...
465: Instruction::IntrinsicDispatchInvokeHost { ... } => ...
```

### JIT execution status
Host asks JIT when enabled:
```text
41: pub fn execute_source_with_snapshot(&self, source: &str) -> Result<Vec<i32>, String> {
44:     if self.config.enable_jit {
50:         .execute_and_snapshot(&bytecode)
54:     execute_and_snapshot(&bytecode)
```
JIT strategy remains safe-by-construction:
```text
23: if cranelift::supports_bytecode(bytecode) { ... } else VM fallback
24: fn supports_core_instruction(...) -> matches!(... core subset ...)
```
Newer instructions outside the Cranelift core subset keep parity through VM fallback, so JIT mode still executes programs correctly.

## IL Layers: What Exists vs. What Is Still Evolving
Current practical pipeline (implemented):
- parse + symbol resolve (`resolve.rs`)
- typecheck (`typecheck.rs`)
- optimize (`optimize.rs`)
- lower-to-hir call exists (`let _hir = lower_to_hir::lower_to_hir(&optimized)`) as an architectural waypoint
- executable bytecode emission (`emit.rs`) and runtime execution (VM/JIT)

Interpretation:
- executable IR today is bytecode
- HIR/MIR/CFG ambitions from MACH1000 are still partially scaffolded and planned for deeper optimization work

## Robustness + Verification + Performance Structures In Place

### Formal obligation registry (`v48..v56` now wired)
`docs/evidence/formal/obligations.csv` contains dedicated rows:
```text
FO-V48-001..003
FO-V49-001..003
...
FO-V56-001..003
```
These are executed by `run-formal.ps1` (non-blocking policy) and feed `latest_run.md/csv`.

### Matrix gate discipline
`docs/evidence/profiles/v56/gate_report.md`:
```text
- Profile scope: mvp-language-stdlib-consolidation-gate-v56
- Final gate status: PASS
| windows | x64 | vm  | green | 94 |
| windows | x64 | jit | green | 94 |
```

### Async formal operations for long lanes
- strict WSL Kani starts with `run-formal-kani-async.ps1`
- now paired with independent 10-minute liveness poller script
- status/log paths are explicit and reproducible

### Performance hooks
- optimization toggle (`OXVBA_DISABLE_OPT`) remains part of parity and benchmark discipline
- benchmark default now points at `docs/evidence/profiles/v56/benchmark_latest.md`

## Deferred / Planned Next
- True VBA string/Variant/COM fidelity expansion beyond current deterministic projection subsets
- Full `With` block semantics and line continuation support
- Full conditional compilation semantics (`#If`/`#Const`) instead of current no-op tolerance
- Forms runtime and designer maturation
- Broader Cranelift support to reduce fallback reliance on newer instructions
- Longer strict Kani completions at `v56` scale (async lane now operational + monitored)
