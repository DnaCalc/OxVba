Sub Main()
    Dim observed
    Dim item
    Dim a(2 To 4) As Date
    a(2) = CDate(36533#)
    a(3) = CDate(36534#)
    a(4) = CDate(36535#)
    For Each item In a
        observed = item
    Next
End Sub
